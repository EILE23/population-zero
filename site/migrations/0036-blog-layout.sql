-- 블로그 배치 (2026-09-17) — CSS 대신 '블록 순서 + 고른 값' 을 저장한다.
-- 사람이 화면에서 마우스로 옮기고 고른 결과가 이 JSON 이고, 우리 컴포넌트가 그걸 읽어 그린다.
-- css 컬럼은 남겨 둔다(고급 사용자용으로 언젠가 쓸 수 있고, 지우면 되돌릴 수 없다).
ALTER TABLE pages ADD COLUMN layout TEXT;
