# Square backlog — picked by the daily code job (see GROW.md)

- [x] `C` with empty hands near a bench/sofa/bed/swing: sit or lie on it (poses `sit`/lying), stand up on any move. Send the pose so others see it. Swings sway. — added `b.sitting`: `C` toggles it near a `SITTABLE` spot (`world.ts`), locks movement, any direction/jump key stands you up; pose sent over the socket and rendered for others. Residents already did this via their routine's `act:'sit'` at these same spots, so the reverse rule was already satisfied.
- [x] Eating: `C` with a sandwich/cup near a café table or bench eats it (short `eat` pose, item consumed). Residents with food do the same at their spots. — added `FOOD` (`goose.ts`); `C` near a cafe/table/bench with a sandwich or coffee sets `b.eating` (freezes briefly, `sit` pose, toast) instead of dropping it; NPCs already used the `sit` pose + "eating" label for `act:'eat'`, now any NPC holding food shows it the moment they stop anywhere on their routine, not just at a designated eat spot.
- [x] Exercise props in the park (pull-up bar, bench press) as `PropKind`s with a `pushup`/`pullup` pose; `C` near them exercises for a few seconds; residents with the `jogger` job use them. — added `pullbar`/`benchpress` `PropKind`s + two park spots (`world.ts`), `pushup`/`pullup` `Activity`s and stickman poses; `C` with empty hands near either now exercises for ~2.4s (movement locked, broadcast like sitting); `jogger`'s routine spots now include both, so joggers already do this on their own via the existing act→pose pipeline.
- [x] Throwing (owner's first pick, done by hand 2026-09-22 — `X` while carrying throws; `throw` pose; `ev throw` so everyone sees the arc; hits residents (down), people (`hitp` kind throw), props (one kick's damage on landing); residents throw with the same pose and event): `V` (or `X` while carrying) throws the top carried item forward — a real wind-up pose (`throw`, arm back then forward, ~0.3s), the item flies in an arc (the `thrown` list already exists for residents: reuse it), hits a resident (they go `down` + a `shoved` line) or a player (send `hitp`) or a prop (cracks it like a kick), then lies on the ground as a loose item. Residents already throw when chasing; give them the same `throw` pose. Send the pose over the socket so others see the wind-up, and emit the flying item so others see the arc.
- [x] Watching TV and reading: `C` near a TV / bookshelf shows a small line above the head ("watching", "reading") and a matching still pose. — TV's spot act changed `stand`→`watch` (`world.ts`); new `watch` pose (arms folded, head tilted to the screen) in `stickman.ts`; `C` with empty hands near a `tv`/`shelf` toggles `b.still` (locks movement, any direction/jump key stands you up, same as sitting), rendered with the existing `actIcon` label ("watching"/"reading") above the head. Residents already stop at both spots on a house owner's routine (`world.ts`'s `houseInterior` stops), so they get the same pose and label for free through the existing act→pose pipeline — no resident-specific code needed.
- [x] Watering: `C` near a garden with no item waters it (small drops); gardener residents do the same. — `C` with empty hands near a `garden` spot sets `b.watering` (locks movement ~1.4s, `water` pose — the watering-can drops were already drawn by the existing pose); gardener residents already visited `garden`/`garden2` on their routine with `act:'water'` so they got the pose and "watering" label for free. Added task kind `water` (`tasksFor`) and quest kind `water` (`questFor`) in `goose.ts` so a day's list can ask you to water a named bed and a resident can ask "could you water mine".
- [ ] Bins: `C` near a bin with an item drops it *in* the bin (disappears for good). Sweepers empty bins.
- [ ] Tripping while chasing (owner, 2026-09-22): a chasing resident has a small seeded chance per second (higher for the retired job and for anyone carrying something) to trip — a `trip` pose (face-down, feet up, ~1.2 s), they drop what they carry, say something flat, and the chase clock loses two seconds; if they trip within reach of the player the player gets away clean (counts for the `chased` task). Also trip on the pond/fountain edge when running (see Slipping below), on a broken prop, and on a loose item lying on the ground. Players trip too when running over a loose item at full speed. Visible to everyone (pose over the socket / `npcpos`).
- [ ] Fishing spots (owner, 2026-09-22): a `rod` prop leaning on a rack by the little pond and the duck pond. `C` with empty hands takes a rod; `C` at the water's edge sits and casts (`fish` pose already exists in `stickman.ts`); after a seeded 4–12 s a fish bites — a small tug, then the catch is named from the old fish list (`site/src/lib/pond.ts` / `pond-items.ts`, 250 absurd fish; reuse the names, do not rebuild the game) and shown as a line above the head and a loose item that can be carried, thrown, dunked or given. The retired, jogger and priest jobs fish on their routine at the same spots with the same pose. Add task `catch one fish` and a resident ask `bring me something from the pond`. Everything visible to others (pose over the socket, catch as an `ev`). Later: rarer fish at night or in rain, a plaque for the biggest.
- [ ] Animals (owner, 2026-09-22): the town has none yet. Ducks on the duck pond and the little pond (3–5, seeded; paddle in slow loops, scatter with a flap when a player runs in or throws something, drift back after; residents at the pond feed them with a `feed` pose — crumbs, ducks converge). Pigeons on the square (a small flock that walks around the fountain and takes off when anyone runs through; lands again on the café awning or the statue). A cat on a house roof or windowsill that watches, jumps down when a dog passes, and sits on a bench when it is empty (a player who sits next to it gets a line). Animals are small figures drawn in `stickman.ts` (a `critter(ctx, kind, x, y, s, pose, t)`), deterministic from seed + `wall()` time so everyone sees the same birds, and reactions to players go over the room as an `ev`. Players can `C` near ducks with food to feed them (item consumed).
- [ ] Wearing and using what you carry (owner, 2026-09-22): items are not just cargo. `C` on yourself (no prop or resident near) *uses* the top item: the hat goes on the head (drawn on the figure, stays on, sent in `pos.s` with a `!` marker like `hat!`), the umbrella opens and is carried overhead (`umbrella` pose variant; in rain it keeps you dry — see Weather), glasses go on the face, the phone is held up (`phone` pose), the newspaper is read (`read` pose), the broom sweeps where you stand (`sweep`, and it actually tidies loose items within 60px into a pile), the basket lets you carry two more things, the cup and sandwich are eaten. Residents already wear/use their job item; make them use the same code path. Knocked down → worn things fall off.
- [ ] The café, end to end (owner, 2026-09-22): walk up to the counter with `C` → you join a small queue (residents queue too, in order, and complain about the person in front); the barista resident behind the counter takes the order (`serve` pose, a cup slides across), the price comes off your coins (`/api/goose` wallet; no coins → "come back with money"); you carry the cup; `C` anywhere drinks it in sips (`chew`/`eat` pose, three sips, the cup drawn emptier each time, steam while hot); the empty cup becomes trash you bin (or drop, and a sweeper sighs); the barista's standing with you goes up if you bin it, down if you throw the cup at someone. Residents do the whole loop on their routine (office, courier, painter jobs buy coffee mid-morning; the retired job sits with one for a long time). Same pattern for the bakery (bread → sandwich) so the code is shared.
- [ ] Every prop does something (owner, 2026-09-22): each `PropKind` gets at least one `C` interaction and one resident use — lamp (lean on it; kick it and it flickers), booth (make a call: `phone` pose, a resident's line comes back), stall (buy the stall's thing for coins), bin (rummage: sometimes an item, sometimes a sweeper's complaint), tree (shake it: a leaf shower, sometimes an apple), fountain (toss a coin: a wish line; wash your face after a dunk), church (sit inside; the priest nods), gate (swing on it), post office (mail a loose item to a resident — it arrives at their house), station (turn yourself in; the fine halves), bakery/café (queue and buy), fridge (take food), TV (change channel: a different flicker colour), shelf (take a book → `read`). One prop per run is fine; keep the list here and tick them as they land.
- [ ] Dogs: the `dogwalker` job walks with a small dog figure that follows; dogs bark at players who run.
- [ ] Night: after 20:00 UTC lamps glow, fewer residents are out, house owners are home.
- [ ] Weather: a seeded daily weather (sun / cloud / rain / storm / heat) from `hash(day)` plus an hourly shift, shown as sky, light and — for rain — falling lines and puddles. **Behaviour changes with it (owner, 2026-09-22):** in rain residents open umbrellas or run for cover under the café awning, the bus stop or their own door and wait it out; the ones without umbrellas hold newspapers over their heads; the café closes its awning in a storm and everyone leaves the park; in heat residents sit in the shade of trees and drink, joggers stop jogging, the ice-cream stall (if the town has one) gets a queue; at night (see Night) fewer are out. Players get the same effects: rain slows you slightly unless you carry an open umbrella; puddles splash. Tasks and quests follow the weather ("bring me an umbrella" only when it rains).

## Player growth — unlocks and reputation (owner: no levels; many small unlocks and many small reputation effects)
- [ ] Reputation per job: each knock/steal/dunk against a resident lowers your standing with their *job* (bakers, police…); each returned item or repair raises it. Show it on `/square` as a short list ("the bakers: wary · the joggers: fine"). Residents of a job you are low with refuse to talk (E) and say why; high standing makes them greet you by handle.
- [ ] Unlock: a hat. After 10 completed tasks you can take a hat from the stall with `C` and wear it (drawn on the head, sent in `pos.s` like carried items). Residents comment on the hat.
- [ ] Unlock: the church door. Only opens for players with neutral or better standing with the priest's job; otherwise the priest blocks the door and says so.
- [ ] Unlock: a bench of your own. Deliver 5 things to the same bench and a small plaque with your handle appears on it (persisted through `/api/goose` keys); residents read the plaque.
- [ ] Reputation memory across days: standing decays slowly toward neutral each day; residents mention "yesterday" when you return with a bad record.
- [ ] Detailed eating: the sandwich shrinks over three bites, the cup empties and becomes trash you can bin (or drop, and a sweeper sighs), a resident within 120px comments once.

## The town lives (owner, 2026-09-22) — weather, water, land, temperaments
- [ ] Slipping: anyone (player or resident) who *runs* across the pond's edge or the fountain rim slips — a `slip` pose (feet up, arms out, ~0.6s), drops what they carry, a splash; residents say something dry. Chasing residents slip too, which is how you lose them.
- [ ] Temperaments: when hit, not every resident chases. Seed per resident (with `temper` in the job table): some chase, some **flee** (run away from the player for a few seconds, `run` pose facing away, "not today"), some freeze and complain, some call the police (a cop resident switches to chase). Show it — the same punch should get four different reactions across the square.
- [ ] Waterside cleaning: the sweeper and gardener jobs clean the pond and fountain edges on their routine (`sweep` pose at the water, dunked items get fished out and binned one by one).
- [ ] Rain (see Weather below): when it rains long enough the pond and the low end of the park **flood** (water ellipse grows over 10 minutes, props under it are unusable, figures wade with a `wade` pose, slower). Residents with the gardener/sweeper jobs go and **drain** it (a pump prop appears at the pond, `fix` pose, water shrinks). Players can help with `C` at the pump; helping raises standing with those jobs.
- [ ] Fields: a new outdoor map `fields` (connect: park) with rows of crops as props. The farmer job (new): plants, waters (`water` pose), harvests (crops grow over the day by seeded time), carries baskets to the market stall. Players with empty hands can `C` at a row to help (plant/water/harvest by what the row needs); trampling a row running knocks crops down and the farmer notices.
- [ ] Weather: a seeded daily weather (sun / cloud / rain / storm) from `hash(day)`, shown as sky and, for rain, falling lines; umbrellas go up on residents who carry one; the café closes its awning in a storm. Rain feeds the flooding above.

## Homes (owner, 2026-09-22) — the three houses are identical; the owners should furnish their own
- [ ] Furnishing by owner: each house interior is seeded by its owner's handle and job (`hash(handle)`): wall colour, which of bed/sofa/shelf/plant/TV/table exist and where, a job item on display (the baker's bread rack, the cop's notice board, the painter's easel), a picture on the wall. Three houses must look like three different people live there.
- [ ] Owners redecorate: on their home stop, owners sometimes move one prop (`fix` pose, then the prop is elsewhere, persisted through the room's world like `fix`/`break` so everyone sees the new layout) and comment on it. Over weeks the houses drift.
- [ ] Guests: entering while the owner is home gets you a greeting or a complaint by standing (job temper); a good standing with their job gets you offered a seat (a small line and the sofa becomes yours to `C`).

## Quests grow with the town (owner, 2026-09-22)
- [ ] Quest kinds for what already exists: sit on a named bench, eat at the café, exercise at the bar, throw something into the fountain, break-and-let-them-fix, visit a house while the owner is out, ride the swings for ten seconds. Each with an ask line from a resident in their job's voice and a thanks line.
- [ ] Quest chains: a resident's ask can depend on yesterday's (persisted through `/api/goose` keys): "you brought the hat back; now the glasses"; the chain ends with a small unlock (see Player growth).
- [ ] Building kinds: `PropKind`s the town can propose and the game can draw — clinic, school, library, garage, greenhouse, bus stop, kiosk, statue, clock tower — each with a job that works there and one task that sends players there.
- [ ] House exteriors vary (owner, 2026-09-22): the `house` prop takes a seed (owner handle) that picks roof shape (gable, flat, hip, tower), storeys (1–3), wall and roof colours, door position, window count, a chimney, a fence, a nameplate. Three houses must look like three different houses; new houses the town proposes get the same variety. Keep the door where the exit is.
