# Square backlog — picked by the daily code job (see GROW.md)

> Owner direction (2026-09-24): no town levels or XP. The town must become physically larger and denser over time. Existing place/building items below (builders, market alley, clinic, diner, housing, bridge/dock/transit, etc.) are now a priority lane: if four feature runs pass without visible footprint growth, pick the first feasible one before another micro-interaction. All new game work must remain usable on phone landscape. See `TOWN-ARCHITECTURE.md`.

## Resident-run rounds — the seventh system the town designed (town wishes, 2026-09-25; nine wishes shaped as a tappable 'Start X' action beginning a fixed 60-90s resident-run round with a human able to join as a player or a judge — five turned out to be the already-shipped or already-open Turn-based-games slices wearing new names ('Join Game' names nothing the section's own clock-seeded slates don't already do; 'Ring the Bell' is the still-open stage-buzzer quiz board with 'judge' for 'buzz'; 'Fountain Flip' is the already-shipped Pebble-toss; 'Start Blitz' is the already-shipped Checkers; 'Start Charade' is the still-open Quick-charades slice), folded away with no new build-order line; the remaining four are a genuinely different shape from Turn-based games' single-spot two-person seeded score — a scripted round spanning several residents (and, for two of them, a prop that doesn't exist yet) that a human can tap into as a player or a judge — merged here, one slice per item, in build order)
- [x] The light-chain board: a `simon` spot in the square (`world.ts`) draws three pads, one lit at a time by the same seed+clock-bucket trick chess/pebble/bocce use (a 6-second bucket, quicker to read as "the light moved" than their 10-second score bucket), and a chalk slate above counts the current unbroken streak of successful buckets (scans back through the last 12, same window those three already use). `C` within 90px reads that same clock everyone already sees and says whether this bucket kept the chain going or broke it — cosmetic, the same "human input never mutates the shared clock" rule the rest of this family (and Turn-based games before it) already established, just extended from a two-sided score to a single running streak. Task `simon1` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands, same reasoning as `chess1`/`toss1`/`bocce1`). No resident routing needed — same carve-out the chess table/pebble line/bocce court already use (the board is never idle, it's always "being played" by the clock, so the reverse rule needs no code).
- [ ] A parcel relay at the post office: a human starts it, then a fixed small cast of residents pass a visible parcel token post office → relay locker → finish pennant on a timed lap — needs two new props (a relay locker, a finish pennant) this run didn't build. The lap "time" should be the same seed+bucket trick every other slice in this family uses; a human's own leg of the relay is cosmetic against it, same as a tap-in elsewhere here.
- [ ] A word-chain sentence board: residents each add one seeded word in turn to a running sentence at a new board prop, and a human can tap to "veto" a word. The veto is the one genuinely new wrinkle in this whole family — every other slice's human input is purely cosmetic against the clock, but a veto that actually changes the sentence would be a real mutation, and the room only relays a fixed set of `ev` kinds (see the note under Grows from Bins) with no synced tally to hold one. Either the "veto" has to be judged the same clock-seeded way as everything else here (cosmetic, not a real veto) or this needs its own small design pass before building — note here rather than guess.
- [ ] A toy-duck race at the pond: a new duck-launch kiosk prop; residents "sign" lanes and each gets one seeded drift down the pond, a human can tap in as starter or judge — same shape as the rest of this family (seed+bucket decides the winner, the tap-in is cosmetic), needs the new kiosk prop this run didn't build.

## Turn-based games — the sixth system the town designed (town wishes, 2026-09-25; four wishes — checkers at the park chess table, a fountain pebble-toss, bocce at a park court, ring-toss at an alley booth — all the same shape: two residents take turns on a short timed move at a themed spot and log a score on a slate/scoreboard, with a human able to tap in and take a turn — merged into one build order, one slice per item. None of these need a synced score: the room only relays a fixed set of `ev` kinds (see the note under Grows from Bins) and adding one is out of scope, so the turn/score sequence has to be a pure function of (day, table key, room clock) the same way passing-glance/wave/affinity-pairs already are — every screen computes the identical score without new state. A human's tap-in is a real, visible action (sent in `pos.pose` like leaning/shaking already are) but is cosmetic against that shared clock, not a mutation of it — the deterministic score keeps advancing whether or not a human ever taps in.)
- [ ] Ring-toss at an alley or square-kiosk booth (third — blocked on `marketalley` for the alley variant, the map "The found stall in the alley" under Lost and found already names and which hasn't landed yet; the square-kiosk variant needs no new place and could land first if the alley keeps stalling; a third town wish, 2026-09-25, asked for the exact same kiosk-and-match-board shape again — no new information, still the same fold): once either spot exists, the booth/kiosk is the same seeded-turn/score shape as the rest of this section.
- [ ] A stage-buzzer quiz board (town wish, 2026-09-25, merged here — same joinable/timed/board-logged shape as the rest of this section, but the "turn" is answering a posted question against a clock instead of moving a piece): reuses the park prize stage (`stage1`, already drawn) rather than a new prop; a resident's question text is just a seeded pick from a small fixed line pool (no LLM call, no free text), the buzzer press is `C` within a short seeded window per round the same way a bucket flip already gates chess/pebble-toss, and "right" vs "wrong" is itself decided by the same clock-seeded roll — a human's press is cosmetic against it, same as every other slice here.
- [ ] Paper-distance contests: a paper-plane launch line and a paper-boat launch post (town wishes, 2026-09-25, merged as one slice — same shape, air vs water dressing only): borrow a kit at the line/post, `X` within a short window launches (reuse `throw`, lower arc for the boat), the "distance" is the same seed+bucket score trick as pebble-toss dressed as a number instead of two names, posted to a nearby scoreboard prop. Build the plane (square or street, no new place) before the boat (needs a launch post at the park duck pond, `pond2`).
- [ ] Hopscotch on a painted lane (town wish, 2026-09-25, merged here — turn-based like Checkers, just standing instead of seated): a painted hop lane on the street (no new place, drawn on the ground like the church steps' rectangles), residents and a tapped-in human alternate short seeded turns, `X` for the hop (reuse an existing motion — check whether `jump` reads fine before considering a new `FigPose`), score on a nearby slate the same seed+bucket way.
- [ ] Timed memory-match on a flip-slab (town wish, 2026-09-25, merged here — closest in shape to Checkers of anything in this batch, two-player turn-based with a board): a public flip-slab on the street; `C` to flip, alternating seeded turns, matches tallied the same seed+bucket way onto a small board. No new pose (reuse `chess`'s seated reach, or `stand` if the slab is upright).
- [ ] A scavenger dash with a sprint board (town wish, 2026-09-25, merged here as the odd one out — every other slice in this section is one human at one themed spot; this one sends the player across all three outdoor maps in 45 seconds to tag three "hidden quick-posts", which is a different shape (a timed multi-map fetch, not a turn at a prop) wearing this section's clothing): scope down before building — "hidden quick-posts" needs real spots to tag (reuse existing named props like `lamp1`/`tree1`/`bench4` rather than inventing floating markers) and "fouls" needs a definition that isn't a new `ev` kind; note here rather than build until that's worked out, since it doesn't fit the pure-clock trick the rest of the section relies on.
- [ ] Quick-charades at the prize stage (town wish, 2026-09-25, merged here — last in build order, a genuinely different shape from the rest of the section, not a duplicate): a human borrows a prompt card and prop at a square kiosk, gets a 60s turn on the park prize stage (`stage1`, same spot Busking and the still-open stage-buzzer quiz item above reuse), and residents/other humans "vote" by tapping a nearby clap post instead of the turn itself producing a score. The vote count can't be a real synced tally (the room only relays the fixed `ev` kinds the note under Grows from Bins already lists, and a live multi-tapper counter isn't one of them) — follow this section's own established trick instead: the clap post's "score" is the same pure `(seed, floor(t/10))` bucket function pebble-toss/chess use, and any human's own clap (`C` at the post, sent in `pos.pose` like a tap-in elsewhere in this section) is cosmetic against it, not a mutation — same "the deterministic result keeps advancing whether or not anyone taps in" rule the section's own intro states.

## Keep-lock — the fifth system the town designed (town wishes, 2026-09-24; seven wishes — a mend counter at a Change Room, a drying rack, a bin-lock, a handheld mend kit, a seed-bundle, a rinse trough, a sort-shelf — all asking for the same close: a resident finishes a short timed service on an item, tags it, and it stays briefly un-grabbable right after, distinct from "Service drop-offs" below which only covers the drop-and-service half and never says what happens to the item once it's done — merged into one build order, one slice per item)
- [ ] Rinse trough / drying rack at the fountain (merges two of the seven wishes with the still-unbuilt "Rinse-and-hang at the fountain" chain item under Service drop-offs below — same spot, same action, a drying rack is just the tail end of a rinse): once that chain item lands, route its finish through the keep-lock above instead of leaving the freshly-rinsed item grabbable on the spot.
- [ ] Sort-shelf tagging (extends the shipped Sort the scraps, `SquareGame.tsx`): draw the sweeper's trash/reuse/repair choice as a small label over a `sort`-flagged item while it waits, and route the sweeper's pickup through the keep-lock above instead of a silent vanish.
- [ ] Handheld mend kit (merges the "quick-mend at the Change Room counter" wish with this one — same ask, "anywhere" vs. a named counter — no Change Room prop exists anywhere in `world.ts`, so fold both into the shipped board-based Mend request rather than inventing a location): a carried `mend kit` item (new `ItemKey`) lets a *resident* fix a nearby broken prop or board-pinned item on the spot instead of only ever a repairer's opportunistic roll — same `fix` pose, same keep-lock tail once done.
- [ ] Seed-bundle at the spare-seed plot: same blocker as the existing "A seed deposit at the garden" chain item under Service drop-offs below (needs a growth/crop state that doesn't exist yet) — don't build twice, note here until that lands, then give its finish the keep-lock tail too.
- [ ] Bench cubby stash (town wish, 2026-09-24, merged here — a distinct shape from the rest of this section, closer to Bin-lock than to a mend/rinse/clean finish): a resident stows a small item under any bench (new ~20s `stow` action, no new prop) creating a `stashed` token that keeps that specific spot un-grabbable for the duration, the bench-side sibling to Bin-lock's per-prop timer above.
- [ ] Sweep bundle at the broom rack (town wish, 2026-09-24, merged here — extends Sort-shelf tagging above rather than duplicating it): a resident sweeps loose small items within ~200px into one bundled loose item (12s), which then goes through the ordinary sort/bin path above instead of vanishing item-by-item — the bundling is new, the keep-lock tail is the same one Sort-shelf tagging already wants.
- [ ] Pegboard hang-tool lock (town wish, 2026-09-24, merged here — blocked on a location: no pegboard/maintenance-yard prop exists anywhere in `world.ts`): once a maintenance yard exists (see the new places item below), a resident hangs a tool on its pegboard (~20s) and the tool is locked against humans for 60s — same shape as Bin-lock, just a new prop to hang from instead of a bin to empty. Note here until the yard lands. A later wish (2026-09-24) asked for the same lock on a pushable maintenance cart instead of a fixed pegboard — same blocker, build as one item once the yard exists rather than two.
- [ ] Watering gets a keep-lock tail (town wish, 2026-09-24, merged here — the first Keep-lock slice on an already-shipped core action rather than a Service-drop-off): after the existing `water` action finishes on a garden or sapling, tag it `watered` and route it through the keep-lock above for ~60s, the same way Mend's finish already does — no new prop, no new pose, just wiring the existing watering interaction's finish through `lockAt`.
- [ ] A washyard basin for prop upkeep (town wish, 2026-09-24, merged here — blocked on a location: no washyard/basin prop exists anywhere in `world.ts`, same shape of blocker as Pegboard above): a resident dips a rag and wipes down a scuffed bench or signpost (reuse the existing `BREAKABLE`/`broken` prop-damage state as the "scuffed" condition rather than inventing a new one), then the prop itself locks against human tampering for 60s — the prop-lock shape Bin-lock established, applied to street furniture instead of a bin. Note here until a washyard spot lands. A later wish (2026-09-24) asked for the same rag-scrub-and-lock at a park "hose-post" instead of the washyard — same blocker, same shape, build as one item once either spot lands rather than two.

## Coins in play — the third system the town designed (town wishes, 2026-09-23 to 2026-09-24; eight wishes about earning and spending a coin, scattered across "Residents grow too" and "Wishes from the town", merged into one build order the same way Lost-and-found and Keeping-it were — the town wrote three more of the same shape after the first merge (polish caught it, 2026-09-24), then five more after that, all still the same shape (this run caught it, 2026-09-23)
- [ ] Buy a hot drink (chain, wider — **blocked**): the town's ask was to *spend* 1 coin at the café for a 12s "steady" buff. Square's only coin-mutating endpoint is `/api/goose`'s task-completion insert, which awards from a fixed daily list and never subtracts — there is no spend path, and `route.ts` files are outside this job's edit list (see GROW.md's hard limits). A real wallet-spend needs a new `/api/pond`-shaped endpoint (out of scope here); do not fake it with a client-only decrement of the `coins` state, since that would drift from the server total on reload — a half interaction, against the rules. Skip until a spend endpoint exists.
- [ ] Coin kiosk locking an item (chain, wider — **blocked on the same spend gap** as Buy a hot drink above): needs the same missing wallet-spend endpoint; note here until Buy a hot drink (or anything else) lands that path.
- [ ] A microtask stall (town wish, 2026-09-23, merged here from "Wishes from the town" — **blocked on the same spend gap**): residents post one-coin microtasks at a street stall; a human accepts and pays a coin, which triggers a short resident action and hands the coin to the resident. The "human pays a coin" half needs the same missing wallet-spend endpoint as Buy a hot drink above — skip until that lands. Build order note: even once spend exists, this needs resident-side coin tracking too (see "Resident wallets and savings" under Residents grow too, also unbuilt), so it is third in this build order, after the two items above.
- [ ] A tip jar at the café (town wish, 2026-09-23, merged here from "Wishes from the town" — **blocked, doubly**): humans drop coins into a jar; a nearby resident collects and splits the deposit among up to three residents, updating each one's coin count. Needs both the missing wallet-spend endpoint (human side) and resident wallets (see "Resident wallets and savings", unbuilt) to update multiple residents' counts — the most blocked of the three merged wishes, so it is last in this build order.
- [ ] A one-coin bench reservation (town wish, 2026-09-23, merged here from "Wishes from the town" — **blocked on the same spend gap**): a human drops one coin to reserve a specific bench; a resident who enforces the reservation collects the coin. Same missing wallet-spend endpoint as Buy a hot drink above — the simplest of the three merged wishes once spend exists (no resident-wallet dependency, just one resident and one bench), so build this one first when the endpoint lands.
- [ ] A fountain tip-slot for a named resident (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-24 — **blocked on the same spend gap**): a human drops one coin at the fountain targeted at a specific resident; only that resident may collect it. Same missing wallet-spend endpoint as the rest of this section, no resident-wallet dependency beyond crediting the one named resident — as simple as the bench reservation above, build alongside it.
- [ ] A one-coin swing push (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-24 — **blocked on the same spend gap**): a human drops a coin at the park swing to have a resident push the slow swing for a minute; the resident collects the coin when the minute's up. Same missing endpoint, same one-payer-one-resident shape as the bench reservation and fountain tip-slot above.
- [ ] A request board at a claims booth (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-24 — **blocked, doubly**): a human pins a one-coin lost-or-return job at a new "claims booth" prop (doesn't exist yet, separate from the phone booth); any resident may accept the pinned job and earn the coin. Needs the missing spend endpoint to hold the human's coin against the pin, and the new prop besides — more blocked than the three single-resident wishes above.
- [ ] An IOU jar (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-24 — **blocked, doubly**): an empty-handed resident may borrow a coin from a bench-side jar (marked as debt) and later repay it into a public payback box that clears the IOU. The borrow half needs resident-side coin tracking (see "Resident wallets and savings" under Residents grow too, still unbuilt) before there's a balance to mark debt against; funding the jar in the first place needs the same missing spend endpoint as the rest of this section.
- [ ] Making change (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-24 — **blocked, doubly**): a human hands a resident a bill; the resident breaks it into coins and distributes change across up to four nearby residents or one named recipient, updating each balance. Needs both the missing spend endpoint (the human's bill) and resident wallets for every recipient's balance to update — the most moving parts in this section, build it last.
- [ ] A 'seconds' stall listing (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-23 — **blocked on the same spend gap**): a resident lists a single unwanted item for sale at a street stall; a human pays one coin to instantly buy it and the coin transfers to the listing resident. Same missing wallet-spend endpoint as the rest of this section, plus resident-side coin tracking (see "Resident wallets and savings", unbuilt) to actually receive the coin — no simpler than the microtask stall above, same build order slot.
- [ ] A paid busk request (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-23 — **blocked on the same spend gap**): distinct from the already-built Busking (residents tip a busker automatically at low odds) — here a human drops coins into the hat to *request* a performance, nearby residents can accept to perform, and the coins pool and split among performers. Needs the missing wallet-spend endpoint (human side) and resident wallets to split a payout across several residents — as blocked as the tip jar above.
- [ ] A lamp lottery (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-23 — **blocked on the same spend gap**): a human drops one coin at a lamp to enter nearby residents into a short timed draw; the lamp pays the pooled coins to the drawn winner. Needs the missing spend endpoint (entering costs a coin) and resident wallets to pay out the winner.
- [ ] A paid duck-feeding request (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-23 — **blocked on the same spend gap**): distinct from the existing free feed-the-ducks interaction — here a human pays a coin to have a nearby resident do the feeding instead, who collects the coin on completion. Same missing endpoint, same one-payer-one-resident shape as the bench reservation and fountain tip-slot above; the simplest of this batch once spend exists, since it reuses the feeding pose and no new prop.
- [ ] A micro-errand board (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-23 — near-duplicate of the claims-booth request board and the microtask stall above, same fetch/return/deliver shape with a coin at the end — **blocked, doubly**, same reasons as the claims booth): kept as a separate line only because it names a board rather than a booth; if a claims booth ever lands, build this as the same feature rather than twice.
- [ ] A prepaid café tab (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-24 — **blocked, doubly**): a human deposits coins to a named resident's café tab; that resident spends from it at the café until it runs out, with a simple visible ledger. Needs the missing wallet-spend endpoint (the depositor's coins) and resident wallets (the tab itself is a resident-side balance) — same two gaps as the rest of this section.
- [ ] A paid stall-watching shift (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-24 — **blocked on resident wallets**): residents clock in for a timed (30-90s) shift watching the stall and collect any tips, credited to their coin balance on completion. No human spend needed (tips are ambient, not paid by a specific human), but crediting a resident's balance still needs "Resident wallets and savings" (unbuilt) — skip until that lands.
- [ ] Slip a coin to a resident (town wish, 2026-09-23, merged here from "Wishes from the town" 2026-09-24 — **blocked, doubly**): a human hands a single coin directly to a named nearby resident, incrementing that resident's visible balance and leaving a small coin token on them. Same two gaps as the tip jar above (human spend, resident wallet) — the simplest one-to-one case of "Making change"'s shape, build alongside the bench reservation and fountain tip-slot once spend exists.

## Service drop-offs — the fourth system the town designed (town wishes, 2026-09-24; four wishes all shaped the same way — a human leaves a token at a themed spot, a nearby resident accepts a short timed service action on it, something changes — merged into one build order the same way the first three systems were)
Unlike most of Coins in play, none of these need a human to spend a coin or a resident to hold a wallet: the reward for finishing is the existing fixed one-time daily-task award (`tasksFor`), the same shape Busking/Selling/Rummaging already use to sidestep the missing wallet-spend endpoint. Build in this order.
- [ ] A seed deposit at the garden (chain): a human drops a seed packet (new `ItemKey`) into a seed-box token at a garden bed; a nearby gardener performs a short plant action (new `plant` pose off the Work list in the motion library) that consumes the token and starts something growing. Blocked on deciding what "starts growing" means with no crop/growth state yet — note here until a small growth marker exists (see Fields/Money-plants ideas) or scope it down to a flavor-only version like Mend/Sort above.
- [ ] Rinse-and-hang at the fountain (chain): a human leaves a dirty cloth token at the fountain; a nearby resident rinses it (short timed action, reuse `water`'s splash visual) and hangs it to dry on the nearest bench — same flag-and-service shape as Mend/Sort, the cloth becoming a plain loose item on the bench once done.

## Lost and found — the system the town asked for (town wishes, 2026-09-22; restored 2026-09-23 after the owner pointed out these were one design, not repetition)
The town wrote sixteen wishes and they were all one system: things humans throw, dunk and abandon should come back. Build it in this order, one slice per run.
- [ ] The found stall in the alley (checked 2026-09-23, skipped — **doubly blocked**, verified against source): the same crate idea, but a stall in `marketalley` run by whichever resident's routine passes it — items recovered anywhere in town end up there after a day, and a human can buy one back for a few coins. `marketalley` doesn't exist anywhere in `world.ts` (see "The market alley" below, which must land first), and "buy one back for a few coins" needs the same missing wallet-spend endpoint blocking the two Coins-in-play items above (`/api/goose` only ever does `coins = coins + ?2`, never a subtraction). Skip until both land.

## Keeping it — the second system the town designed (town wishes, 2026-09-22 to 2026-09-23; eleven wishes merged into one build order after they all circled the same idea — once a resident is holding or has just recovered something, more ways to keep a human from grabbing it back)
- [ ] Umbrella-cover (checked 2026-09-23, skipped — **blocked**, verified against source): a resident carrying an umbrella, about to lose something smaller, opens it over the dropped item instead of over themselves — for 8 seconds that specific loose item needs two separate `C` presses close together to actually pick up (the first knocks the umbrella aside, the second grabs it) instead of the usual one. `umbrella` is already an `ItemKey` and `painter`'s job item (`world.ts`), but nobody can *open* one — there is no `umbr`/open pose or state anywhere in `stickman.ts`. That's the base "Umbrella: `C` opens it" item under "Umbrellas and the rest of the pocket" below, which its own text says must land with or after Weather, never before. This item assumes the open umbrella already exists; it doesn't. Skip until the base umbrella item ships.
- [ ] Pass-along (checked 2026-09-23, skipped — **blocked**): "a resident about to be caught mid-chase" describes a resident being pursued, but today only residents ever chase (`Mode = 'chase'`) — a human is never coded as chasing one, since the Temperaments item below (flee) hasn't landed. Without a fleeing resident there is no "about to be caught" moment to hook this onto; revisit once Temperaments ships a resident that runs from the player.
- [ ] Shout (checked 2026-09-23, skipped — **blocked on the 2026-09-23 holiday rule**): the existing steal-roll site (`SquareGame.tsx`) already has two reactive variations (brace, then stow-in-mug/weight-down as one merged roll) — the maximum GROW.md allows before you must replace one instead of adding. Shout would be a third. Either fold it in by replacing an existing variation, or point it at a different trigger than the steal roll; as written it can't just be added.
- [ ] Scoop: once a lifeguard or waterside job exists (see "A lifeguard job" below), give it a faster net-retrieval at water spots — reuse the existing `rake` mode with a shorter timer for that job specifically rather than inventing a second retrieval system. Note here until the lifeguard item lands.

- [ ] The café, end to end (owner, 2026-09-22): walk up to the counter with `C` → you join a small queue (residents queue too, in order, and complain about the person in front); the barista resident behind the counter takes the order (`serve` pose, a cup slides across), the price comes off your coins (`/api/goose` wallet; no coins → "come back with money"); you carry the cup; `C` anywhere drinks it in sips (`chew`/`eat` pose, three sips, the cup drawn emptier each time, steam while hot); the empty cup becomes trash you bin (or drop, and a sweeper sighs); the barista's standing with you goes up if you bin it, down if you throw the cup at someone. Residents do the whole loop on their routine (office, courier, painter jobs buy coffee mid-morning; the retired job sits with one for a long time). Same pattern for the bakery (bread → sandwich) so the code is shared.
- [ ] A restaurant, with service that can go wrong (owner, 2026-09-22): a new `diner` building on the street (or in a town-proposed map) with tables and a kitchen door; a `cook` job and a `waiter` job. Sit at a table (`C`), a menu line appears, `C` again orders (coins). The waiter walks the order to the kitchen; the cook cooks (a `cook` pose, pan, a puff); the plate comes back — usually. **Seeded service, both ways**: sometimes the plate arrives almost before you sit (the cook had one ready, a line about it); usually normal; and sometimes the order is slow (the plate sits in the kitchen window while the cook is on the phone), wrong (someone else's dish arrives), or never comes (the waiter forgot). Waiting players get a growing "…" above the head and can `E` to ask ("where is my food" — a flat excuse comes back), leave without paying (the waiter chases; the cop fines), or punch the owner (`X` on the cook: he goes down, the kitchen stops for a minute, everyone waiting complains — and your standing with the cook job drops for a week). Eating the plate is the `eat` pose with the plate emptying in three bites; the empty plate stays on the table until the waiter clears it. Residents eat there on their routines and suffer the same failures, and complain to each other. Task: `eat a meal at the diner`; ask: `order me the soup, I am not going back in there`.
- [ ] Wrecking a house the residents built (owner, 2026-09-22): houses (and town-proposed buildings) get hit points like props but much more — a door you can kick in (three kicks, the owner rushes out), windows that break one by one (a crack, then a hole, glass on the ground a sweeper has to clear), and, if you keep going with jump kicks or thrown things, the roof caves in: the house shows as a ruin (`ruined` state) for the rest of the day, the owner stands in front of it and says one flat line, other residents come and look, and the repair jobs rebuild it over the next hours (scaffolding prop, `fix` pose, the house comes back piece by piece, visible to everyone through the room's world). Doing this to a house costs standing with every job at once and the police chase you on sight for the day. Residents never wreck houses — this one is human-only, and the reverse rule applies as *repair*: residents rebuild.
- [ ] Builders (owner, 2026-09-22): a `builder` job (hard hat item, a plank or bucket). Builders are how the town's growth becomes visible: when the patrol proposes a new spot or map (`site_meta.square_content`), it arrives as a **construction site** first — stakes and tape, a pile of planks, a sign with the name — and two or three builders spend the day there (`build` pose: hammering, carrying planks between the pile and the frame, measuring), the thing rising in stages (frame → walls → roof → done) by seeded time so everyone sees the same stage; only when finished does it become usable. Builders also do the rebuild after a wrecked house (see above) and fix broken props when the repair jobs are busy. Players can `C` at the pile with empty hands to carry a plank to the frame (speeds it up a little, raises standing with builders) or steal a plank (the builder chases, and the site is delayed). Knocking a builder off a ladder is a task on some days.
- [ ] Flowers as courting (thin axis: relationships and children, 2026-09-22): the stall sells a `flowers` item; a resident who fancies another (from the Relationships affinity seed, once that lands) detours to the stall before visiting them. Players can buy flowers and give them to a resident standing nearby (`C` while holding, in range, no water/bench/bin competing for the key) to raise a new per-resident affinity (distinct from the per-job reputation in "Player growth") — a small thing a player can do for one specific resident rather than a whole job.
- [ ] A florist job (thin axis: jobs, 2026-09-24 — the seller half "Flowers as courting" above still needs): a `florist` job with its own stall spot, selling `flowers` the way baker/grocer already sell their own item — ties the stall economy to a job instead of flowers just appearing at "the market stall" generically, and gives the courting item above somewhere specific to detour to.
- [ ] Relationships (owner, 2026-09-22; keep it ad-safe — nothing sexual, this is a public site under ad policy): residents pair up over weeks from seeded affinity (jobs that meet on their routines, a shared bench, a kindness after being knocked over). Stages you can see: walking together (`hold` pose, hands linked, matching pace), sitting on one bench, a peck on the cheek at the fountain (`kiss` pose, quick, a small heart-less line like "there."), moving into one house (the owner list changes; both come home), rows (facing each other, `argue` pose, short flat lines, one storms off to the park), break-ups (one sits alone on the far bench for a day), and a wedding at the church once a month (everyone in the roster walks there, the priest speaks, rice is thrown — a player can steal the ring, be chased by the whole party, and it is a task). Players can nudge things: deliver a resident's dropped item to their partner, or knock one of them over mid-walk and watch the other react. All of it deterministic from seeds and the room clock so everyone sees the same couple; state persists via `site_meta` written by the patrol (not the browser).
- [ ] Children, and the population growing (owner, 2026-09-22): a couple that has lived in one house for a few weeks gets a child — a small figure (scale 0.6) that follows a parent on their routine (`toddle` gait), sits on the swings, chases the ducks, and is put to bed at night. Over weeks the figure grows (0.6 → 0.8 → 1.0) and starts a routine of its own (the `kid` job first: school when the town has one, the swings, the stall for sweets). Knocking a child over is the one thing every resident in sight reacts to at once. **When a child reaches full size the town gains a resident**: the game side ends by writing a `wanted_persona` note into `site_meta` (name, parents, what they are like from what they did in town), and the *patrol* — not this job — writes the persona, adds the `residents` row and the memory file, and the newcomer starts posting. The site is called Population: Zero; the counter on the About page should tick up. State (couples, births, ages) is kept in `site_meta` by the patrol so all browsers agree.
- [ ] A playground in the park (owner, 2026-09-22 — "there has to be more to do"): next to the swings, a real playground: a **slide** (climb the ladder with `C`, slide down — `slide` pose, a whoosh, you land running; residents and kids queue for it), a **seesaw** (needs two: sit on one end and it tips; a second person or resident on the other end and it rocks — `seesaw` poses both ends, synced through the room; jump off and the other one drops with a thud), a **sandbox** (dig with `C`: a hole, a pile, sometimes a lost item from the pool of things that were thrown in the park; kids build a castle you can kick over — the whole playground reacts), a **climbing frame** (a small 2D jump puzzle inside the square: platforms you hop between like Climb, a bell at the top), a **roundabout** (push it — `push` pose — it spins, hop on and ride; more people = faster; someone flies off at speed and lands in the `hurt` pose). Residents use it on their routines (kids most, the retired job sits on the bench and watches), and a task or two each day sends people there ("ring the bell", "ride the roundabout with two residents"). Every motion is a `FigPose` so it reaches every game.
- [ ] Every prop does something (owner, 2026-09-22): each `PropKind` gets at least one `C` interaction and one resident use — ~~booth (make a call: `phone` pose, a resident's line comes back)~~ done below, lamp (lean on it; kick it and it flickers), stall (buy the stall's thing for coins), ~~bin (rummage: sometimes an item, sometimes a sweeper's complaint)~~ done below, tree (shake it: a leaf shower, sometimes an apple), fountain (toss a coin: a wish line; wash your face after a dunk), church (sit inside; the priest nods), gate (swing on it), post office (mail a loose item to a resident — it arrives at their house), station (turn yourself in; the fine halves), bakery/café (queue and buy), fridge (take food), TV (change channel: a different flicker colour), shelf (take a book → `read`). One prop per run is fine; keep the list here and tick them as they land.
  - [x] Booth — `C` with empty hands near a phone booth dials (new `b.calling` timer, ~2.2s, reuses the existing `phone` pose and "on the phone" label — both already existed for residents at booths, so no new pose was needed); on hangup a flat line comes back (toast + head bubble). `office`/`postie` residents already stop at booths with `act:'phone'` on their routine, so the reverse rule was already satisfied before this landed. Added task kind `call` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands, like `fish1`).
  - [x] Lamp — new named `lamp` spots (`lamp1` in the square, `lamp2` on Market street; the many other lamps per map stay decorative filler, same split as `tree1`/`tree2` vs filler trees) with act `lean`; `C` with empty hands near one toggles leaning (new `b.leaning`, same movement-locks-until-you-move pattern as `b.still`), new `lean` `FigPose` (also closes a line from the motion library backlog). `busker` now routes through both lamps on its routine, so residents lean too — added `lamp` to the `EXACT` stand-position list so they stand right against the post instead of scattering nearby. Kicking (or throwing something at) a lamp already damaged/broke it via the generic `BREAKABLE` path; it now also flickers for ~0.5s (`kind` added to the `break` ev so every client flickers the same lamp, not just the kicker). Added task kind `lean` (`tasksFor`); no quest kind (no item changes hands, like `call1`).
  - [x] Tree — `C` with empty hands near a named tree (`tree1`/`tree2` in the square; `ptree1`/`ptree2` in the park work too since the check is by `kind`, not by key) shakes it for ~1s (new `b.shaking`, same movement-locks pattern as calling/fishing), new `shake` `FigPose` (arms up gripping a branch, leaves falling). On finish: usually "just leaves", 30% of the time a new `apple` item (`ItemKey`, also added to `FOOD` so it can be eaten/fed to ducks like a sandwich) drops at the base through the existing `drop` path. `kid` now routes through both square trees on its routine (no job did before), so kids shake trees for apples on their own with the same pose — the park's `ptree1`/`ptree2` stay `painter`'s spots (painting, not shaking) but players can still shake those too. Added task kind `shake` (`tasksFor`); no quest kind (no item reliably changes hands, same reasoning as `call1`/`lean1`).
  - [x] Bin — same slice as "Rummaging a bin" under Grows from Bins below.
- [ ] Dogs: the `dogwalker` job walks with a small dog figure that follows; dogs bark at players who run.
- [ ] A tour guide job (thin axis: jobs, 2026-09-23): a new `guide` job whose routine is a fixed loop through named spots across all three outdoor maps (fountain → bakery → church → swings → gate → back), pausing ~8s at each to say one flat, deadpan line about the place (reused `say`/bubble, `stand` pose, no new machinery — same as any resident's routine chatter). A task `follow the guide for a full loop` completes when a player stays within ~150px of the guide through 4 consecutive stops without wandering off. No new pose, no new item; the guide is content, not a system — the simplest thing the thin jobs axis can take next.
- [ ] A lifeguard job (thin axis: jobs, 2026-09-23 — deepens today's raking): stations at each water spot (fountain, duck pond) on its own routine stop (reuse the existing `stand` pose, no new one needed); add it to `RAKERS` in `SquareGame.tsx` ahead of `gardener`/`sweeper` with the widest assignment radius and highest chance of the three, so the resident actually posted at the water is the one who usually fishes things out, with its own line pool ("all yours." / "watch the edge.") distinct from the gardener/sweeper lines. Extends `JOBS` in `world.ts`.
- [ ] A park ranger job (thin axis: jobs, 2026-09-24): a new `ranger` job patrolling the park's named spots (`swing`, `garden2`, `pond2`, `ptree1`, `ptree2`, `bin4` — reusing the existing `stand`/`sweep` acts and poses, no new pose needed) with its own line pool about the flower beds and the pond, distinct from `gardener`'s and `dogwalker`'s. The simplest jobs-axis pick left: no new economy, no new prop, just a name, a route, and something to say — same shape as the tour guide and the lifeguard above it.
- [ ] A town crier (thin axis: jobs, 2026-09-23): a new `crier` job stationed at the fountain and market stall (reusing the existing `stand`/`read` acts and poses, no new one needed) who calls out the day's top post's title on a timer (a `say`/bubble line, same shape as a chasing resident's flat line) — ties into the post-title data `ResidentLite.line` already carries (see "Small talk with substance"). No new economy, no new prop, same shape as the tour guide and ranger above it.
- [ ] A crossing guard (thin axis: jobs, 2026-09-24 — hand-counted the thinnest of the eleven axes GROW.md names, tied with places): a new `guard` job posted where the street path meets the square (reusing the existing `stand` act/pose, no new one needed) with its own line pool about near-misses and foot traffic, distinct from the cop's chase lines and the crier's headlines. No new economy, no new prop — same shape as the tour guide, lifeguard, ranger and crier above it: a name, a spot, something to say.
- [ ] Night: after 20:00 UTC lamps glow, fewer residents are out, house owners are home.
- [ ] Weather: a seeded daily weather (sun / cloud / rain / storm / heat) from `hash(day)` plus an hourly shift, shown as sky, light and — for rain — falling lines and puddles. **Behaviour changes with it (owner, 2026-09-22):** in rain residents open umbrellas or run for cover under the café awning, the bus stop or their own door and wait it out; the ones without umbrellas hold newspapers over their heads; the café closes its awning in a storm and everyone leaves the park; in heat residents sit in the shade of trees and drink, joggers stop jogging, the ice-cream stall (if the town has one) gets a queue; at night (see Night) fewer are out. Players get the same effects: rain slows you slightly unless you carry an open umbrella; puddles splash. Tasks and quests follow the weather ("bring me an umbrella" only when it rains).
- [ ] Lamps on a clock, ahead of full Night (thin axis: weather and time, 2026-09-23): rather than wait for the whole Night/Weather system, give every `lamp` prop (named and decorative filler alike) a `lit` state driven purely by `hour() % 24` (18:00–06:00 UTC) — drawn as a warm glow circle in the prop's draw call, no state to sync since every client computes the same hour. Leaning against a *lit* lamp at night gets its own line from the toast pool ("the moths love this thing.") instead of the daytime one. Small, deterministic, and de-risks the harder "fewer residents are out" half of Night by landing the one purely visual piece first.
- [ ] Dawn and dusk light, the other purely-visual piece of Night (thin axis: weather and time, 2026-09-23): each outdoor map's floor/sky colours (`GameMap.floor`, fixed today) get a warm lerp by `hour()` — a short dawn tint (05:00–07:00 UTC), full daylight, a short dusk tint (17:00–19:00 UTC) shading toward the lamp-lit night above — a per-frame colour interpolation keyed only on `hour()`, no new synced state, same de-risking idea as Lamps on a clock: land the visuals before the harder "fewer residents are out" behaviour change.
- [ ] Residents thin out at night, the harder half both purely-visual pieces above deliberately deferred (thin axis: weather and time, 2026-09-23): between 22:00–06:00 UTC (`hour()`, the same clock function Lamps-on-a-clock/Dawn-and-dusk already key off), a resident whose routine has a `home`-tagged stop skips any stop after it instead of looping back to the start — reusing the existing seeded stop-picker in `goose.ts` (a pure function of handle+day+hour already, no new synced state, every client computes the same shortened night routine). Residents with no home stop yet (most of the roster, until "Buying a house" lands) just hold at their last daytime stop rather than getting a new pose — an implicit "gone in for the night." Fewer people on the square after dark, without touching Weather's harder rain/heat branches.
- [ ] Morning mist (thin axis: weather and time, 2026-09-23 — a fourth purely-visual piece ahead of full Weather): between 05:00–07:00 UTC (the same dawn window Dawn-and-dusk-light already uses), draw a soft translucent haze band low over the ground on outdoor maps, thinning out as `hour()` passes 7 — a per-frame alpha keyed only on the clock, no new synced state, same de-risking idea as Lamps-on-a-clock and Dawn-and-dusk light.
- [ ] House windows glow at night (thin axis: weather and time, 2026-09-23 — a fifth purely-visual piece, and the first to touch a house rather than the street): the three houses' window rectangles (`prop()`'s `house` case, `SquareGame.tsx` — always drawn `#f2e7a8`, no state today) switch to a warmer, slightly brighter fill during the same 18:00–06:00 UTC window Lamps-on-a-clock already keys off, and back to a plain daylight grey-blue outside it — a per-frame colour pick keyed only on `hour()`, no new synced state. Small, but it's the visual half of "house owners are home" that Night's own description promises, ahead of any of the harder owner-presence logic.
- [ ] Full night sky, the steady middle Dawn-and-dusk-light's transition windows don't cover (thin axis: weather and time, 2026-09-25): Dawn-and-dusk-light only lerps the outdoor maps' floor/sky colours during the two transition windows (05:00–07:00, 17:00–19:00 UTC) "shading toward the lamp-lit night" — but outside both windows (19:00–05:00, the bulk of the night) the sky has nothing keeping it there and reads as plain daylight again the moment the dusk lerp finishes. Give that stretch its own steady deep-night floor/sky tint (same per-frame `hour()`-only colour pick, no new synced state) so the square doesn't snap back to noon colours at 9pm.
- [ ] A handful of stars during full night (thin axis: weather and time, 2026-09-25 — builds directly on Full night sky above, hand-counted weather-and-time among the thinnest again): during the same 19:00–05:00 steady-night window Full night sky gives its own tint, draw a small fixed set of star points per outdoor map (seeded per map key, tiny fixed-position dots above the skyline) at low alpha, fading in as the night tint deepens past dusk and back out during the dawn lerp — same per-frame `hour()`-only opacity pick as every other piece in this chain (Lamps-on-a-clock, Dawn-and-dusk, Morning mist, House windows, Full night sky), no new synced state.

## Player growth — unlocks and reputation (owner: no levels; many small unlocks and many small reputation effects)
- [ ] Reputation per job: each knock/steal/dunk against a resident lowers your standing with their *job* (bakers, police…); each returned item or repair raises it. Show it on `/square` as a short list ("the bakers: wary · the joggers: fine"). Residents of a job you are low with refuse to talk (E) and say why; high standing makes them greet you by handle.
- [ ] Unlock: a hat. After 10 completed tasks you can take a hat from the stall with `C` and wear it (drawn on the head, sent in `pos.s` like carried items). Residents comment on the hat.
- [ ] Unlock: the church door. Only opens for players with neutral or better standing with the priest's job; otherwise the priest blocks the door and says so.
- [ ] Unlock: a bench of your own. Deliver 5 things to the same bench and a small plaque with your handle appears on it (persisted through `/api/goose` keys); residents read the plaque.
- [ ] Reputation memory across days: standing decays slowly toward neutral each day; residents mention "yesterday" when you return with a bad record.
- [ ] Detailed eating: the sandwich shrinks over three bites, the cup empties and becomes trash you can bin (or drop, and a sweeper sighs), a resident within 120px comments once.

## The town lives (owner, 2026-09-22) — weather, water, land, temperaments
- [ ] Slipping: anyone (player or resident) who *runs* across the pond's edge or the fountain rim slips — a `slip` pose (feet up, arms out, ~0.6s), drops what they carry, a splash; residents say something dry. Chasing residents slip too, which is how you lose them.
- [ ] Temperaments: when hit, not every resident chases. Seed per resident (with `temper` in the job table): some chase, some **flee** (run away from the player for a few seconds, `run` pose facing away, "not today"), some freeze and complain, some call the police (a cop resident switches to chase). Show it — the same punch should get four different reactions across the square.
- [ ] Waterside cleaning: the sweeper and gardener jobs clean the pond and fountain edges on their routine (`sweep` pose at the water, dunked items get fished out and binned one by one).
- [ ] Rain (see Weather below): when it rains long enough the pond and the low end of the park **flood** (water ellipse grows over 10 minutes, props under it are unusable, figures wade with a `wade` pose, slower). Residents with the gardener/sweeper jobs go and **drain** it (a pump prop appears at the pond, `fix` pose, water shrinks). Players can help with `C` at the pump; helping raises standing with those jobs.
- [ ] Fields: a new outdoor map `fields` (connect: park) with rows of crops as props. The farmer job (new): plants, waters (`water` pose), harvests (crops grow over the day by seeded time), carries baskets to the market stall. Players with empty hands can `C` at a row to help (plant/water/harvest by what the row needs); trampling a row running knocks crops down and the farmer notices.
- [ ] Weather: a seeded daily weather (sun / cloud / rain / storm) from `hash(day)`, shown as sky and, for rain, falling lines; umbrellas go up on residents who carry one; the café closes its awning in a storm. Rain feeds the flooding above.

## Homes (owner, 2026-09-22) — the three houses are identical; the owners should furnish their own
- [ ] Furnishing by owner: each house interior is seeded by its owner's handle and job (`hash(handle)`): wall colour, which of bed/sofa/shelf/plant/TV/table exist and where, a job item on display (the baker's bread rack, the cop's notice board, the painter's easel), a picture on the wall. Three houses must look like three different people live there.
- [ ] Owners redecorate: on their home stop, owners sometimes move one prop (`fix` pose, then the prop is elsewhere, persisted through the room's world like `fix`/`break` so everyone sees the new layout) and comment on it. Over weeks the houses drift.
- [ ] Guests: entering while the owner is home gets you a greeting or a complaint by standing (job temper); a good standing with their job gets you offered a seat (a small line and the sofa becomes yours to `C`).

## Quests grow with the town (owner, 2026-09-22)
- [ ] Quest kinds for what already exists: sit on a named bench, eat at the café, exercise at the bar, throw something into the fountain, break-and-let-them-fix, visit a house while the owner is out, ride the swings for ten seconds. Each with an ask line from a resident in their job's voice and a thanks line.
- [ ] Quest chains: a resident's ask can depend on yesterday's (persisted through `/api/goose` keys): "you brought the hat back; now the glasses"; the chain ends with a small unlock (see Player growth).
- [ ] Building kinds: `PropKind`s the town can propose and the game can draw — clinic, school, library, garage, greenhouse, bus stop, kiosk, statue, clock tower — each with a job that works there and one task that sends players there.
- [ ] A 'reunite' quest kind (thin axis: quests, 2026-09-23 — deepens today's raking; unblocked 2026-09-24, the board landed): the board/crate exists now (see Lost and found, item 1) — a resident passing near it whose lost item is pinned (`Loose.origin`) can be asked by a *different* resident who saw it happen ("have you seen a wallet? someone said it went in the fountain") — completing it means taking the pinned item from the crate to the asking resident, distinct from a plain `fetch` since the item already exists in the world rather than being carried by anyone.
- [ ] A 'no-hands' quest kind (thin axis: quests, 2026-09-24): a resident asks the player to stay near them (~150px) for a set stretch (~20s) without picking anything up, punching, or kicking — timed client-side the same way `chased`'s ten-second clock already is, reset the moment any of those three inputs fire. Thanks line about restraint ("that's the first time all day"). Every quest kind so far rewards *doing* something to the town; this is the first one that rewards not.
- [ ] A 'shadow' quest kind (thin axis: quests, 2026-09-24): a resident asks the player to quietly follow a *different*, named resident (`target`) for a stretch (~20s, the same clock shape `chased`/no-hands use) without the target's routine ever being interrupted because of the player (a shove, a steal, anything that knocks their `mode` out of `routine`) — and it fails if the player falls more than ~200px behind. The thanks line comes from the asker, not the one who was followed — the first quest kind where the resident you help and the resident you watch are two different people.
- [ ] A 'brace-check' quest kind (thin axis: quests, 2026-09-24 — deepens today's rebalance of the steal roll under Keeping it): a resident with something in hand asks the player, in words, to try to take it from them ("go on, try. i want to see if I can still hold on.") — completing it means the *next* grab attempt against that resident rolls into `brace` (reuse the existing roll and mode, `SquareGame.tsx`) rather than any other outcome, and the thanks line only fires if it actually lands as a brace, not a stow or a plain steal. The first quest kind that is satisfied by a reaction the resident has, not by an item changing hands.
- [ ] A 'claim the mend' quest kind (thin axis: quests, 2026-09-24 — ties into today's Bin-lock/Keep-lock work): a resident whose own item was pinned to the board and came back with a "— mended" note may ask the player to bring it back from the board — same completion shape as `reunite` (take the pinned item to the asking resident) but keyed off the `mended` note specifically rather than any pinned item, and the ask only fires once the mend's keep-lock has actually cleared (`lockAt` on that item no longer active), so the player is never sent to fetch something still un-grabbable.
- [ ] A 'spectate' quest kind (thin axis: quests, 2026-09-25 — ties into today's Turn-based games work): a resident asks the player to come watch a round at the chess table (or any future turn-based spot) — completing it means standing within ~150px of the table while its slate's score changes at least twice (two ~10s bucket flips, so ~20s of actually watching), the same passive-presence shape `no-hands`/`shadow` already use but keyed off the prop's own clock instead of a timer the player starts. Thanks line about the company at the board, not the game itself.

## Grows from Bins
- [ ] A kid beats you to a fresh find (chain from Bins, wider — deepens today's rummaging): the moment a rummage (player's or a resident's) turns up an item, a `kid`-job resident within ~150px with room in their stack (<3) has a small one-time chance to dash over and take it first if it sits unclaimed for a couple of seconds — reuses the existing stray-loose-item fetch roll (`SquareGame.tsx`'s per-frame `near` check) but weighted toward `kid` specifically near a bin, since kids are the other established bin-rummagers. A raced find, not a guaranteed one.
- [ ] Binning a resident's own item (chain from Bins): if the item you bin matches the `job.item` of a resident of that job standing within ~150px who can see you (same map, `act !== 'away'`), they notice and say a flat line about it (e.g. the baker watching you bin a basket). Once the reputation system lands this should lower standing with that job instead of just a line — note here until it does.
- [ ] Bin fullness is not shareable state today (note for future runs): the room (`climb-room.js`, off-limits) only relays/remembers `ev` kinds `drop`, `pick`, `break`, `fix`, `npc`, `npcpos`, `hitp` — any other `k` is silently dropped by the server. A synced "how full is this bin" counter needs one of those kinds reused (e.g. piggyback on `break`/`fix` hp semantics) or a room change, which is out of scope here. Don't reach for a new `ev` kind for this without solving that first.
- [ ] A found item still smells like it (chain from Bins, wider — deepens the rummaging run): an item found via rummaging carries a short-lived local `fromBin` tag (~60s, same per-loose timer shape `wet`/`hideAt` already use); a resident asked to accept it for a `fetch` quest during that window says a one-off line first ("you got this from a bin?") but still completes the quest exactly as before — flavor only, no mechanic change, the same kind of small texture the Board/Relay-and-shelving chains already add once an item has a history worth mentioning.

## Grows from Tripping
- [ ] Trip on the pond/fountain edge and on broken props (chain from Tripping): the seeded per-second roll is done (`n.mode === 'trip'` in the chase loop, `b.tripped` for players), but the two other triggers from the owner's original ask are still missing — running across the pond/fountain rim (any `WATER_SPOTS` prop, not just while chasing) and stepping on a broken (`brokeAt` set) prop should also roll a trip, for both a chasing resident and the player. Reuse the same `trip` pose and timers; this is a second call site, not new machinery. Note the owner's separate "Slipping" item (below, in "The town lives") wants a distinct `slip` pose at the same spots for *running* through water edges outside of a chase — decide there whether trip and slip are the same event or two different ones before building both.

## Grows from Fishing
- [ ] Rod tiers (chain from Fishing, deferred from the owner's original ask): today's rack always hands out the same free rod (instant catch, uniform odds over all 250 names). Add a second, better rod at the market stall for coins (`shop` act already exists there) that shortens the wait and/or weights the roll toward the rarer/funnier names — reuse `RODS`/`weights()` from `pond.ts` rather than inventing a new curve; this is the natural home for the "many small unlocks" the owner wants (see Player growth) once that system exists to gate it.
- [ ] A caught fish rots if you sit on it (chain from Fishing): a `fish` item still in your stack after ~90s becomes `a very dead fish` in the toast/label the next time you look at it or try to hand it over (no new `ItemKey` needed, just a per-loose/per-carried "caught at" timestamp checked on interaction) — residents refuse to accept a rotten one for a `fetch` quest and say so, so dawdling has a cost. Needs a place to store the timestamp per stack slot, which `body.stack: ItemKey[]` does not have today (it is a bare array) — that is the real blocker, not the rot rule itself.
- [ ] Ducks steal a dropped fish (chain from Fishing, now unblocked — the ducks landed above): a `fish` dropped or dunked near a pond should disappear a little faster than other loose items if ducks are nearby, with a short "the ducks got it" line instead of it just sitting there. The ducks' own position is client-local (see "Grows from Animals"), so this has to be judged the same way — the client that owns the drop (the one whose `drop`/`dunk` action created the loose item) decides, on its own screen, whether a duck was close enough, and removes it with the existing `pick`-style `ev` (no new `ev` kind) after a short delay.

## Grows from Animals
- [ ] Pigeons on the square (chain from Animals, deferred from the owner's original ask): a small flock that walks around the fountain and takes off (a `flap` critter pose) when anyone runs through, landing again on the café awning or the statue. Same pattern as ducks (deterministic seed+`wall()` loop, client-local reaction to already-synced `pose`/position) but on dry land and with a perch to return to instead of a pond center.
- [ ] A cat on a house roof or windowsill (chain from Animals, deferred): watches the street, jumps down when a dog passes (needs the Dogs backlog item first), sits on a bench when it is empty, and a player who sits next to it gets a line. Also a `critter` kind; unlike ducks/pigeons it is stationary except for the jump-down, so it is the simplest of the three — a good next pick.
- [ ] Bees over the garden beds (thin axis: animals, 2026-09-23): a small buzzing cluster (`critter` kind `bee`, pose `buzz`) hovering over any `garden` spot, drifting in a tight seeded loop; scatters briefly if someone runs through or the bed gets watered (same trigger the watering sparkle already fires on). A player who stands still within range for ~2s without moving gets stung — a quick `flinch` (fills another line from the motion library's Reactions list) — the first animal with its own defensive reaction to the player instead of just decorative dressing.
- [ ] Fish jump at dusk (thin axis: animals, 2026-09-23 — a third small critter reaction alongside the still-unbuilt squirrels/bees above, this time tied to the day's clock instead of a prop or a player action, straight off the "fish that jump in the pond at dusk" line in "Animals, many" below): during the dusk hour (`hour() === 18`, the same purely-visual clock check Lamps-on-a-clock already established, no synced state) each pond occasionally shows a small fish arc out of the water and back in (`critter` kind `fish`, one pose `jump`, client-local seed+`wall()` loop like ducks/pigeons/squirrels/bees); any ducks nearby scatter briefly the same way a thrown item landing startles them today.
- [ ] Gulls at the café (thin axis: animals, 2026-09-24): 2-3 gulls (`critter` kind `gull`, poses `perch`/`swoop`) that sit on the café awning; a `sandwich` or `apple` left as a loose item on a café table for more than ~20s (the same local timer shape `wet`/`hideAt` already use) gets swooped and taken — "a gull got it." toast, item removed with the existing `pick`-style `ev`, judged locally by whichever client owns the drop, same as ducks stealing a dropped fish above. The first animal reaction to *leaving food unattended* rather than to proximity or a prop hit — a small reason not to walk away from a plate.
- [ ] A loose park dog (thin axis: animals, 2026-09-24 — the first animal not tied to any job's routine): a stray dog wanders the park on its own client-local seed+`wall()` loop (same shape as pigeons/squirrels), no leash, no owner. `C` with empty hands and no item near it pets it (`critter` pose `sit`, a beat, tail-wag jitter) — a flat line the first time each visit ("good dog." / "doesn't bite. probably."). Distinct from the still-unbuilt dogwalker's own leashed dog (that one is a job accessory and needs the Dogs backlog item first); this one needs nothing else to land.
- [ ] A mouse by the bakery bin (thin axis: animals, 2026-09-24): a single `critter` kind `mouse`, pose `scurry`, that darts along a short seeded path out from under `bin1`'s base and back when nobody is within ~150px, and simply vanishes (not freezes, unlike squirrels) the instant anyone gets closer — the shyest animal yet, disappearing from view rather than holding still in it. Same client-local seed+`wall()` loop as ducks/pigeons/squirrels, no synced state; no task/quest, purely reactive dressing (same carve-out Squirrels used).
- [ ] Rabbits at the garden beds (thin axis: animals, 2026-09-25 — animals among the thinnest again): 2-3 rabbits (`critter` kind `rabbit`, pose `hop`) nibble around `garden`/`garden2` on the same client-local seed+`wall()` loop ducks/pigeons/squirrels/bees already use, scattering in a quick hopping flee — not a freeze like squirrels, not a sting like bees — the moment the bed is watered or anyone runs through. A fourth small critter reaction to a prop, this time the garden rather than a tree, a bin or a café table.
- [ ] Frogs at the duck pond (thin axis: animals, 2026-09-25 — full-counted animals still the thinnest of the lot besides motions, at 8 open items against most other axes' 9-12): a couple of frogs (`critter` kind `frog`, poses `sit`/`hop`) perched at `pond2`'s edge among the reeds, hopping into the water and vanishing entirely for a few seconds when anyone comes within ~50px, then resurfacing a little further along the bank — the first animal that can leave the visible world for a beat instead of just freezing (squirrels), fleeing (rabbits) or vanishing in place (mice).
- [ ] Moths around a lit lamp at night (thin axis: animals, 2026-09-25 — animals still the second-thinnest axis after today's motions pick; also the first animal tied to an existing weather-and-time piece, Lamps-on-a-clock, rather than a prop hit or a player action): during the same 18:00–06:00 UTC window that already makes a `lamp` spot glow (`lit`, purely a hand check on `hour()`, no synced state), a small cluster of 2-3 moths (`critter` kind `moth`, pose `flit`) circles the lit bulb on a tight client-local seed+`wall()` loop, the same shape ducks/pigeons/squirrels already use; scatters briefly and regroups when someone leans on or kicks the lamp. Purely decorative, no task/quest (same carve-out Squirrels/Mice used) — the moths care about the lamp's own already-synced `lit`/`flickering` state, nothing new to broadcast.

## Grows from Booth calls
- [ ] Residents get their own line pool at the booth (chain from Booth, wider): `postie`/`office` already stop at booths with `act:'phone'` on their routine (that's why the reverse rule needed no code today), but they show no line at all right now, just the pose and the "on the phone" label. Give the resident side an occasional `say`/bubble line of its own (distinct from the player's `CALL_LINES` — their own business, not a wrong number) the same way a chasing resident's flat lines work, so a booth with someone on it reads like an actual call, not just a pose.
- [ ] A missed call gets a callback (thin axis: quests, chain from Booth — deepens the real-call item above): if the 1-in-6 real call handed the player a quest and it is still open (`quests.current` still has it) the next time they `C` at any phone booth, the booth rings that same resident back instead of rolling the dice again — the resident's `q.ask` line comes through once more, a little sharper ("you said you would.") from a small fixed nag-line pool, and the wait is shorter (~1s, not 2.2s) since it isn't a stock wrong-number call. No new state: it is the same `quests.current` lookup the in-person `E` talk already does, just triggered from the booth instead of proximity to the resident.

## Grows from Tree
- [ ] An apple that lands on someone (chain from Tree, today's item): if a resident is standing under a tree (`dist` to the tree spot, same range as the shake itself) when it is shaken, the apple that would have dropped instead beans them — a startled line, and the apple still ends up as a loose item at their feet rather than the tree's base. Reuses the exact roll and `drop` call already in `SquareGame.tsx`'s shaking block, just re-targeted; no new machinery.
- [ ] The grocer collects (chain from Tree, wider): the `grocer` job, already at the market stall, occasionally detours to a tree spot on its routine and picks up any loose `apple` sitting there (same fetch behaviour repairers already use for stray items) instead of leaving it for a player — ties the new fruit into the existing stall economy once buying lands (see "Every prop does something" → stall).

## Grows from Lamp
- [ ] Shoved into the post (chain from Lamp, today's item): a player who punches or kicks someone currently leaning (`n.act === 'lean'` or, for a leaning human, checking `others`' pose) staggers them back exactly onto the lamp instead of the usual knockback offset, with a short line about it ("into the lamp. nice."); three such hits on the same lamp in a day crack it through the existing `BREAKABLE`/`broken` machinery, same as a direct kick — ties leaning and the kick-flicker together instead of leaving the two lamp interactions unrelated to each other.

## Grows from Repairer
- [ ] A busier square calls in a second fixer (chain from Repairer, today's item, thin axis: jobs): today's opportunistic repair roll (`Math.random() < 0.02` per idle `REPAIRERS`-job resident per frame) already skips a prop someone else is already targeting, so two fixers never pile onto one break — but nothing yet speeds up when the backlog builds. While two or more props are broken at once on the same map, double that roll (`0.04`) for any other idle repairer, so a square with several things broken empties its backlog faster instead of everything just sitting there until the 5-minute auto-clear. No new state: reuses `broken.current` and the existing `props.filter(...).length` check, just gates the constant.
- [ ] A repairer who is thanked remembers (chain from Repairer, deferred until reputation lands): helping finish a repair (`C` near an in-progress fix, landed today) should raise standing with the repairer job specifically, distinct from the general "every job" reputation in "Player growth" — noted here rather than built now since no reputation store exists yet to raise.

## Grows from Affinity
- [ ] A passing glance (chain from Affinity, thin axis: relationships and children, 2026-09-22 — a small use of `affinityOf`/`pairsFancying` ahead of the full Relationships item): when two residents who are both in today's roster and come back from `pairsFancying(day, roster)` pass within ~80px of each other on their routines, they exchange a short seeded line ("oh. hello.") and a quick glance (reuse `face` flip toward each other for a beat, no new pose needed yet — the motion library's `wave`/idle-fidget items are the natural upgrade once one lands). Deterministic from seed + room clock so every spectator sees the same two residents share the moment. No couples, houses or weddings yet — that stays the big Relationships item.
- [ ] A shared bench (thin axis: relationships and children, 2026-09-23 — a second small step ahead of full Relationships, this time on a bench instead of in passing): when two residents who fancy each other (`pairsFancying`) both have a `bench`-kind stop in their routine, nudge their stop timing (same seeded offset idea `routineAt`/`routine` already use per-stop) so the stops overlap and they land on the *same* bench together for the shared part of it instead of two coincidentally-empty seats — reuse the existing `seat`/`sit` pose, no new one, just two residents sitting closer than usual with a short exchanged line like the passing glance above. Still no couples, houses or persisted state; the big Relationships item stays the one that adds those.
- [ ] A rivalry glance (thin axis: relationships and children, 2026-09-23 — the unhappy mirror of "A passing glance" above): reuse `affinityOf` at the *low* end (say `<= 0.15`) as a seeded pair who cannot stand each other; when two such residents pass within ~80px on their routines they exchange a short cold line ("you again.") and step slightly apart instead of glancing toward one another — same mechanism as the passing-glance item (seed + room clock, no new state, reuse the existing `face`-flip trick, just mirrored away instead of toward). No couples, houses or feuds yet — same "stays the big Relationships item" caveat.
- [ ] A returned smile at the gate (thin axis: relationships and children, 2026-09-23 — a fourth small step ahead of full Relationships, this time at a shared exit instead of a bench or a passing moment): when two residents who fancy each other (`pairsFancying`) are both leaving the same map through the same `Exit` within a few seconds of each other (their `routineAt` legs both crossing that exit's x/d), the second one glances back at the first with a short seeded line ("you too, then.") before continuing off-map — same mechanism as the passing glance and shared bench above (seed + room clock, no new state, reuse the `face`-flip trick), just at a doorway instead of a bench or in passing. Still no couples, houses or persisted state; the big Relationships item stays the one that adds those.
- [ ] Walking part of the way together (thin axis: relationships and children, 2026-09-23 — a fifth small step ahead of full Relationships, this time along a shared stretch of route rather than at a bench, a doorway, or a passing moment): when two residents who fancy each other (`pairsFancying`) have routine legs that run the same direction on the same map for a stretch, nudge one's pace by a few percent (same seeded-offset idea `routineAt` already uses for the shared-bench item) so they end up walking that stretch side by side instead of independently timed — no new pose (`run` already draws fine for two adjacent figures, the way linked residents already stand adjacent in "Brace-with"), no synced state, same seed+clock mechanism as the passing glance/shared bench/rivalry glance/returned smile above. Still no couples, houses or persisted state; the big Relationships item stays the one that adds those.
- [ ] A look across the square (thin axis: relationships and children, 2026-09-24 — a sixth small step ahead of full Relationships, this time between two residents who never actually meet): when two residents who fancy each other (`pairsFancying`) are both stationary on the same map (`act === 'stand'`/`sit`/etc., not mid-route) more than 150px apart — the one case the passing-glance/shared-bench/rivalry-glance/returned-smile/walking-together items all skip, since those need the pair to actually cross paths — they occasionally turn to face each other's direction for a beat (reuse the `face`-flip trick) with a short seeded line ("still there, i see.") before going back to whatever they were doing. Same seed+room-clock mechanism, no synced state, no new pose. Still no couples, houses or persisted state; the big Relationships item stays the one that adds those.

## Grows from Fishing-things-out
- [ ] A resident notices a still-wet item (chain from Fishing-things-out, today's item): a resident whose routine stop puts them within ~120px of a loose item currently flagged `wet` (the drip-marked minute right after a rake pulls it out, `SquareGame.tsx`) says a short flat line about it once ("careful, that's still wet.") — same shape as the booth/lamp one-off reactions, no new state since the local `wet` map already carries the deadline, just a second reader of it.
- [ ] Ducks investigate the rim (chain from Fishing-things-out, wider — pairs with the deferred "ducks steal a dropped fish" idea under Fishing): a duck near a pond whose rim just got a fresh rake-drop drifts toward it for a few seconds the same way ducks already converge on a feeding spot (`feedRef`-style local target), then drifts off — client-local like the rest of duck behaviour, no new `ev` kind.

## Grows from Interception
- [ ] A caught throw sometimes comes right back (chain from Interception, wider): a resident with a temper roll (reuse the existing `angry`/`job.temper` check already used to decide chase-vs-flee) skips the walk-it-back and instead relaunches the caught item straight at the human who threw it, reusing the NPC-throw arc that already exists in the chase loop (`n.threw`) but triggered immediately instead of waiting for the usual chase-distance window; a calmer resident still does today's walk-it-back deliver.
- [ ] A resident who catches for you remembers it (chain from Interception, deferred until reputation lands): being caught rather than knocked down should raise standing with that specific resident, distinct from the general per-job reputation in "Player growth" — note here until that store exists.
- [ ] A dropped mid-delivery item doesn't get slide-under's tuck (chain from Interception/Board/Relay-and-shelving, 2026-09-25 — deepens today's polish fix): today's polish (`SquareGame.tsx`) fixed `n.caught`/`n.retX`/`n.retD`/`n.stowUntil` never being cleared on knockdown, so a resident mid-`catch`/`deliver`/`board`/`shelve` who gets hit now correctly drops what they were carrying for someone else instead of it floating forever — but that new drop calls `drop()` without `slideNear`, so it always lands fully in the open, unlike the resident's own item at the same knockdown site (which already tucks near a bench or water's edge). Route it through the same `slideNear` check for consistency.

## Grows from Holding-on
- [ ] A brace that outlasts the human becomes a steal (chain from Holding on, wider): today the roll (brace → stow-or-weight → steal, `SquareGame.tsx`, fixed 2026-09-23 to comply with the holiday rule) re-runs identically on every `C`, so a human who just waits out someone else's 3-second brace gets another fair roll instead of anything changing — if the *same* human is still within `GRAB_R` when a brace's `until` expires, skip straight to a plain steal instead of re-rolling (they clearly were not giving up), while a human who wandered off and comes back still gets the full roll. Reuses `n.until` and the distance already computed at the `C` site, no new state.
- [ ] Weight-down and stow-in-mug tell a resident who saw it (chain from Holding on, wider — deepens the 2026-09-23 rebalance): the merged "hold" roll (stow near a café, weight-down elsewhere) picks silently between the two by `cafeNear`; a `routine`-mode resident of the same job standing within ~150px who watches their colleague weigh down or stow an item could get its own one-off flat line about it ("saw that.") the next time the human tries the same trick on them — the seed of a per-job "word gets around" reaction, distinct from the per-resident memory Decoy's old chain wanted (removed today) — note here until the reputation store from Player growth exists to back it with something more than a line.

## Grows from Board
- [ ] A note that outlives the item (chain from Board, today's item): today a pinned note only exists for as long as its item sits in the crate (`Loose.note`, cleared the moment someone takes it) — the original ask wanted notes that stay ("up to five, oldest falls off") independent of whether the item's already been claimed. Keep a small separate per-board list of the last five notes (text only, no `Loose` needed) so a passerby can still read "sandwich — courier's, taken" after it's gone — closer to a real corkboard than a shelf.

## Grows from Relay and shelving
- [ ] A shelf that fills up (chain from Relay and shelving, today's item): unlike the board's crate (capped at five, oldest evicted), today's shelf drop has no cap at all — a bakery nobody visits could grow a wall of unclaimed baskets. Give each building's shelf the same "oldest falls off" rule the crate already has (reuse the `crated.length >= 5` pattern from the board-delivery code, keyed by `l.shelf === shelfKey` instead of `l.board`), so a crowded shelf starts losing its oldest item back onto the floor instead of stacking forever.
- [ ] The building's own resident notices you taking one (chain from Relay and shelving, wider): today taking a shelved item is the same silent `C` pickup as the board's crate — but a baker who is *at* the bakery when a human takes something off their own shelf (not necessarily the item's owner, just whoever is working the counter that moment) should say a flat line about it ("that's not yours either, but fine"), the same one-off-reaction shape the lamp/tree/booth props already use for a resident standing nearby.

## Grows from Dust-off
- [ ] Tripping gets the same grace (chain from Dust-off, today's item, wider): a resident who trips while chasing (`n.mode === 'trip'`) already drops whatever they're carrying on the spot — today a human can grab it the instant it lands, no protection at all, unlike a knockdown which now gets the ~2s `dust` grace once the resident is back up. When a trip's `tripUntil` clears and the resident resumes (chase or gives up), route through the same `dust` mode instead of straight back to `chase`/`return`, so a tripped-and-dropped item gets the same one-time "can't see straight" miss a knocked-down one does — otherwise tripping stays the strictly worse of the two ways for a resident to lose something.

## Grows from Slide-under
- [ ] The trip drop gets the same tuck (chain from Slide-under, today's item, wider): today's `slideNear` bench/water-edge check only runs at the two knockdown drop sites in `hit()` — the resident-trips-while-chasing drop (`SquareGame.tsx`, the per-second trip roll) still always lands the item in the open, its own `drop(n.item, ...)` call never wired to `slideNear`. Route that third drop through the same check so a resident who trips near a bench or a pond's edge tucks what they were carrying too, instead of tripping staying the one drop path Slide-under didn't reach.

## Grows from Stow-in-mug
- [ ] Pass-along can finally read a stow (chain from Stow-in-mug, today's item, wider — unblocks part of the still-unbuilt Pass-along item under Keeping it): today a stowed item is only protected from a human's `C`; add the resident side the original ask already promised — a *different* routine-mode resident within `GRAB_R` of a stowed carrier can take it from them exactly once (the same shape as a human steal, but npc-to-npc, no player input), timed to fire just before the carrier's `stowUntil` runs out so the item changes hands before it becomes grabbable again — the missing piece Pass-along already assumes exists.

## Grows from Weight-down
- [ ] The brick doesn't disappear (chain from Weight-down, today's item): once a human finally works an item free from under a dropped brick, the brick itself should stay behind as a plain loose `brick` `ItemKey` (throwable, binnable, dunkable like anything else) instead of just vanishing with the pickup — a resident's opportunistic fetch (the same one that already tidies stray items into the board/shelf system) would then eventually clear it like any other litter, so it doesn't sit there forever.

## Grows from Tether
- [ ] Tether protects against a thrown hit too (chain from Tether, today's item, wider): today's protection only guards the punch/kick knockdown in `hit()` — a resident holding a just-recovered (`n.tether`) item who gets knocked down by a *thrown* item (the separate npc-hit block further down in `SquareGame.tsx`) still drops everything on the very first hit, same as before Tether landed. Route that knockdown through the same `dislodge` check (skip the drop, flip `tetherMissed`, sync `tether`/`tm`) so a tethered resident is protected from either kind of first knockdown, not just melee.

## Grows from Sell
- [ ] A resident notices what you sell (chain from Sell, wider): a `routine`-mode resident whose job item (`job.item`) matches the item you just sold, standing within ~150px, gets a one-off flat line about it ("that could've been someone's.") — same one-off-reaction shape the lamp/tree/booth props already use, no new state.

## Grows from Brace-with
- [ ] A human who waits out the link doesn't get a free re-roll (chain from Brace-with, today's item, wider — mirrors the 2026-09-23 fix to Holding-on's outlasted-brace rule): today the moment a linked pair's 14s window lapses, the very next `C` on either of them rolls a completely fresh steal (brace → hold, still capped at two by the holiday rule) even if the same human never left their side. If the same human is still within `GRAB_R` when a link ends, skip straight to the plain steal outcome once (same shape as the outlasted-brace fix) instead of handing back a full re-roll — waiting out a link should cost the same as waiting out a brace, not less.
- [ ] A passerby notices the between-them trick (chain from Brace-with, deferred until reputation lands): a `routine`-mode resident of either linked resident's job standing within ~150px who watches a human successfully wait out the linked-pair pickup delay could get a one-off flat line about it next time ("saw that.") — same deferred-reputation shape as the Interception and Holding-on chains above; note here until the reputation store from Player growth exists to back it with more than a line.

## Grows from Squirrels
- [ ] A squirrel makes off with a dropped apple (chain from Squirrels, wider — pairs with the still-deferred "ducks steal a dropped fish" under Fishing): an `apple` sitting as a loose item within ~90px of a tree's squirrels for more than a few seconds gets carried off by one of them — same "owning client judges it, removes with the existing `pick`-style `ev`" shape ducks-stealing-fish/gulls already use, with a short "a squirrel got it." toast — the second animal (after gulls) to react to unattended food rather than to proximity or a prop hit.
- [ ] A squirrel that's just climbed drops down startled (chain from Squirrels, wider): today a squirrel mid-`climb` just keeps climbing until its 1.8s window runs out even if the shake ends early or a second shake restarts the timer indefinitely; give it a matching `drop` beat (reuse the still-unclaimed `flinch` off the motion library's Reactions list, scaled down for a critter) when the tree stops being shaken for more than a second while a squirrel is still up it, so climbing always resolves instead of only ever timing out.

## Grows from Mend request
- [ ] A repairer who is thanked remembers a mend (chain from Mend request, deferred until reputation lands): the resident who services a pinned item today just re-drops it with an updated note and no lasting effect — once the reputation store from Player growth exists, finishing a mend should raise standing with the repairer job specifically, the same deferred shape "A repairer who is thanked remembers" (Grows from Repairer) already notes for the broken-prop case; note here until that store exists.

## Grows from Sort the scraps
- [ ] Sorted the right way (chain from Sort the scraps, today's item, wider): today every `sort`-flagged item gets the same generic line pool and just vanishes once a sweeper reaches it, regardless of what it actually is — split the outcome by kind instead: `FOOD` items keep vanishing (compost, the current behaviour is already right for these), but anything else has a chance to detour to the lost-and-found board instead of disappearing for good, the same way an unclaimed stray item already does — reuse the existing `n.mode = 'board'` path (pin-with-a-note) rather than inventing a second disposal outcome, so a binned wrench or hat has a chance of turning up on the board instead of just being gone.

## Grows from Keep-lock
- [ ] The lock reaches repair, rake and the board too (chain from Keep-lock, today's item, wider; folded in a town wish 2026-09-24 asking for the same reservation window on a freshly-pinned board item): today only a finished Mend request sets the `lockAt` timer (`SquareGame.tsx`) — the repairer's plain broken-prop fix, the rake's fished-out-of-the-water item, and a just-pinned board/crate item all resolve through their own drop but leave the result immediately grabbable. Route all three through the same `lockAt.current.set(id, now + 15000)` call the mend finish uses, so any resident's finished timed service (and a fresh pin) protects its result for the same 15s — the base mechanic was built generic on purpose, this just widens who calls it.
- [ ] A passerby notices someone waiting out a lock (chain from Keep-lock, deferred until reputation lands): a `routine`-mode resident within ~150px who watches a human hover near a locked item until it opens up could get a one-off flat line about it ("patient, aren't you.") — same deferred-reputation shape as the Interception/Holding-on/Brace-with chains above; note here until the reputation store from Player growth exists to back it with more than a line.

## Grows from Checkers
- [ ] A passerby comments on the score (chain from Checkers, today's item, wider): a `routine`-mode resident passing within ~150px of the chess table when the score's 10s bucket flips gets a small seeded chance at a one-off flat line about it ("someone's winning.") — the same one-off-reaction shape the lamp/tree/booth props already use, no new state (the bucket flip is already computed for the slate itself); ties the ambient, always-running slate to the rest of the square instead of leaving it scenery nobody remarks on.

## Grows from Pebble-toss
- [ ] A passerby comments on the throw (chain from Pebble-toss, today's item): a `routine`-mode resident passing within ~150px of the pebble-toss line when the score's 10s bucket flips gets a small seeded chance at a one-off flat line about it ("nice arc." / "short again.") — the exact shape Checkers' own passerby-comment chain above already established, no new state (the bucket flip is already computed for the scoreboard itself).

## Grows from Bocce
- [ ] A passerby comments on the roll (chain from Bocce, today's item): the same passerby-comment shape Checkers and Pebble-toss both already got (`routine`-mode resident within ~150px, small seeded chance on the score's 10s bucket flip, a flat line — "close one." / "that's a miss.") — the third and last of this run's turn-based spots to want it, no new state.

## Grows from Light-chain board
- [ ] A passerby comments on the chain (chain from the light-chain board, today's item): the same passerby-comment shape Checkers/Pebble-toss/Bocce already got (`routine`-mode resident within ~150px, small seeded chance right when the 6-second bucket flips to a new lit pad, a flat line — "still going." / "that one broke it.") — the fourth turn-based-family spot to want it, no new state, same reasoning as the other three.
- [ ] A 'spectate' completion for the light-chain board (chain from the light-chain board, today's item — the existing `spectate` quest kind already generalizes to "any future turn-based spot"): confirm/extend the check so standing near `simon1` while its streak-slate changes twice (two 6-second bucket flips this time, not chess/pebble/bocce's 10-second ones) satisfies a `spectate` quest the same way the chess table already does — a config tweak on an existing quest kind, not a new one.

## Climb grows too (owner, 2026-09-22 — the tower has had nothing new)
- [ ] Rest floors with life: every 5th band is a rest floor already; give it props from the town (a bench to `seat` on, a vending machine that sells a cup for coins, a bin, a lamp) and one or two residents doing what they do in Square (reading, on the phone, eating) with the same poses — the engine's props and jobs, not a Climb-only copy.
- [ ] Weather on the tower: the seeded daily weather (see Weather) reaches Climb — rain makes `ice` platforms out of `std` ones on some bands, wind gusts (the existing `windOf`) get stronger in a storm with a visible streak, snow above 300 m in winter months.
- [ ] Things to carry up: loose items on platforms (a hat, an umbrella) you can `C` to pick up and wear (the same wearing code as Square); carrying two things makes the charge jump 10% shorter; a resident on a rest floor asks for one of them (a quest that pays coins).
- [ ] Residents that climb: a couple of roster residents (jogger, courier, kid) actually climb the tower on a seeded schedule with the same jump physics as the player — you can see them charge, jump, miss and fall, and they say something flat when they land on you. Same `step()` code, no cheating.
- [ ] Hazards with motion: birds (from Animals) that cross a band and knock you sideways if you jump into them (`flap` critter already exists); a resident with a broom who sweeps a platform and pushes loose items — and you — off the edge if you stand still too long (`sweep` pose).
- [ ] Records that mean something: the best height already persists; add a plaque at the height of each day's best climber (their handle, their colour), visible to everyone at that band, and a small ceremony line from a resident when someone passes it.
- [ ] Windy bands brace you (thin axis: Climb, 2026-09-23 — reuses today's new pose): on a band where `windOf` rolls a strong gust, briefly force the climbing figure (the player, and residents once "Residents that climb" lands) into the new `brace` `FigPose` (added today, `stickman.ts`) — feet planted, no jump input read for ~0.4s — instead of only nudging position sideways the way wind does today; same idea as a gust that makes you dig in rather than just drift.
- [ ] A dropped item lands in the square below (thin axis: Climb, 2026-09-24 — the first time something that happens in one game shows up in the other, not just shared code): a loose item a climber lets go of, or drops on a fall, doesn't just vanish — write it into that player's `/api/goose` state (the same per-user persistence other cross-visit state already uses) as "waiting at the tower door"; the next time they (or, once "Residents that climb" lands, any resident) are in the square, it appears as a normal loose item there for anyone to take. Climb and Square run on separate rooms (`/ws/climb`, `/ws/square`), so this is a mailbox between visits, not a live sync — same tradeoff the patrol's own cross-system writes already accept.
- [ ] A personal-best cheer (thin axis: Climb, 2026-09-23 — the small, standalone half of the still-unbuilt "Records that mean something" plaque above): the moment a climber's height passes their own previous best (already persisted, `tower.ts`), a one-off flat line and a small burst of upward-drifting motes at the player's position — purely local, client-side celebration, no synced state and no new pose, unlike the shared plaque item which needs everyone to agree on the day's best.
- [ ] Idle fidgets on rest floors (thin axis: Climb, 2026-09-23 — the engine already knows how, Climb just never asks): a climber standing still on a rest floor between charges (the player, and residents once "Residents that climb" lands) cycles through the same seeded idle fidgets Square already uses (`yawn`/`stretch`/`look`/`check`/`shrug`/`sneeze`, `stickman.ts`) instead of a bare stand — same shared `(t + seed) % 22` clock check as Square's, no new state, since `figure()` already draws every one of these poses and Climb just never calls for them.
- [ ] Wind streaks, ahead of the storm variant (thin axis: Climb, 2026-09-24): `windOf` already varies gust strength per band (the value "Windy bands brace you" uses to force the `brace` pose) — before Weather brings a real storm to the tower, draw a light streak effect across bands with a strong gust (purely visual, keyed on the same seeded `windOf` value every client already computes, no new state) so a windy band reads as windy even between gust-brace moments — same de-risking approach Lamps-on-a-clock and Dawn-and-dusk light used for Night before the harder behaviour landed.
- [ ] A dropped item's landing puff at the base (thin axis: Climb, 2026-09-25): an item lost off the tower entirely — past band 0, so it misses "A dropped item lands in the square below" above's mailbox — just vanishes today; give it a small dust-puff sprite at the base line instead, the same one-off local-motes trick "A personal-best cheer" already uses for its own celebration, purely visual and client-local, no synced state.

## New houses the town proposes (owner, 2026-09-22 — houses must vary, and the town must be able to add them)
- [ ] Town-proposed houses: allow `house` in the patrol's proposal kinds (`patrol/square-grow.mjs` KINDS — that file is the patrol's, so the game side must accept `house` spots from `site_meta.square_content` first). For every house spot that is not one of the three originals, the game generates an interior map (`houseInterior` with a seed from the spot key: room width, which furniture exists and where, wall colour) and an owner (a roster resident who owns no house yet, chosen by seed), adds a door exit both ways, and draws the exterior with the seeded variety from "House exteriors vary". The owner's routine gains the home stop like the original owners. Then tell the patrol it may propose houses by adding `house` to KINDS in `square-grow.mjs` (one line — allowed for this item only, note it in the log).

- [ ] The market alley (thin axis: places, 2026-09-23 — unblocks "The found stall in the alley" above, which already names this map): a new narrow map `marketalley` (connect: street), a tight lane behind Market street with two or three small stalls closer together than the open ones, a bin, one lamp — the game side of the place the Lost-and-found system's third slice already assumes exists.
- [ ] A clinic on Market street (thin axis: places, 2026-09-24 — gives the still-generic "clinic" in Building kinds an actual spot and the simplest interaction that fits it): a new `clinic` building spot (`world.ts`, same shape as `bakery`/`post`/`station`) between the church and the fish stall; a `nurse` job (new, item `bag`) stationed there. `C` near the clinic on a resident currently `mode === 'down'` (or the player, if hurt) patches them up — shortens the remaining `down`/`hurt` time instead of waiting it out, the same kind of assist the repairer/rake helpers already give, just aimed at a knocked-over figure instead of a broken prop or a dunked item.
- [ ] A footbridge at the pond (thin axis: places, 2026-09-23 — the first place item that's a piece of terrain rather than a building or a whole map, per the axis's own definition): a short bridge prop spanning the little pond's narrow end in the park, drawn as a walkable strip a few px above the water instead of a `Spot` you interact with, connecting the two banks directly; anyone shoved while standing on it goes into the pond the same way a shove near the open water edge already can. Small, no new machinery — just a prop kind that changes where you can stand.
- [ ] A wooden dock on the little pond (thin axis: places, 2026-09-24 — a second terrain piece alongside the footbridge and the church steps, per the axis's own "terrain" note; hand-counted places as the thinnest of the eleven axes GROW.md names, tied with jobs): a short walkable jetty jutting a few steps out over the little pond in the park, reached by a short jump like the church steps — gives the still-unbuilt rowboat (see Vehicles) somewhere to be tied up and the still-unbuilt fishing pier idea a nearer cousin, without building either of those yet. `retired` sometimes stands at the end of it instead of the bank on its pond-side routine stop (reuse the existing `stand` pose, no new one needed).
- [ ] A bus stop on Market street (thin axis: places, 2026-09-23): a new `busstop` prop (`PropKind`, `world.ts`) with a small shelter and a bench; `C` with empty hands sits under it (reuse the existing `sit` pose and `SITTABLE` handling, no new pose) like any other bench. It's also the covered spot Weather's rain plan already names ("their own door or the bus stop") — landing the spot now gives that future behaviour somewhere concrete to route to. No job routes through it yet; it's furniture until Weather (or a transit item) gives it a reason to matter, same as the footbridge landing ahead of real terrain traffic.
- [ ] A maintenance yard on Market street (thin axis: places, 2026-09-24 — hand-counted places as the thinnest axis again; also the concrete spot the merged Pegboard hang-tool wish under Keep-lock above is waiting on): a small fenced side-yard off Market street — a shed wall, a pegboard, a bin — gives the `repairer` job a home base to route through on its own routine (reuse the existing `stand`/`sweep` acts, no new pose) instead of only ever passing through on the way to whatever just broke.
- [ ] A library on Market street (thin axis: places, 2026-09-25 — hand-counted places and quests the two thinnest again; gives the still-generic "library" in Building kinds an actual spot, the same way the clinic item gave "clinic" one): a new `library` building spot (`world.ts`, same shape as `bakery`/`post`/`clinic`) between the post office and the church; `C` near it with empty hands borrows the shelf's book the same way a house's bookshelf already lends a `read` pose, but taking one here also completes a `library1` task the way `shelf1` (Relay and shelving) does — a librarian job isn't required for the first slice, `priest`/`retired` already route past on `steps1`/`church` and can be given this stop later.
- [ ] A newsstand on Market street (thin axis: places, 2026-09-25 — places among the thinnest again): a new `newsstand` prop (`world.ts`, same shape as `bakery`/`library`) between the post office and the police station; `C` with empty hands takes a `paper` (existing `ItemKey`, today only ever starts out already carried by `postie`) the same free-lend way the library's shelf above hands out a book — no coin, no new pose, just an actual place to pick one up instead of it only ever spawning pre-carried.

## Grows from Steps
- [ ] Wedding guests line the steps (chain from Steps, today's item, wider — ties into the still-unbuilt Relationships wedding): once the monthly church wedding lands (see Relationships), guests gather and sit on `steps1` before the ceremony (reuses the sittable spot `steps1` already added, no new prop) instead of everyone just spawning at the door — gives the procession an actual assembly point.

## Beyond the square (owner, 2026-09-22 — later: a farm, a tractor, digging into the mountain, many animals, builders)
- [ ] Vehicles: a tractor at the fields (see Fields) that the farmer drives along the rows (`drive` pose: seated, hands on the wheel, the tractor a drawn prop that moves; residents step out of its way, ducks scatter); a player can `C` to climb on when it is parked and drive it slowly (arrows), which the farmer objects to and chases. Later the same code gives the street a delivery van for the courier and a police car that arrives when the cop is called.
- [ ] A bike rack (thin axis: vehicles, 2026-09-22): a new prop by the market street and the park gate; `courier` and `jogger` take a bike from it at the start of their routine and ride between stops noticeably faster than walking (same `run` cadence but with a drawn bike prop under the figure, no new pose needed — the bike is carried like an item, not a body change). Players can take a bike the same way empty-handed residents do (`C` at the rack) and move faster until they park it back at any rack or drop it; a dropped bike is a loose prop others can also take. First vehicle a player can actually ride day one, ahead of the tractor.
- [ ] A rowboat on the pond (thin axis: vehicles, 2026-09-22): a boat prop tied up at the little pond; `C` with empty hands boards it (new `row` `FigPose`: seated, oars pulling), drifts a slow seeded loop away from the bank and back, ducks give it a wider berth than a swimmer. `C` again gets out at the near bank. `retired` (already the pond-side job via the rod rack) takes the boat out on some stops instead of fishing, same pose, so the water is not only a fishing spot. No steering — the loop is the ride, same spirit as the bike being speed-not-control.
- [ ] A police car at the station (thin axis: vehicles, 2026-09-22): a parked car prop by the police station; when the cop job is summoned to chase (see Temperaments — call the police) and the target is more than ~400px away, the cop gets in (`drive` pose, seated, hands on the wheel — same pose the tractor item wants, first thing to actually use it) and drives over at `RUN` speed along the street's lane instead of walking, a short honk line as they arrive, then gets out to chase on foot like today. When it is parked and empty, a player can `C` to take it for a slow joyride the same way the tractor works (open world, nothing locked) — the cop who owns it notices, chases on foot, and the fine (see station/money items) is worse than any other theft, which is the deterrent rather than a lock.
- [ ] A delivery van for the courier (thin axis: vehicles, 2026-09-23): a parked van prop by the post office; `courier` drives it (new `drive` `FigPose` — none of today's other vehicle items have landed yet either, so this is the one that actually needs it first: seated, hands on the wheel) between post/bakery/houses on its routine instead of walking, stepping residents and scattering ducks out of the lane like the tractor wants to. Parked and empty, a player can `C` to climb in and joyride it slowly (open world, nothing locked, same spirit as the bike/tractor); the courier notices it is gone, chases on foot, and is short with you for the rest of the day.
- [ ] A wheelbarrow at the garden and building site (thin axis: vehicles, 2026-09-23 — doubles as the still-unbuilt `push` motion from the library): `gardener` (between garden beds) and, once Builders lands, builders (between the plank pile and the frame) push a loaded wheelbarrow instead of carrying by hand (new `push` `FigPose`, reused from the motion library's Work list rather than invented fresh); a player with empty hands can `C` an idle wheelbarrow to grab its handles and push it themselves (same `push` pose, slower than walking, no punch/kick while pushing, `C` again lets go where it stands) — a small utility vehicle, not a rideable one, so it needs no joyride/theft handling.
- [ ] A skateboard by the playground (thin axis: vehicles, 2026-09-23 — the small, kid-sized companion to the bike rack, and the first vehicle inside the park rather than the street or pond): a `skateboard` prop propped against the swings; `kid` residents ride it between playground stops on their routine — same `run` cadence with a drawn board under the feet, no new pose needed (the same trick the bike rack item already wants for `courier`/`jogger`). Players with empty hands can `C` it to ride (same speed-not-control handling the bike rack specifies: faster than walking, drop it anywhere with `C`, a dropped board is a loose prop anyone — human or kid — can take next).
- [ ] A moped for the postie (thin axis: vehicles, 2026-09-23 — postie already routes between the post office and all three houses, the natural first rider): a parked moped prop by the post office; `postie` rides it (new `drive` `FigPose` — none of today's other vehicle items have landed yet either, so this is just another job waiting on the same pose, seated, hands on the bars) between stops instead of walking, faster like the courier's van. Parked and empty, a player can `C` to take it for a slow joyride the same way the tractor/van work (open world, nothing locked); the postie notices it is gone, chases on foot, and is short with you about it for the rest of the day.
- [ ] A gardener's cart (thin axis: vehicles, 2026-09-24 — ties into today's polish fix that finally gave `gardener` real houseplant stops, `world.ts`): `gardener` now pulls a small two-bucket cart between `garden`/`garden2` and the three houseplant stops instead of carrying nothing extra between them — drawn as a prop trailing a step behind on the ground (no steering, same speed-not-control spirit as the bike), shares the still-unbuilt `push`/`pull` `FigPose` the wheelbarrow item above already wants rather than inventing a second one. Parked and idle, a player with empty hands can `C` it to pull it themselves between garden spots (slower than walking, `C` again lets go); watering from a full cart doubles the `water` action's puddle-sparkle duration for a few uses before it needs refilling at a garden.
- [ ] A kid's pull-wagon in the park (thin axis: vehicles, 2026-09-24): a small wagon prop by the swings; `kid` residents pull it between playground stops on their routine (one more job waiting on the still-unbuilt `push`/`pull` `FigPose` the wheelbarrow and gardener's cart items above already want) with a stuffed animal or ball riding loose inside that a player can take with `C` once the wagon is parked and empty — the passenger, not the wagon, is what changes hands. Parking it (`C` again) leaves it for the next kid or player to pull. First vehicle inside the play area itself rather than the street, pond or garden.
- [ ] The mountain: a new outdoor map beyond the fields with a slope (the 2.5D stage gets a ground line that rises), a path with switchbacks, a quarry face and a mine mouth. A `miner` job digs at the face (`dig` pose, pickaxe, a chip of rock each swing) and carries ore to a cart; players can dig too (`C` with empty hands) and find things (a seeded loot table: stone, a coin, a fossil that becomes a plaque in the square). Climbing the slope uses the tower's jump physics on the rising ground. Rockfalls on the path when it rains (Weather) that block it until a builder clears them.
- [ ] Animals, many: after ducks — pigeons, a cat, dogs (dogwalker), chickens at the farm (they follow whoever carries a sandwich), goats on the mountain (they butt you off the path), fish that jump in the pond at dusk, bees over fresh flowers, crows on the quarry. Every animal is a `critter` kind with two or three poses and one reaction to players, and one resident job that tends it.

## The motion library (owner, 2026-09-22 — the stick figure must have an enormous range; one per run when nothing else needs one)
- [ ] Reactions: `slip` (feet forward, arms up, lands on the back), `flee` (run with the head turned back), `flinch` (shoulders up, arms in), `brace` (feet planted, leaning against a pull), `shrug`, `wave`, `point`, `laugh` (head back, shaking), `cry` (hands to face), `sneeze`, `yawn`, `stretch`, `shiver` (cold), `fan` (hot, hand fanning the face).
- [ ] Work: `dig`, `drive`, `carry` (heavy, two hands low, slow gait), `push` and `pull` (a cart, the roundabout), `ladder` (climbing rungs), `hammer` (builder), `paint` (painter at an easel), `sweepwide` (long broom strokes), `serve` (tray), `cook` (pan flip), `rake`, `plant` (kneel, press soil), `harvest` (bend and pull).
- [ ] Play and rest: `slide`, `seesaw` (both ends), `spin` (on the roundabout, arms out), `wade`, `swim` (in the pond, if you fall in), `dance` (two variants), `cheer`, `clap`, `nap` (on a bench, different from the bed), `lean` (against a lamp or wall), `crouch`, `tiptoe`.
- [ ] Together: `hold` (walking hand in hand, two figures synced), `hug`, `kiss` (cheek), `argue` (facing, arms out), `handshake`, `highfive`, `carrychild` (a small figure on the shoulders).
- [ ] Transitions and idles: sit-down and stand-up in-betweens, a reach before taking, a bend before picking up, a turn step, three seeded idle fidgets while standing (weight shift, look around, check the phone), and a startle when something flies past.
Each pose is one `if (pose === '…')` block in `stickman.ts` with a Korean comment on the shape, ≤ 6 characters in the name (the room truncates), and at least one place in Square where a resident and a player both use it.
- [ ] A wave hello (thin axis: motions, 2026-09-23 — next unclaimed pose off the Reactions list above): two `routine`-mode residents with no relationship on file (no affinity pair, not currently linked — the everyday case "A passing glance" and "Brace-with" both carve out) who pass within ~50px on their separate routes trade a quick `wave` `FigPose` (arm up, a beat, back down) instead of walking by silent — same deterministic pass-by mechanism (seed + room clock, no synced state) the passing-glance/rivalry-glance/brace-with items already established. Purely reactive, no player mirror needed (same carve-out as those).
- [ ] A startle when something flies past (thin axis: motions, 2026-09-23 — closes the last unclaimed line in "Transitions and idles" above): a resident (or the player) who is *not* the target of a thrown item, but has one pass within ~40px of them without hitting, flinches — new `flinch` `FigPose` (shoulders up, arms drawn in, ~0.3s, from the still-unclaimed Reactions list too) instead of no reaction at all, the way a near-miss reads as nothing today. Judged the same way ducks scattering from a landed throw already is: client-local, off state (`thrown`/position) that is already synced, no new `ev` kind. Purely reactive for residents; the player side is the same check run against the player's own position, so both get it for free from one roll site.
- [ ] A point before setting off (thin axis: motions, 2026-09-23 — next unclaimed pose off the Reactions list above, `point`): the moment a resident's opportunistic fetch/repair/rake roll picks a target (`n.mode` flips from `routine` to `fetch`/`repair`/`rake` in the main loop, `SquareGame.tsx`) they briefly face the target and hold a new `point` `FigPose` (arm extended, forefinger out, ~0.4s) before setting off, instead of just turning and walking — a small "I see it, I'm going" beat that reads the same for a stray item, a broken prop or a dunked one. Purely reactive, no player mirror needed (the player has nothing that maps to "just noticed a fetch target").
- [x] A laugh at a good tip — built 2026-09-25 alongside the light-chain board (motion-library rule: today's main pick reused an existing pose, so add one anyway). When the player's busking tip roll succeeds, one `routine`-mode resident within 80px (`Math.random() < 0.35`, not the seed+clock shape the description here assumed — the trigger itself is already a one-off `Math.random()` roll, so there was no clock pattern to key off) gets the new `laugh` `FigPose` (head back, shoulders shaking, ~0.6s, `stickman.ts`) via `n.swing`/`n.swingKind`. Since a still-`routine`-mode resident isn't part of the periodic `npcpos` sync, had to widen the one-off `npc` event (`apply()`, `SquareGame.tsx`) to carry an optional `swing`/`sk` pair so other players' screens see it too, not just the busker's own. Purely reactive (no player mirror needed, same carve-out as Decoy/Catch/Brace).
- [ ] A cry at the third knockdown (thin axis: motions, 2026-09-24 — next unclaimed pose off the Reactions list above, `cry`): a small per-resident counter (`Npc.downCount`) increments each time `hit()`'s knockdown branch lands on them; the third knockdown in one visit (reset when they make it back to `routine`) plays a new `cry` `FigPose` (hands to face, ~0.5s) instead of the usual shoved line, from its own small line pool ("enough. just — enough."). Purely reactive, no player mirror needed (reverse-rule carve-out, same as Brace/Dust-off/Tether).
- [ ] A bend before picking up (thin axis: motions, 2026-09-25 — hand-counted motions the thinnest axis again; closes the "a reach before taking, a bend before picking up" line in "Transitions and idles" above): every `C` pickup in the game (a loose item, a steal, rummaging a bin, fishing a bite, taking from the board/shelf) snaps straight to holding with no transition — add a short (~0.2s) `bend` `FigPose` (knees flex, one arm reaching down) that plays for the split second before the item actually joins the stack, for both the player and any resident whose own fetch/steal/rummage roll just landed. Purely a wind-up on an action that already exists everywhere, so one pose reaches the largest number of moments of anything left on the Reactions/Transitions lists.
- [ ] A handshake when a delivery lands (thin axis: motions, 2026-09-25 — hand-counted motions the thinnest axis again, still 6 open items even after today's `fan` closed the last unclaimed generic Reactions-list idle fidget; next unclaimed pose off the "Together" list, `handshake`): the moment a `deliver`/`crate`/`shelf`/`reunite`-shaped task or quest actually hands an item to the asking resident, they trade a quick `handshake` `FigPose` (both arms out, a beat, back down) instead of just a thanks line — one pose reused by every existing hand-something-over completion (`complete('deliver:...')`, the board/shelf take, `reunite` once it ships), not a new mechanic. Purely reactive, no player-only half needed (both sides already meet at the same spot for the handoff).
- [ ] A nap on a bench (thin axis: motions, 2026-09-25 — motions thinnest again even after today's `laugh`; next unclaimed pose off the "Play and rest" list, `nap`): sitting still on a `bench`-kind spot specifically (not swing/sofa/bed, which already have their own seated feel) for a stretch (~15s, same "been still this long" shape the idle-fidget clock already proves out) drifts the seated `sit` pose into a new `nap` `FigPose` (head tilted onto one shoulder, slow rise-and-fall breathing) instead of holding the same upright seated frame forever; any move (or getting hit) snaps back to plain sitting or standing. Both the player and any resident whose routine stop is a bench long enough get it automatically once the timer is shared, so the reverse rule needs no separate wiring — same shape as the existing 22-second idle-fidget cycle, just gated on "how long at a bench" instead of "how long standing."

## Talking to residents means something (owner, 2026-09-22 — interaction and quests must deepen)
- [ ] Small talk with substance: `E` on a resident with no open ask gives a line drawn from who they are — their latest post's title paraphrased ("wrote something about the bus fares yesterday. nobody read it."), their job's mood, the weather, what a player did to them earlier today (they remember the last three knocks by handle) — and a small link chip under the bubble to that post (`/p/<id>`) so the square sends people into the forum. `ResidentLite.line` already carries the latest post title; extend it with the post id. Residents say these things to each other too when they pass (a two-line exchange, seeded).
- [ ] Asks that read like the resident: quest ask/thanks lines come from the job voice and, for residents with a persona line, from that; a baker asks for flour-adjacent things, the priest asks you to sit through a sermon, the kid asks for a push on the swing. Refusing an ask (walking away) is remembered for the day.
- [ ] Standing shown, not told: after reputation lands, the residents' greetings change first ("you again" / "morning" / silence and a step back), before any number is shown anywhere.

## Buildings that give roles (owner, 2026-09-22)
- [ ] Take a job: at the town hall (new building on the street; the mayor works there) `C` at the desk shows the day's openings — deputy at the police station, apprentice at the bakery, park keeper, sweeper, lifeguard at the pond, postie's helper. Taking one gives you the job's item and a small uniform mark (drawn on the figure, sent in `pos.s`), a duty list for the day (real ones: fine two people, sell five loaves, water every bed, clear every bin, pull three things from the fountain), pay in coins at the end, and standing with that job. While on duty the residents of that job treat you as one of them (they greet, they back you up in a chase); knock someone over while wearing the badge and you are fired on the spot. Residents also change jobs at the town hall on a seeded rota, so the roster shifts week to week.
- [ ] Other role buildings: the church (an usher role on Sundays), the school (a teacher for the kids, once there are kids), the clinic (a nurse who patches up residents knocked down — a `heal` interaction that shortens their `down` time), the fire station (later, with a fire). Each building = one role with three duties and one uniform mark.

## Money that means something (owner, 2026-09-22 — part-time jobs; being rich opens places; places to spend)
- [ ] Part-time work, right now: quick paid tasks you take on the spot without the town hall — carry crates for the grocer (three trips, coins per crate), hand out flyers for the busker, walk the dogwalker's dog for a lap of the park, hold the ladder for the builder, fish two things out of the fountain for the sweeper. Each is a resident's ask with a coin figure in it, a 2–3 minute loop with its own motions, and a payout line. Residents do the same jobs for each other on their routines.
- [ ] Places to spend: the café and the diner (see their items), the stall (hats, glasses, umbrellas, a better rod, a basket that carries more), the bakery, a barber (a new hair line on the figure, drawn from `pos.s`), a tailor (a colour band or a scarf on the figure), the post office (send an item to a resident's house), the church (a donation that raises standing with everyone), the cinema when the town builds one (sit in the dark with residents for a minute; a line about the film). Prices in coins, receipts as a toast, and the thing you bought visible on you.
- [ ] Money changes what you can *use*, never what you can *see or enter* (the town is open world — no locked doors, no doormen, no hidden maps): the boat sits on the pond for anyone to look at, but rowing it costs a rental (`row` pose); a house for sale has its door open and anyone walks through, but only the buyer's bed saves their spot and only the buyer furnishes it from the stall; the diner's back table seats anyone, but ordering there costs more and the waiter treats you differently. None of it is a level; it is more to do for people who have done more. Residents notice ("nice hat. expensive.") and the police fine rich players more.

## Umbrellas and the rest of the pocket (owner, 2026-09-23 — "what about using things, like putting up an umbrella")
- [ ] Umbrella: `C` with an umbrella and nothing around opens it — an `umbr` pose variant for stand/run/seat (the canopy drawn above the head, the arm up), it stays open while carried, `C` again closes it; in rain (Weather) an open umbrella keeps you dry (no slowdown, no wet line) and residents who carry one open it the moment rain starts and close it when it stops. Do this in the same run as Weather or the run right after, never before — an umbrella with no rain is a prop, not an interaction.
- [ ] Phone: `C` with a phone holds it up (`phone` pose), a resident within 120px who is on the phone too gets a "same call" line; keys: `C` at your own door (once houses can be owned) locks it; newspaper: `C` reads it standing (`read` pose, a headline from the day's top post above the head); broom: `C` sweeps loose items within 60px into a pile at your feet (three strokes, `sweep` pose) and the pile is one loose stack; basket: carrying it raises the stack limit from 3 to 5, drawn as the basket in hand.

## Fishing, later, as its own place (owner, 2026-09-23 — ponds are for the ducks; fishing was removed from the square)
- [ ] Fishing lives at a **fishing spot**, not in the town square: a river path or lake map (the town may propose it; otherwise add `riverside`, connect: park) with a wooden pier and two or three **rod stands** — a rod already set in a holder, a stool beside it, a bucket. No rod in the pocket, ever. `C` at a rod stand *starts fishing right there*: `cast` (whip forward, the float flies and lands with a ring) → `angle` (sit, float bobbing, idle fidgets) → `bite` (float dips, rod bends, `!`, 0.6 s to press `C`) → `reel` (crank, rod pulsing, 1.5 s) → `land` (fish held up, its name from `pond-items` above the head, then it is a loose `fish` item you can carry, give or bin) or `miss` / `snag`. Leaving the stand (any move) puts the rod back. Residents (retired, priest, jogger on a rest day) fish at the same stands with the same sequence and seeded outcomes; two people cannot share a stand. Task `catch a fish at the pier`, ask `bring me something from the river`. Everything visible to others (pose over the socket, the catch as an `ev`).

## Money, pocket, shops (owner, 2026-09-23 — "the diner, currency, inventory, all of that")
- [ ] Pocket panel: `I` (and a touch button) opens a small panel under the canvas — what you carry (up to 3, or 5 with a basket), what you wear, your coins, and today's earnings; items can be reordered (which one is on top matters for `C`) and dropped from the panel. Residents' pockets show when you `E` them ("carrying: a cup").
- [ ] Prices everywhere money changes hands: the stall, café, bakery and diner show a price above the counter, paying is a toast with the coins left, "no coins" is a resident line, and every paid thing appears in the pocket. Earnings: tasks, asks, part-time jobs (see Money that means something), a found coin at the fountain now and then.
- [ ] The diner first (owner priority): build the restaurant item above before the café loop if the café is not done yet; share the counter/queue/serve code between the two.

## Residents grow too — a house of their own, kept up (owner, 2026-09-23)
- [ ] Resident wallets and savings: every resident earns coins on their routine (a shift at their job = a few coins, seeded per day, persisted by the patrol in `site_meta.town_ledger`), spends at the café/stall, and saves. Show it nowhere as a number; show it as what they own.
- [ ] Buying a house: when a resident's savings pass a threshold and a plot is free (the town proposes plots as `plot` spots; the builders raise a house on one — see Builders), the resident buys it: the house gets that owner, the seeded exterior/interior from "House exteriors vary" and "Furnishing by owner", and the resident's routine gains the home stop. Over weeks more residents own homes; the roster's owners list grows; the map fills in. Population: Zero becomes a town of homeowners, not three.
- [ ] Keeping a house: owners sweep the step (`sweep`), water the window box, fix a broken window (after Wrecking), repaint (the painter job does it for coins), and add a room when rich (a second storey appears with scaffolding first). A neglected house (owner away, no coins) shows it: peeling paint, a dark window, weeds — and the sweeper complains about it. Players can help (carry a plank, water the box) and are remembered for it, or hurt it.
- [ ] Losing and moving: a wrecked house that is not rebuilt, or an owner who cannot pay, means the resident moves out — to the alley for a while (a bench and a bag), and the house goes back on the market. Nothing is permanent; the town churns.

## Wishes from the town (the town writes these itself, one per patrol)
(emptied 2026-09-25 — all ten fresh wishes were Turn-based-games-shaped: nine (ring-toss, stage-buzzer quiz, bowls, paper-plane, paper-boat, street-skittles, hopscotch, scavenger dash, memory match) were near-verbatim repeats of slices already sitting in that section, folded away with no new information; the tenth (quick-charades) was genuinely new and became that section's final slice above.)
- [ ] (town wish, 2026-09-25) Add a one-tap 'Join Game' action to the score-claim board that lets a human queue for the next 90-second resident-run challenge and shows a visible 'human waiting' signal the resident can accept to start the round.
- [ ] (town wish, 2026-09-25) Add a tappable 'Ring the Bell' booth on the square that starts a 90-second resident-run quickfire round humans can join as judge.
- [ ] (town wish, 2026-09-25) Add a 90-second 'Fountain Flip' mini-game: residents queue at the pebble stall to borrow a flick pebble, each gets one timed flip aimed at the fountain lip, scores get written to the fountain-flip board, and humans can t
- [ ] (town wish, 2026-09-25) Add a 'Start Blitz' action to the park chess table that begins a 90-second resident-run blitz where residents alternate fast moves and a human can tap 'Join Blitz' to play or judge.
- [ ] (town wish, 2026-09-25) Add a 'Start Charade' action to the prize stage that begins a 90-second resident-run mimic round where residents take turns performing one-word prompts and others buzz to guess; humans can tap 'Join as judge' or 'Join as
- [ ] (town wish, 2026-09-25) Add a 'Start Relay' action to the post office that begins a 90-second resident-run handoff relay: residents pass a visible parcel token along a set route (post office → relay locker → finish pennant) and humans can tap '
- [ ] (town wish, 2026-09-25) Add a 'Start Simon' action to the square that begins a 60–90s resident-run memory sequence where residents light a chain of nearby pads (post, lamp, booth) and a human can tap 'Join' to play or judge.
- [ ] (town wish, 2026-09-25) Add a 'Start Story Chain' action to a new Sentence Board: begins a 90‑second resident round where each resident adds exactly one word token in turn, humans can tap 'Join as judge' to accept or veto words (three vetoes en
- [ ] (town wish, 2026-09-25) Add a 'Start Scavenge' action to the toss kiosk that begins a 90‑second resident-run micro hide-and-seek: residents each hide three pebble tokens around the square/street/park and a human can tap 'Join as Seeker' to get
- [ ] (town wish, 2026-09-25) Add a 'Start Hot Potato' action to the hot-sack booth on the square that begins a 90-second resident-run hot-potato: a warm-sack token is passed between residents, a random timer stops it and the holder is out, eliminati
- [ ] (town wish, 2026-09-25) Add a 'Start Tag' action to the park's tag post that begins a 90‑second resident-run Tag round where one resident starts 'It', taps transfer 'It', and a human can tap 'Join as Runner' to play or 'Join as Spectator' to wa
- [ ] (town wish, 2026-09-25) Add a 'Start Regatta' action to the square fountain that begins a 90‑second resident-run paper-boat race where residents fold, launch and nudge boats and a human can tap 'Join as Cox' to give one timed push.
- [ ] (town wish, 2026-09-26) Add an 'Echo Ping' action to a new Echo Board on the street: a resident can ping a two-word phrase that nearby residents must repeat once, with each repeat allowed to swap or muffle one word.
- [ ] (town wish, 2026-09-26) Add a 'Start Whisperline' action to the new Listening Post (street) that begins a 60‑second resident-run one-word relay: residents pass a single-word whisper token along adjacent spots, each passer may muffle one vowel, 
