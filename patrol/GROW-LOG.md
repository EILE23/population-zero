# Square growth log

- 2026-09-21: Players can sit — `C` with empty hands near a bench/sofa/bed/swing toggles sitting (pose `sit`, movement locked, any move stands you up); broadcast over the room so other players see it. Residents already sat/lay at these spots via their job routine's `act:'sit'`.
- 2026-09-21: Eating — `C` with a sandwich or coffee near a café, table or bench eats it (item consumed, brief freeze, `sit` pose) instead of dropping/delivering it. NPCs holding food (`FOOD` in `goose.ts`) now show the eating pose as soon as they stop anywhere in their routine, not only at spots already tagged `act:'eat'`.
- 2026-09-22: Exercise — new pull-up bar and bench press in the park; `C` with empty hands near either exercises for ~2.4s (`pushup`/`pullup` pose, movement locked, sent over the socket). Joggers now route through both spots as part of their job routine, so they exercise on their own the same way.
- 2026-09-22: Watching TV and reading — the TV's spot activity changed from `stand` to a new `watch` (still pose, arms folded); `C` with empty hands near a TV or bookshelf toggles it (movement locked, any move stands you back up), with a "watching"/"reading" line above the head. House owners already stop at the TV and bookshelf on their routine, so they get the same pose and label automatically.
- 2026-09-22: Watering — `C` with empty hands near a garden waters it (`water` pose with drops, ~1.4s, movement locked, sent over the socket). Gardener residents already stopped at `garden`/`garden2` on their routine with `act:'water'`, so they already watered on their own; added task kind `water` (`tasksFor`) and quest kind `water` (`questFor`) so a day's list or a resident's ask can send players to a named bed.
- 2026-09-22: Bins — `C` near a bin with an item consumes it for good (no loose item created, stack shrinks and syncs to others via the normal `pos` update, same pattern as eating). Bin spots (`bin1`-`bin4`) now use the existing `sweep` act instead of `stand`, so the sweeper job (the only job that routes through bins) already empties them with the sweep pose on its own routine. Added task kind `bin` (`tasksFor`) and quest kind `bin` (`questFor`, "take this X and bin it").
- 2026-09-22: Tripping while chasing — a chasing resident rolls a small per-second chance to trip (higher for `retired` and while carrying something): new `trip` pose (`stickman.ts`), drops the carried item, says a flat line, the chase clock loses two seconds, then resumes chasing or gives up. The roll runs before the catch check, so tripping within reach lets the player go, and the escape still counts toward the `chased` task without any extra code. Players running empty-handed at full speed over a loose item on the ground trip the same way (`b.tripped`). Deferred the pond/fountain-edge and broken-prop triggers to a new backlog chain.
- 2026-09-22: Tripping while chasing (chasing-resident half) — a chasing resident now has a per-second chance to trip (retired jobs and anyone carrying something more likely), a new `trip` pose (`stickman.ts`), `Npc.mode = 'trip'` for 1.2s, drops what they carry, says a flat line, and the chase deadline loses two seconds; tripping within reach ends the chase and completes the `chased` task immediately. Broadcast over the room the same way `down` is. Split the pond/fountain-edge, broken-prop and loose-item variants (and the player's own trip-over-a-loose-item-while-running half) into "Grows from Tripping" in the backlog — they touch every moving figure's code, not just the chase block, so they are their own run.
- 2026-09-22: Fishing spots — a `rack` prop by the little pond and the duck pond; `C` empty-handed near it takes a rod (new `rod` item); `C` while holding it near any water casts (`fish` pose, already in `stickman.ts`, now wired up) for a random 4–12s, then catches a name from the 250-item pond list (reused, not rebuilt) and adds a generic `fish` item to the stack, so every existing carry/throw/dunk/bin path works on it for free. Retired, jogger and priest now route through the rod racks on their job spots, so they fish on their own with the same pose, no resident-specific code. Task `fish1`; no new quest kind needed, since `fish` joining the item table makes it a normal `fetch`-quest item already. Also fixed a real `tsc` failure along the way: the two "Tripping while chasing" runs above (lines 9-10) each added their own `if (pose === 'trip')` block in `stickman.ts` and the merge kept both — the second was dead code and unreachable; removed it.
- 2026-09-22: Fishing — `C` with empty hands at a pond takes a rod (new `ItemKey` `rod`; no separate rack prop, the water's edge is the rack); `C` again while holding it casts (`b.fishing`, 4–12s wait, `fish` pose, movement locked); on the bite, a name from `pond.ts`'s `ITEM_LIST` (uniform pick, not the standalone Pond game's zone/rod/bait weighting) shows as a toast and a loose `fish` item drops at your feet through the existing `drop` path, already synced to others (no new `ev` kind). `retired`/`priest` now route through the ponds; `jogger` (and, as a side effect, `dogwalker`, already the only job with a valid route to `pond2`) already did, so they fish there too via the existing act→pose pipeline. Added task kind `fish` and quest kind `pond` ("bring me something from the pond"). Also fixed a merge bug: two duplicate `pose === 'trip'` blocks in `stickman.ts` (from the two tripping runs above) made the second unreachable and failed `tsc`; removed the dead one.
- 2026-09-22: Animals — ducks (first slice; pigeons and a cat deferred to "Grows from Animals"): 3–5 seeded ducks paddle in slow loops on each pond (`pond`, `pond2`), drawn with a new `critter(ctx, kind, x, y, s, pose, t)` in `stickman.ts` (poses `paddle`/`flap`/`feed`). The swim loop is a pure function of seed + time so every screen agrees; the scatter reaction (a player or resident running in, or a thrown item landing nearby) is judged locally per screen the same way a punched resident's reaction already is — the room only relays a fixed set of `ev` kinds, so this deliberately isn't a new one. `C` with a sandwich or coffee near a pond feeds the ducks instead of eating/dropping (new `feed` task kind, `PONDS` list in `goose.ts`); the `kid` job, which already carries a sandwich past `pond2` on its routine, now feeds them there too with a new `feed` `FigPose` (crouch and toss, falling crumbs) instead of just eating.
- 2026-09-22: Animals (ducks only; pigeons and the cat split into a new "Grows from Animals" chain) — 3–5 ducks per pond, seeded per spot, paddling a deterministic loop (seed + room clock) drawn with a new `critter()` in `stickman.ts` since they aren't stick figures. `C` with a sandwich or coffee near a pond feeds them (`feed` pose, item consumed) instead of eating; residents holding food who stop near a pond now show the same `feed` pose instead of `eat` (reverse rule, one-line change, no resident-specific code). Ducks scatter when a runner gets close and drift back after, and converge slowly on anyone currently feeding — computed independently by every client (including spectators) from state that was already synced (position, pose, moving), since the room can't carry a new `ev` kind without an edit to the off-limits `climb-room.js`. Task `feed1` ("Feed the ducks"); no new quest kind (stateless action, like fish). Not done: pigeons, the cat, and scattering from a thrown item landing nearby (only running triggers it today) — see "Grows from Animals" in the backlog.
- 2026-09-22: Wearing — first slice, hat and glasses (umbrella/phone/newspaper/broom-tidy/basket-capacity deferred to a new "Grows from Wearing" chain): `C` on yourself with nothing around (no bin, water, or bench/café/table in range) toggles the top item worn if it's a hat or glasses (new `WEARABLE` list in `goose.ts`) — drawn on the head/face instead of the floating carry-stack icon, tracked in a `b.wearing` set and sent over the wire as a `!` suffix on the item in `pos.s` (e.g. `hat!`), which the room already relays untouched since it just stores the string. `C` again in the open takes it off (stays carried, doesn't drop). Busker/mayor (hat) and retired (glasses) already carry these as their job item, so the same head-position draw code now applies to them too — the reverse rule needed no per-resident logic. Being knocked down already drops your whole stack, so worn items already fell off before this landed. Added task kind/key `wear:<item>` and quest kind `wear` ("wants to see you in a hat"). Also fixed a real `tsc` failure found along the way: the ducks entries above (lines 13-14) came from two parallel branches building the same feature, and the merge kept a second, dead duck system alongside the working one — a `useRef` Map (`ducks`, never populated) shadowed by a same-named local array, plus a `'scatter'` pose that was never a valid `CritterPose`. Removed the dead block; the ducks you can see were already running on the other one.
- 2026-09-22: Booth — first prop off the "every prop does something" list (`SquareGame.tsx`): `C` with empty hands near a phone booth dials (new `b.calling` timer, ~2.2s, movement locked), showing the existing `phone` pose and "on the phone" label — both already existed because residents (`postie`/`office`) already stop at booths with `act:'phone'` on their routine, so the reverse rule needed no new code. On hangup a flat line (`CALL_LINES`) comes back as a toast and a head bubble. Added task kind `call` (`tasksFor`, `goose.ts`); no quest kind, since no item changes hands (same reasoning as `fish1`). Also found, while reading the ducks code to check what was already wired up, that a backlog line claiming ducks don't react to thrown items was stale — that check has been in `SquareGame.tsx` since the commit that added ducks; corrected the backlog rather than re-implementing it.
- 2026-09-22: Lamp — second prop off the "every prop does something" list: added one named, interactive lamp per outdoor map that has one (`lamp1` in the square, `lamp2` on Market street — the many lamps per map stay decorative filler, unchanged, same split `tree1`/`tree2` already used against filler trees). `C` with empty hands near one toggles leaning (new `b.leaning`, movement-locked until you move, same shape as `b.still` for TV/shelf) and draws a new `lean` `FigPose` (fills a line from the motion library backlog too). `busker` now has both lamps in its routine so residents lean on their own — had to add `lamp` to the `EXACT` stand-position list (`SquareGame.tsx`) so a leaning resident stands exactly on the post instead of scattering up to 300px away like a wander stop. Kicking (or throwing something at) a lamp already chipped/broke it through the existing generic `BREAKABLE` prop-damage path; it now also flickers the light for ~0.5s — piggybacked on the existing `break` ev (added a `kind` field) so every client sees the same lamp flicker, not just the one who kicked it, rather than inventing a new synced signal. Added task kind `lean` (`tasksFor`); no quest kind, same reasoning as `call1` (no item changes hands).
- 2026-09-22: Tree — third prop off the "every prop does something" list: `C` with empty hands near a named tree (`tree1`/`tree2` in the square — the check is by `kind === 'tree'`, so `ptree1`/`ptree2` in the park work too, decorative filler trees don't since they're drawn props, not `Spot`s) shakes it for ~1s (new `b.shaking` timer, same movement-locked shape as calling/fishing) and draws a new `shake` `FigPose` (both arms up gripping a branch, leaves falling, fills another line in the motion library). On finish, usually "just leaves"; 30% of the time a new `apple` item drops at the base (`ItemKey`, added to `FOOD` too so it's eaten/fed to ducks the same way a sandwich is, for free). `kid` — no job routed through `tree1`/`tree2` before — now stops at both on its routine, so kids shake trees for apples on their own with the same pose; the park's `ptree1`/`ptree2` stay `painter`'s (painting, not shaking), players can still shake those, residents there just don't. Added task kind `shake` (`tasksFor`); no quest kind, same reasoning as `call1`/`lean1` (no item reliably changes hands). Backlog: split "Grows from Tree" off for an apple-lands-on-someone variant and a grocer-collects-apples chain; added the affinity-seed stepping stone that Flowers and Relationships were both blocked on, plus a rowboat idea, to the two thinnest axes (relationships-and-children, vehicles).
- 2026-09-22: The affinity seed — `affinityOf(a, b)` (`goose.ts`): sorts the two ids so it's order-independent, folds `hash('affinity:lo:hi')` into 0..1, no state, no UI. `pairsFancying(day, roster)` walks every pair in the day's roster and keeps the ones at or above the top ~15% (`>= 0.85`) as "fancies" for that day, sorted `[a, b]`, so Flowers and Relationships can both read the same seeded pairs instead of each inventing their own threshold. Deliberately just the function — nothing calls it yet. No new poses, no browser dependency, engine stays pure. Backlog: opened "Grows from Affinity" with a small passing-glance chain item (also the day's build-on-the-thin-axis pick, since relationships-and-children was already one of the two thinnest), and added a police car to vehicles, the other thinnest axis — job-only to drive on call, but players can still joyride it parked like the tractor, so it doesn't become a resident-only power.

- 2026-09-22: Booth calls sometimes ring a real ask (`SquareGame.tsx`) — on hangup, 1-in-6 odds now pick a resident from today's roster who doesn't already have a live quest and hands back their `questOf(who)` ask (reusing the exact `quests`/`questFor`/`finishQuest` machinery the in-person `E` talk already uses) instead of a stock `CALL_LINES` wrong-number line; shown as a toast and head bubble in the resident's voice. The other 5-in-6 calls, and calls when no resident is eligible, keep the old flat line. No new quest kind, no new pose, no engine files touched. Backlog: marked the item done; added a jobs-axis item (a `repairer` job that actually does the "fix" work other items already assume exists) and a quests-axis item that also deepens today's booth work (a callback that re-rings an unanswered quest instead of rolling the dice again).
- 2026-09-23: A repairer job, made real — added `repairer` (`world.ts` `JOBS`: item `wrench`, wanders `square`/`street`/`park`) and put it in `SquareGame.tsx`'s `REPAIRERS` list, which already made gardener/sweeper/grocer/courier walk to the nearest currently-broken prop and fix it with the existing `fix` pose/ev — the routine half was already generic, it just had no dedicated job. New: players with empty hands can `C` at a broken prop a repairer is actively working on (`n.mode === 'repair' && n.target === key`) to finish it immediately (`emit({k:'fix'})`, `broken.current.delete`); the repairer notices next tick (`ok` check fails once `brokeAt` is gone) and returns cleanly instead of fixing an already-fixed prop. New `b.fixing` timer plays the reused `fix` pose for both, synced the same way leaning/shaking already are. Added task kind/key `fix1` (`tasksFor`); no quest kind (no item changes hands, same reasoning as `call1`/`lean1`/`shake1`). No new pose needed — `fix` already existed for both sides. Backlog: marked done; added a jobs-axis item (a tour-guide job) and a weather-and-time-axis item (lamps lit by the clock, ahead of full Night) to the two thinnest axes, plus a "Grows from Repairer" chain (a busier square calls in a second fixer) that deepens today's work.
- 2026-09-23: Fishing things out — first slice of the Lost and found system the town asked for (16 wishes merged into one build order, restored to the top of the backlog after the owner flagged the repeat): a dunked item (`fountain`/`pond2`) aged past two minutes (client-local `dunkedAt` timer, `SquareGame.tsx`) gets assigned to a nearby idle `gardener`/`sweeper` (`RAKERS`, wide radius, checked first) or, at low odds, any other passing resident — new NPC mode `rake`, same shape as `repair`'s walk-then-wait-then-`ok`-check pattern, plays a new `rake` `FigPose` (`stickman.ts`, ~1.2s) at the water, then `pick`s the dunked item and `drop`s it back as a normal loose item on the rim, flagged `wet` (client-local, unsynced — drip marks for a minute, same idea as `flicker`). Added a static leaning `rake` prop decoration at each water spot. Players with empty hands can `C` a raking resident to finish it instantly, mirroring the repairer assist from the previous run. Added task kind `rake1` (`tasksFor`); no quest kind (no item reliably changes hands, same reasoning as `call1`/`lean1`/`fix1`). Backlog: marked done, left the other five Lost-and-found slices for future runs; added a jobs-axis item (a lifeguard, stationed at the water and prioritised in `RAKERS`) and a quests-axis item (a `reunite` kind, blocked on the lost-and-found board) to the two thinnest axes; opened a "Grows from Fishing-things-out" chain (a resident's one-off "still wet" line, ducks drifting toward a fresh rake-drop) that deepens today's work.
- 2026-09-23: Decoy — first slice of "Keeping it", the town's second designed system (eleven wishes about keeping a held or just-recovered item from a grabby human, scattered across three sections including the money items and the Wishes-from-the-town section, merged into one build order under a new heading near the top of the backlog, same treatment the sixteen lost-and-found wishes got): a resident about to lose their item to a steal (`n.mode === 'routine'`, in range, 30% roll) keeps it and drops a new `decoy` `ItemKey` (a coin, drawn in `item()`, `SquareGame.tsx`) at the human's feet instead, with a flat line (new `DECOY_LINES` pool); since a loose item on the ground is already checked before an npc-steal in the existing `C` handler, the next press takes the decoy for free — no change needed to the steal logic itself beyond the new branch that creates it. An unpicked decoy cleans itself up after 8s (client-local `decoyAt` timer, checked once per frame, same "owning client removes it" shape `wet`/ducks-stealing-fish already use). Picking one up says "Just a decoy." and completes a new task `decoy1` (`tasksFor`, `goose.ts`); no quest kind (nothing a resident asks for). Purely reactive — no player-side mirror needed (the reactive-ability carve-out in the reverse rule) and no new pose (the resident's own motion doesn't change, only what it says and what appears on the ground). Backlog: marked done, left the other ten Keeping-it slices for future runs; added a relationships-and-children item (two residents who fancy each other landing on the same bench together) and a weather-and-time item (dawn/dusk light, the other half of the purely-visual work Lamps-on-a-clock started) to the two thinnest axes; opened a "Grows from Decoy" chain (a resident who's been decoyed three times remembers, decoys cleaning up early when the human walks away) that deepens today's work.
- 2026-09-23: Interception — first slice of "Lost and found"'s remaining four (a resident hit by a player's thrown item now rolls 30% to catch it instead of going down): new `catch` `FigPose` (arms up, 0.3s) and two new NPC modes in `SquareGame.tsx` — `catch` (plays the pose, holds for 300ms) then `deliver` (walks the caught item, tracked in a new `Npc.caught` field kept separate from the resident's own job item/stack, to the spot the throw originated from — captured as `ox`/`od` on the thrown object at `throwTop()` time — drops it there via the existing `drop()` helper, then returns to routine). `caught` is broadcast as a base field on every `npc` ev (alongside `item`/`st`) so remote screens render the carried item too, not just the owning screen. Purely reactive, no player-side mirror needed (reverse rule's carve-out — same reasoning as Decoy); added task `catch1` (`tasksFor`); no quest kind (no item changes hands with a resident). Backlog: marked done, left the other four Lost-and-found slices; added two vehicle items (a delivery van for the courier, a wheelbarrow at the garden/building site) and two animal items (squirrels around the trees, bees over the garden beds) to the two thinnest axes; opened a "Grows from Interception" chain (a tempered resident throws the catch straight back instead of walking it home, a caught-for-you resident remembering once reputation lands) that deepens today's work.
- 2026-09-23: Holding on — first slice of the four remaining "Lost and found" items: a resident about to be robbed (`n.mode === 'routine'`, in range) now sometimes plants their feet instead of losing the item — new mode `brace` (~8s, `SquareGame.tsx`), new `brace` `FigPose` (feet planted wide, arms clutched to chest, `stickman.ts` — the pose the motion library's Reactions list and the "Brace-with" chain item under Keeping it were both waiting on), a protest line (`BRACE_LINES`). Roll order at the existing steal site: decoy 30% (unchanged) → brace ~30% of the remainder → plain steal otherwise. A second `C` during the 8s does nothing but a local toast ("Still holding on."); knocking the resident down (`hit()`, unchanged — it already only excludes `mode === 'down'`) still drops everything, so "get it by knocking them down first" needed no new code. Added task `brace1` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands, same reasoning as `decoy1`/`catch1`). Purely reactive — no player-side mirror needed (reverse rule's carve-out, same as Decoy/Catch/Interception). Backlog: marked done, left the other three Lost-and-found slices (board, alley stall, relay/shelving); added a Climb-axis item (wind gusts forcing the new `brace` pose) and a places-axis item (the `marketalley` map the alley-stall slice already assumes exists) to the two thinnest axes — the two untouched by today's three earlier runs; opened a "Grows from Holding-on" chain (skip the re-roll and let a steal through once a human outlasts a brace) that deepens today's work.
- 2026-09-24: The lost-and-found board — first of the three remaining Lost and found slices: a `board` prop in the square (`world.ts`); when a resident's opportunistic fetch (`SquareGame.tsx`) picks up a stray item that isn't theirs, they now detour to it instead of keeping it forever — new `board` NPC mode (reuses the `catch`/`deliver` carry fields, `caught`/`retX`/`retD`) walks it to the crate and drops it with a pinned note (`Loose.note`, e.g. "sandwich — courier's", or "no name on it" when the item's origin resident is unknown) — capped at five, oldest evicted first (`Map` iteration order does the ordering for free). Anyone, human or resident, takes an item with the existing plain `C` pickup (loose items aren't `dunked`, so no new code was needed there); the owner's resident notices it on their own routine (same shape as the wet-item raking check) and reclaims it with a flat line. Added task kind `crate1` (`tasksFor`); no quest kind yet (see the now-unblocked `reunite` item below). No new pose needed (walk-and-drop, same as fetch/deliver), so per the motion-library rule added one anyway: `yawn` (`stickman.ts`) — a purely time-seeded idle fidget (`(t + seed) % 22 < 1.2`) for both the player (standing still) and any resident whose act is plain `stand`, no synced state since every client computes the same phase. Backlog: marked the board item done, unblocked the `reunite` quest item (it was waiting on this), added a jobs-axis item (a florist job for the still-unbuilt "Flowers as courting") and a quests-axis item (a `no-hands` quest — the first one that rewards not touching anything) to the two axes most overdue by rotation; opened a "Grows from Board" chain (notes that outlive the item, closer to the original ask) that deepens today's work.
- 2026-09-24: Relay and shelving — second of the three remaining Lost and found slices: a resident whose opportunistic fetch (`SquareGame.tsx`) picks up a stray item belonging to a resident of `baker`/`postie`/`cop` (new `JOB_BUILDING` map, `world.ts`: those three jobs to `bakery`/`post`/`station`) now walks it to that building instead of the board's crate — new mode `shelve` (reuses the `caught`/`retX`/`retD`/`boardNote`/`boardOrigin` fields `board` already carries), new `shelve` `FigPose` (`stickman.ts`, one arm reaching up to place something) held for ~0.7s on arrival, same wait-then-place shape `repair`/`rake` already use. The item drops as a normal loose item flagged `Loose.shelf` (parallel to `board`) and is drawn on the building itself (`bakery`/`post`/`station` cases in `prop()`) instead of the floor. Anyone takes it with the plain `C` pickup (mirrors the board's `l.board` branch); the owner's resident notices it on their own routine and reclaims it (the existing board-reclaim check now also matches `l.shelf`). Falls back to the board (or, off the square, just carrying it) when the owner's job has no building or none exists on that map. Added task kind `shelf` (`tasksFor`); no quest kind (no item changes hands with a resident, same reasoning as `call1`/`crate1`). Backlog: marked done, left the last Lost-and-found slice (the found stall in the alley, still blocked on the market-alley map); added a Climb-axis item (a dropped item mailed to the square between visits, since the two games run on separate rooms) and a places-axis item (a clinic on Market street, giving the generic "clinic" building kind an actual spot and a nurse who shortens `down` time) to the two thinnest tracked axes; opened a "Grows from Relay and shelving" chain (a cap on the shelf mirroring the crate's, the building's own resident noticing a taken item) that deepens today's work.
- 2026-09-23: Dust-off — first slice of "Keeping it"'s remaining ten: when a knocked-down resident's `down` timer expires and they aren't angry enough to chase, they now spend ~2s in a new npc mode `dust` (`SquareGame.tsx`, same walk-through-mode shape as `brace`/`catch`) brushing themselves off — new `dust` `FigPose` (`stickman.ts`: arms alternately wipe the torso, small dust particles rise, same particle-loop convention `water`/`shake`/`feed` already use). The `C` pickup site now checks, before anything else, whether the nearest loose item on the ground belongs to (`l.from`) a resident currently in `dust` mode who hasn't yet given their one miss (`Npc.dustMissed`) — if so the grab fails once with "Can't see straight for the dust." and the flag flips so the next try succeeds normally, the same one-shot-miss shape Decoy uses but triggered by getting up instead of by a notice. Purely reactive, no player-side mirror needed (reverse rule's carve-out, same as Decoy/Catch/Brace/Shelve). Added task kind `dust1` (`tasksFor`, `goose.ts`). Backlog: marked done, left the other ten Keeping-it slices; added a relationships-and-children item (a rivalry glance, the unhappy mirror of the existing passing-glance) and a vehicles item (a skateboard by the playground, the first vehicle inside the park and the first one a kid resident actually rides) to the two thinnest axes; opened a "Grows from Dust-off" chain (giving a trip the same one-time grace a knockdown now gets) that deepens today's work.
- 2026-09-23: Slide-under — first of "Keeping it"'s remaining nine: a resident whose held item is dropped by a knockdown (the punch/kick knockdown in `hit()`, the thrown-item knockdown in the throw-hit block, both `SquareGame.tsx`) now checks `slideNear` (a bench, sofa, bed, swing or water spot's edge within 60px) and, if one is there, tucks the item instead of leaving it in the open — the `drop` call carries a new `hide` flag, `apply` turns that into a client-local `hideAt` deadline (7s, same unsynced-timer shape as `wet`), the render loop draws a tucked item at low alpha and a lower offset, and the `C`-pickup site checks `hideAt` first ("Tucked out of sight." instead of finding it as the nearest loose item, no fallback to stealing from the resident that drop). Purely reactive, no player-side mirror needed (reverse rule's carve-out, same as Decoy/Brace/Catch/Shelve/Dust-off). No new pose needed (the knockdown motion itself doesn't change), so per the motion-library rule added one anyway: `stretch` (`stickman.ts`) — a second seeded idle fidget alongside `yawn`, wired into the same shared time-only check both the player and any standing resident already use. Added task kind `slide1` (`tasksFor`, `goose.ts`). Backlog: marked done, left the other nine Keeping-it slices; added a weather-and-time item (residents thinning out at night, the harder half both Lamps-on-a-clock and Dawn-and-dusk-light deliberately deferred) and an animals item (fish jumping at dusk, the first animal reaction tied to the clock instead of a prop or a player) to the two axes tied for fewest open items and most overdue by rotation; opened a "Grows from Slide-under" chain (the trip-while-chasing drop still doesn't get the same tuck) that deepens today's work.
- 2026-09-24: Stow-in-mug — first of "Keeping it"'s remaining eight (also closes a duplicate town wish asking for the same thing): at the existing steal roll (`SquareGame.tsx`, where a routine-mode resident about to lose an item already checks decoy then brace), a resident holding a single item within ~140px of a `cafe` spot now gets a first shot at a third option — 45% odds to stow it in their cup (new `Npc.stowUntil`, ~10s) instead of losing it, with a flat line (new `STOW_LINES`). While stowed, a human's `C` on them just says "Still in the cup." (checked before decoy/brace so it always wins during the window) and the figure draws a plain `cup` icon in place of the real item (`item()` call swaps `n.item` for `'cup'` while `now < n.stowUntil`) so it reads as an ordinary held coffee, not a tell. Synced to other screens with a new `stow` field on the `npc` ev (absolute room-clock deadline, same conversion `until` already uses) since the resident's `mode` never leaves `routine` for this one. Purely reactive, no player-side mirror needed (reverse rule's carve-out, same as Decoy/Brace/Catch/Dust-off/Slide-under). No new pose needed (the resident's own motion doesn't change), so per the motion-library rule added one anyway: `look` (`stickman.ts`) — a third seeded idle fidget alongside `yawn`/`stretch`, same shared time-only slot mechanic, both player and standing residents. Added task kind `stow1` (`tasksFor`, `goose.ts`). Backlog: marked both the "Keeping it" item and the duplicate town wish done, left the other seven Keeping-it slices; added a jobs item (a park ranger, the simplest jobs pick left — no new economy, no new prop) and a quests item (a `shadow` quest kind, the first where the resident you help and the resident you watch differ) to the two axes next in the rotation (jobs+quests, Climb+places, relationships+vehicles, weather-time+animals, repeating); opened a "Grows from Stow-in-mug" chain (the resident-to-resident take Pass-along still needs) that deepens today's work.
- 2026-09-23: Weight-down — "Keeping it"'s first pick-up delay (Umbrella-cover, next in build order, is still blocked on the pocket-umbrella system the owner said must land with or after Weather, so skipped to the next unblocked slice): at the existing steal-roll site (`SquareGame.tsx`, decoy → brace → …), a resident who fails to brace on a 40%-of-the-remainder roll now instead drops what they were carrying with a brick on it (new `Loose.weight` flag, plain `drop()` call with a new trailing `weight` arg) instead of losing it clean. A human can still take it, but standing at it starts a channel (new `b.weighTarget`/`b.weighing`) that finishes the pickup after 1.5s only if they don't move — a direction key press cancels the channel the same "not this frame" way `sitting`/`still`/`leaning` already do, so leaving and coming back just restarts the wait. Drawn as a small brick rect over the item in the render loop. Purely reactive, no player-side mirror needed (reverse rule's carve-out, same as Decoy/Brace/Stow-in-mug). No new pose needed (the resident's own motion doesn't change when it drops something), so per the motion-library rule added one anyway: `check` (`stickman.ts`) — a fourth seeded idle fidget (checking a phone) alongside `yawn`/`stretch`/`look`, same shared 22s clock slot, both player and standing residents. Added task kind `weight` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands with a resident, same reasoning as `decoy1`/`brace1`). Backlog: marked done, left the other six Keeping-it slices; added a relationships-and-children item (a returned glance at a shared exit, the fourth small step ahead of full Relationships) and a vehicles item (a moped for the postie, another job waiting on the still-unbuilt `drive` pose) to the two axes now thinnest by count; opened a "Grows from Weight-down" chain (the brick itself sticking around as a plain loose item instead of vanishing with the pickup) that deepens today's work.
- 2026-09-23: Tether — second slice of "Keeping it"'s remaining seven: a resident who recovers their own item through an opportunistic fetch (the `own` branch in `SquareGame.tsx`'s fetch resolution) is now tagged `tether` — the first punch/kick that knocks them down keeps the item in hand instead of dropping it (a new `TETHER_LINES` flat line replaces the usual shoved one), and the item now draws even while they're down. A second hit within the same down-cycle (through the `dust` window that already follows `down`) finally dislodges it exactly like an untethered knockdown always has, no new code needed for that half. If the second hit never lands and the resident dusts off and returns to routine, the spent one-time protection re-arms rather than staying used up forever, so a much later, unrelated knockdown doesn't wrongly count as "the second hit." Synced with new `tether`/`tm` fields on the `npc` ev. Purely reactive, no player-side mirror needed (reverse rule's carve-out, same as Decoy/Brace/Stow/Weight-down). No new pose needed (the knockdown motion itself doesn't change). Added task kind `tether1` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands with a resident, same reasoning as `decoy1`/`brace1`/`weight1`). Backlog: marked done, left the other six Keeping-it slices; added a Climb-axis item (a personal-best cheer, the small standalone half of the still-unbuilt shared-plaque item) and a places-axis item (a footbridge at the pond, the first place item that's terrain rather than a building or a whole map) to the two thinnest axes; opened a "Grows from Tether" chain (extending the same protection to the thrown-item knockdown path, which today's item deliberately left out) that deepens today's work.
- 2026-09-23: Busking — merged three scattered town wishes about coins (one under "Residents grow too", two under "Wishes from the town") into a new "Coins in play" build order, the same treatment Lost-and-found and Keeping-it got, and built the first slice: a `stage` prop (`world.ts`, one in the square) where `C` with empty hands busks for 6s (new `busk` `Activity`/`FigPose`, `stickman.ts` — strums, a note drifts up). On finish, every nearby `routine`-mode resident (standing in for "passersby") rolls a 20% chance to tip; a successful tip completes the new daily task `busk1` for coins through the normal `tasksFor`/`/api/goose` path (repeat busking after that is flavor-only, same as every other mini-interaction — coins here have only ever been one-time daily-task awards, never a live balance). `busker`'s routine now includes the stage, so residents busk there on their own with the same pose — the reverse rule needed no extra code once the spot existed. Left the other two wishes ("buy a hot drink" spending a coin, and the coin-kiosk locking an item) explicitly blocked in the backlog: both need a wallet-*spend* endpoint, and Square's only coin-mutating route (`/api/goose`) only ever awards from a fixed daily list — `route.ts` files are outside this job's edit list, so a real spend needs new server work out of scope here; a client-only fake decrement was rejected as a half interaction that would drift from the server total on reload. Also added "Sell a found trinket" as an earn-only third slice (same one-time-task shape as Busking, no spend needed). Backlog: two of four axes untouched by the last three runs (jobs, weather-and-time) got one item each — a town crier job (reuses `stand`/`read`, no new pose) and morning mist (a fourth purely-visual weather piece keyed on `hour()`, alongside Lamps-on-a-clock/Dawn-and-dusk/Residents-thin-out).
- 2026-09-24: polish — this is run 32, the fourth since the last polish (none had happened yet), so no feature this run. Walked the core loops from GROW.md against the holiday rule the owner wrote in yesterday (max two reactive variations per action, plain outcome ≥40%). Found "take from a resident" still broken: the fc998be fix ("가짜 동전 분기 제거, 버티기 70%·3초, 컵에 숨기기 15%") dropped decoy but left the steal-roll (`SquareGame.tsx`) three deep — stow-in-mug 15%, brace 70%, weight-down 40% of what's left — which blocks ~85-98% of grabs (plain steal 2-15%), and brace alone at 0.7 already violates the 40%-floor example GROW.md gives verbatim ("brace 0.7 → plain 0.3 is too low"). Fixed: merged stow-in-mug and weight-down into one "hold" roll (mutually exclusive by café-proximity — stow if near a café, weight-down otherwise — so it's one roll, not two) and lowered both: brace 0.3, hold 0.3 of the remainder → blocked 0.51, plain 0.49 (arithmetic in the code comment, `SquareGame.tsx`). This surfaced a second bug: the decoy mechanic was fully dead — fc998be removed the only code that ever created a `decoy` item, but `DECOY_LINES`, the `decoyAt` timer and its cleanup, the pickup branch, the `decoy` `item()` draw case, and the `decoy` `ItemKey`/`NOT_CARRIED` entry all stayed behind, and the `decoy1` daily task (`tasksFor`, `goose.ts`) had been permanently uncompletable since that fix landed. Removed all of it. Also found the `rack` `PropKind` (type, `EXACT` list, `prop()` draw case) unreachable since the 2026-09-22 fishing redesign moved the rod-take to the water's edge and dropped the standalone rack spot from `world.ts` — removed it too. `tsc` and `schema-parity.test.mjs` both pass; `app-parity.test.mjs` unaffected (Square isn't one of its pinned files). Backlog: fixed the stale "three-way roll (decoy/brace/steal)" line under "Grows from Holding-on" to match the new two-roll shape and deleted the now-moot "Grows from Decoy" chain (both its items depended on a mechanic that no longer exists); added an animals-axis item (gulls swooping unattended café food, the first animal reaction to food left alone rather than proximity or a prop hit) and a quests-axis item (a "brace-check" quest tied to today's fix) to the two axes least touched by the last three runs, plus one more "Grows from Holding-on" item (a job noticing the hold trick, deferred to reputation) that deepens today's work.
- 2026-09-23: Sell a found trinket — first slice of "Coins in play"'s second wish (after Busking): `C` with a non-wearable, non-food item at a bin or booth sells it, once a day, for a flat toast ("Sold. Not much, but something.") and completes a new daily task `sell1` (`tasksFor`, `goose.ts`) — same one-time-per-day-award shape as every other mini-interaction (`/api/goose` still never subtracts or sets an arbitrary balance). The check runs before the existing bin-disposal branch (`SquareGame.tsx`) and only fires while `sell1` is still undone, so once it lands the same `C` at a bin goes back to plain binning — no core action stops working, it just gains a one-time alternate outcome. Reverse rule: a resident's own opportunistic fetch (the board/shelve pickup that already routes unclaimed stray items) now rolls 30% for an item with no known owner (`tgL.from === null`, not food/wearable) to be sold on the spot with a flat line instead of pinned to the board — the resident's own version of cashing in a find nobody claimed, no coin state needed since Busking already established residents don't carry a live balance. No new pose needed for the sale itself (same reasoning as `bin`/`call1`), so per the motion-library rule added one anyway: `shrug` (`stickman.ts`) — a fifth seeded idle fidget alongside `yawn`/`stretch`/`look`/`check`, same shared 22s clock slot (`t + seed`), wired for both the player and any standing resident. `tsc` and `schema-parity.test.mjs` both pass. Backlog: marked done, left the two blocked Coins-in-play wishes (both need a real wallet-spend endpoint, out of scope); added a places-axis item (a bus stop on Market street, the covered spot Weather's rain plan already names) and a Climb-axis item (idle fidgets on rest floors — Climb never calls the poses Square's engine already knows) to the two thinnest axes; opened a "Grows from Sell" chain (a resident of the matching job noticing what you sold) that deepens today's work.
- 2026-09-23: Brace-with — first pick of "Keeping it"'s remaining six after re-verifying three earlier ones against source and finding them genuinely blocked (not just stale text): Umbrella-cover needs an "open umbrella" state that doesn't exist yet (`umbrella` is an `ItemKey`/`painter`'s job item, but no `umbr`/open pose or logic anywhere — that's a separate, still-unbuilt item that its own text says must land with or after Weather); Pass-along assumes a resident being chased by a human, but today only residents ever chase (Temperaments' flee hasn't shipped, so nothing is ever "about to be caught"); Shout would be a third reactive variation on the steal-roll site, over the 2026-09-23 holiday rule's cap of two. Also confirmed "The found stall in the alley" (Lost and found) stays blocked: `marketalley` doesn't exist in `world.ts`, and there's still no wallet-spend endpoint. All four left `- [ ]` with dated "checked, skipped" notes explaining exactly what's missing. Built: two `routine`-mode residents who are each other's nearest neighbor within 60px link arms for a deterministic ~14s-window-out-of-14s (35% odds, seed+clock hash — `linkedNpc()` in `SquareGame.tsx`, no synced state, every screen agrees, same trick the affinity glances use), drawn with a new `link` `FigPose` (`stickman.ts`) on both figures since the check is mutual. While linked: a punch/kick against either is absorbed once per window (new `Npc.linkAbsorbBucket`, a flat `LINK_LINES` line replaces the knockdown, `hit()`) instead of going down; a human's `C` on a loose item within 70px of a linked pair now takes the existing Weight-down 1.5s stand-still channel even with no brick (`linkedGuard()`, same `b.weighTarget` code, different say-line and task-key so it doesn't get credited as a brick pickup). Added task kind `bracewith1` (`tasksFor`, `goose.ts`); no quest kind (purely reactive, same carve-out as Brace/Stow/Weight-down/Tether). `tsc` and `schema-parity.test.mjs` both pass. Backlog: marked done; counted open items per axis by hand (tag-only counts undercount badly — e.g. "interactions with props" and "economy" balloon past 12 once every untagged `Grows from X` and `Money`/`Residents grow too` item is folded in) and found motions clearly thinnest at 5 open lines (just the five pose-library category bullets) with places/relationships tied at 7 just above it; added one item to motions (a `wave` pose for two unrelated residents passing, next off the Reactions list) and one to relationships-and-children (walking part of a shared route together, since places was the axis the immediately-preceding Sell run already touched) — the two thinnest; opened a "Grows from Brace-with" chain (skipping the re-roll for a human who outlasts a link, a passerby noticing the trick once reputation lands) that deepens today's work.
- 2026-09-23: Rummaging a bin — the "bin" slot of "Every prop does something" (booth/lamp/tree already landed) and the first item of "Grows from Bins": `C` with empty hands near a bin now rummages (~1.2s, new `b.rummaging` timer, same movement-lock shape as shaking/calling), new `root` `FigPose` (`stickman.ts` — crouched, one arm in the bin, a couple of flecks flying). On finish: usually "Just rubbish.", 22% of the time (`RUMMAGE_ODDS`) a loose item from `RUMMAGE_ITEMS` (the full item table minus `NOT_CARRIED` — rod/fish/wrench stay job tools, not trash) drops at the feet. Reverse rule: `kid` and `retired` (`world.ts` `JOBS`) now route through `bin1` too; since the bin spot's own `act` is `sweep` (the sweeper's animation), a new override in the NPC routine loop swaps it to `root` for just these two jobs for the stop's first 1.2s, then rolls the same 22% into their carry stack (`n.stack`, since their `item` slot is already their job's own prop) — a `binSeg` field (mirrors `shakeSeg`) keeps the roll to once per visit. A `sweeper` within 150px when a *player's* rummage finishes gets a flat complaint line from the `content.shoved` pool as a head bubble; the standing hit itself waits on the reputation system (see Player growth), same deferred shape as Sell/Interception/Holding-on. Added `bin` to the `EXACT` stand-position list so the crouch lines up on the bin — the sweeper's existing `sweep` pose was drifting up to 300px off before this, now fixed as a side effect. Added task kind `rummage` (`tasksFor`, `goose.ts`); no quest kind (no item reliably changes hands, same reasoning as `call1`/`lean1`/`shake1`). `tsc` and `schema-parity.test.mjs` both pass. Backlog: before picking, checked "Wishes from the town" per GROW.md's rule — three wishes (a microtask stall, a café tip jar, a one-coin bench reservation) all turned out to need the same missing wallet-spend endpoint already blocking "Buy a hot drink"/"Coin kiosk locking an item" under Coins in play, so merged them into that section in build order (bench reservation first once spend exists — no resident-wallet dependency — then the microtask stall, then the tip jar, which also needs the still-unbuilt resident wallets) and deleted the three wish lines; all three stay `- [ ]`, blocked, same as their two siblings. Marked Rummaging a bin done (both here and as the `bin` sub-bullet under Every-prop); opened a chain item (a `kid` racing the player to a fresh rummage find) that deepens today's work; hand-counted from Brace-with's fresh baseline (motions 6, places 7 after that run's additions) and added one item each to motions (a `flinch` startle when a thrown item passes close without hitting, closing the last unclaimed "Transitions and idles" line) and places (three steps up to the church, a small terrain piece) — the two thinnest.
- 2026-09-24: polish — this is run 36, the fourth since the last polish (run 32). Played the core loops in my head against the last several runs' additions and read `SquareGame.tsx`/`world.ts`/`goose.ts`/`stickman.ts` end to end looking for exactly what the holiday-rule polish found last time: dead branches, leaked timers, and unreachable spots. Found three real bugs, all fixed: (1) **Tether leak** — `hit()`'s dislodge check (`n.tether ? n.tetherMissed : true`) only ever got reset to `false` on a *second* knockdown; but a tethered item could also leave a resident's hands through the plain steal branch or the weight-down branch (both just did `n.item = null` with no tether reset), so a resident who lost their tethered item that way kept the flag stuck `true` — the next *unrelated* knockdown then wrongly refused to drop their stack, since `dislodge` stayed false with `tetherMissed` never set. Fixed both branches to clear `n.tether`/`n.tetherMissed` (and sync it, `tether: 0, tm: 0`) exactly when they clear `n.item`. (2) **`b.weighTarget` not cleared by `knock()`** — every other timed channel (`sitting`, `eating`, `watering`, etc.) is defensively reset there, this one was missed; getting hit mid-channel (the 1.5s "hold still to dig it out from under the brick/link-guard" wait) paused instead of cancelling, so resuming after `hurt` clears could finish the channel almost instantly instead of requiring 1.5 continuous seconds. Added it to `knock()`'s reset list. (3) **`gardener`'s job spots included the literal string `'plant'`** (`world.ts` `JOBS`), which matches no real spot (`spotOf`) or map (`wander`'s fallback) — houseplants are keyed `house1:plant`/`house2:plant`/`house3:plant` — so gardeners silently never visited them despite `plant` spots having a matching `water` act ready and waiting; replaced with the three real keys. Also found and fixed a smaller one while re-reading `tasksFor`: the "Steal `<handle>`'s `<item>`" task (`goose.ts`) named a random item from the old, stale `residentsOut` model instead of the item that resident's actual job (`jobOf`, `world.ts`) carries in the live game — right about 1-in-11 of the time by chance, wrong the rest — now reads `jobOf(handles[p.who]).item` so the task text matches what's actually on the resident. `tsc` and `schema-parity.test.mjs` both pass; no feature shipped. Backlog: cleaned up a real duplication the town had re-created — five of the eight fresh "Wishes from the town" entries were near-verbatim repeats of wishes already merged into "Coins in play" two runs ago (the merge note itself warned this was the exact pattern the owner once had to point out), so folded all eight into that section (five new slices: fountain tip-slot, swing push, claims-booth request board, IOU jar, making change — all blocked on the same missing wallet-spend endpoint, some doubly on resident wallets too) and emptied the Wishes section back out. Added a vehicles item (a gardener's cart, tying directly into today's plant-spots fix) and an animals item (a loose park dog, the first animal not tied to any job) to the two most rotation-overdue axes (vehicles hadn't been touched in six runs); opened a "Grows from Bins" chain (a found item still smelling like the bin it came from, for a resident asked to accept it in a fetch quest) that deepens the Rummaging-a-bin run.
- 2026-09-23: Squirrels around the trees (animals axis) — 2-3 squirrels per named tree (`tree1`/`tree2`/`ptree1`/`ptree2`, seeded off `hash('squirrel:'+key)`; decorative filler trees are props, not spots, so they get none), hopping a small seeded loop around the base like ducks/gulls paddle a pond. New `critter` kind `squirrel` and poses `hop`/`freeze`/`climb` (`stickman.ts`, plain shapes — an ellipse body, a round head, a tail that changes angle by pose). They freeze in place when the player or any resident comes within 70px, and climb the trunk (drawn 22px higher, no new position state needed since it's a pure render offset) for ~1.8s when that tree is being shaken — checked against both `b.shaking && b.shakeSpot` (player) and any resident with `act === 'shake'` in the same frame, so the reverse rule needed no separate code path: one check covers both. The first animal reaction tied to a prop interaction (tree-shaking) rather than just proximity, as the backlog item asked. No task/quest kind, same carve-out ducks' scatter-from-running and the still-unbuilt fish-jump/gulls items use — purely reactive dressing, not a player action. `tsc` and `schema-parity.test.mjs` both pass. Backlog: before picking, checked "Wishes from the town" per GROW.md's rule and found the town had written 13 more coin wishes since the last merge (run 36's polish, above) — 8 were near-verbatim repeats of wishes already sitting in "Coins in play" (microtask stall, tip jar, bench reservation, claims booth, swing push, fountain tip-slot, IOU jar, making change), folded away as duplicates; the other 4 (a seconds-stall listing, a paid busk request distinct from the automatic tipping Busking already has, a lamp lottery, a paid duck-feeding request) were genuinely new but the same shape, so merged them into that section too, all blocked on the same missing wallet-spend endpoint; one (a micro-errand board) is a near-duplicate of the claims-booth item, kept as a one-line note rather than built twice. Marked Squirrels done; opened a "Grows from Squirrels" chain (a squirrel making off with a dropped apple, pairing with the still-deferred ducks-steal-a-fish idea; a startled drop-down when a climb's shake stops early) that deepens today's work; added one item each to the two thinnest tracked axes (motions at 2 open items, weather-and-time next at 4, both untouched by today's own animals pick) — a `point` pose for a resident who's just spotted a fetch/repair/rake target, and house windows that glow warm during the existing lamps-lit night window.
- 2026-09-24: Mend request — the town wrote twenty more coin-shaped wishes since the last merge; per GROW.md's rule, checked each against the existing merged systems before picking anything. Sixteen were near-verbatim repeats of wishes already sitting in "Coins in play" (microtask stall, tip jar, reserve seat, claims-booth board, swing push, fountain tip-slot, IOU jar, make change, seconds stall, busk spot, lamp lottery, paid duck-feeding, micro-errand board) or genuinely new but the same shape (prepaid café tab, a paid stall-watching shift, slip-a-coin) — folded all of them into that section in build order and emptied "Wishes from the town" back out. The remaining four (mend request, sort the scraps, seed deposit, rinse-and-hang) share a different, new shape — a human leaves a token at a themed spot, a nearby resident does a short timed service on it — and none of them need the missing wallet-spend endpoint or resident wallets the way the coin wishes do, so merged them into a new top-level system, "Service drop-offs", and built the first slice: `C` with a single non-food, non-wearable item near the lost-and-found board (`board1`) pins it there instead of the usual crate/sell/bin outcome (new `Loose.mend` flag, same five-notes cap the crate already enforces), completing task `mend1` (`tasksFor`, `goose.ts`) on the pin itself — same instant-completion, no-live-balance shape Busking/Selling/Rummaging already established. Reverse rule: a `repairer`-job resident on `routine` near the board who spots a `mend`-flagged item (same 2%-per-frame roll and radius-free check the broken-prop repair roll already uses) detours, plays the *already-built* `fix` pose for ~1.2s (new NPC mode `mend`, same shape as `rake`'s wait-then-resolve), then re-drops the item with the note changed to "— mended" and the flag cleared, so it settles into the board like any other crate item for a later `C`. No quest kind (no item changes hands with a resident, same reasoning as `crate1`/`shelf1`). Per the motion-library rule (today's interaction needed no new pose, reusing `fix`), added a sixth seeded idle fidget alongside `yawn`/`stretch`/`look`/`check`/`shrug`: `sneeze` (`stickman.ts`), same shared 22s clock slot, both player and standing residents. `tsc` and `schema-parity.test.mjs` both pass. Backlog: marked Mend request done, left the other three Service-drop-offs slices (Sort the scraps, next in build order; Seed deposit and Rinse-and-hang behind it) for future runs; opened a "Grows from Mend request" chain (a repairer who's thanked remembers, deferred until reputation lands); hand-counted by "thin axis" tag from the fresh baseline after today's merges (motions 3, Climb 4, animals 4) and added one item each to motions (a `laugh` reaction to a successful busking tip, next off the Reactions list) and Climb (wind streaks on gusty bands, ahead of the storm variant) — the two thinnest, animals having just been the previous run's own pick.
- 2026-09-24: Sort the scraps — second of "Service drop-offs"'s remaining three, next in build order after Mend request: binning no longer vanishes an item on the spot (`SquareGame.tsx`) — it drops as a normal loose item flagged `Loose.sort` at the bin (the `bin:<item>:<spot>` task still completes immediately, same as before; a human or resident can still take it back with the plain loose-item `C` pickup until a sweeper gets to it), and a `sweeper`-job resident on `routine` near it (same 2%-per-frame roll and radius-free check the mend/repair detours already use) walks over, plays the existing `sweep` pose for ~1.2s (new NPC mode `sort`, same wait-then-resolve shape as `mend`/`rake`), then picks it up for good — the one difference from `mend`, which re-drops with a new note instead of removing it. No new task/quest kind: the player's own action (binning) gained no new branch, same carve-out Squirrels used for purely reactive dressing. Per the motion-library rule (reused `sweep`, no pose needed for the interaction itself), added a seventh seeded idle fidget alongside `yawn`/`stretch`/`look`/`check`/`shrug`/`sneeze`: `shiver` (`stickman.ts`), same shared 22s clock slot, both player and standing residents — also added to `KNOWN_POSES` (`SquareGame.tsx`) so other players render it too, closing a gap `sneeze` had been left in by a previous run. `tsc` and `schema-parity.test.mjs` both pass. Backlog: marked Sort the scraps done, left the other two Service-drop-offs slices (Seed deposit, Rinse-and-hang) for future runs; opened a "Grows from Sort the scraps" chain (splitting the sort outcome by item kind — food composts, anything else has a chance at the board instead of vanishing) that deepens today's work; hand-counted by "thin axis" tag (motions 4, animals 4, tied lowest) and added one item each to motions (a `cry` reaction on a resident's third knockdown in one visit, next off the Reactions list) and animals (a mouse that darts from the bakery bin and vanishes rather than freezes, the shyest animal yet) — the two thinnest.
- 2026-09-24: Keep-lock — the town's freshest seven wishes (a mend counter at a "Change Room", a drying rack, a bin-lock, a handheld mend kit, a seed-bundle, a rinse trough, a sort-shelf) all turned out to want the same missing close: after a resident finishes a short timed service on an item, tag it and make it briefly un-grabbable, which "Service drop-offs" never covers (that system only built the drop-and-service half). Merged all seven into a new "Keep-lock" section, one slice per item, and built the base mechanic first since the rest lean on it: the already-shipped Mend request now sets a client-local `lockAt` timer (15s, same unsynced-timer pattern `hideAt`/`wet` already use, `SquareGame.tsx`) on the item it just re-drops; the `C`-pickup site checks it right alongside the existing `hidden` check and fails once with a flat line ("Still being looked at.") instead of the usual grab. Added task kind/key `lock1` (`tasksFor`, `goose.ts`); no new pose (purely reactive, same carve-out as Dust-off/Slide-under/Weight-down — the mender's own motion doesn't change). `tsc` and `schema-parity.test.mjs` both pass. Backlog: merged and deleted the seven wish lines (Wishes from the town is empty again), marked the base slice done, left the other five Keep-lock slices (rinse trough/drying rack, sort-shelf tagging, handheld mend kit, bin-lock, seed-bundle) for future runs; opened a "Grows from Keep-lock" chain (widening the lock to repair/rake finishes too, a passerby noticing someone wait one out once reputation lands) that deepens today's work; full-counted (not just tag-count, which undercounts owner-original untagged items the way a past run already warned) and found places (6 open items, all under "New houses the town proposes") and jobs (7, across "Every prop does something" and the tour-guide/lifeguard/ranger/crier cluster) the two genuinely thinnest, ahead of the tag-tied five — added a wooden dock on the little pond (places) and a crossing guard (jobs), both no new economy, no new prop beyond what they name.
- 2026-09-24: polish — counting from the last polish (run 36), this is the fifth run since, one past the fourth-run cadence GROW.md sets (run 40, Keep-lock, should have been it but shipped a feature instead); catching up now rather than skipping a second one. Read `SquareGame.tsx`/`goose.ts`/`world.ts`/`stickman.ts` end to end for dead branches, leaked state and reachability gaps, the same way the last two polish runs did. Found two real ones: (1) **`KNOWN_POSES` was still missing `feed` and `sneeze`** — both are real `FigPose`s with their own drawing code in `stickman.ts` (`feed` since the ducks/animals work, `sneeze` since Mend request added it as an idle fidget), but the array that sanitizes a *remote* player's incoming pose string (`SquareGame.tsx`, right before `figure()` draws `others`) never listed either, so another player watching you feed the ducks or sneeze saw you fall back to a plain `stand` instead — the exact class of bug the `shiver` fix caught two runs ago, just two more instances of it; added both. (2) **`goose.ts` carried a fully dead island**: its own `SPOTS` array (a stale, pre-multi-map copy of spot data — even wrong coordinates in places, e.g. `lamp1` at a different x than the real one in `world.ts`), the `spotOf` helper, and the `routineAt` function were exported but never imported anywhere outside the file itself; `routineAt`'s only caller was itself gone, and `residentsOut` (the one real caller of the old `SPOTS`) only ever needed it for a spot *key* to stuff into a `Routine` that nothing downstream reads except `.who` (confirmed — `tasksFor`'s `pick()` only touches `.who`, same fact the 2026-09-24 `jobOf` fix already established for `.item`). Deleted all three and pointed `residentsOut` at the real `ALL_SPOTS` (`world.ts`-derived) instead of the stale duplicate. `tsc` and `schema-parity.test.mjs` both pass; `app-parity.test.mjs` unaffected. No feature shipped. Backlog: the town had written twelve more keep-lock-shaped wishes (a Change Room mend counter, a drying rack, bin-lock, a handheld mend kit, a seed-bundle, a rinse trough, a soap stall, a bench cubby, a broom-rack sweep bundle, a pegboard hang-tool, a mend dropbox, a clothesline peg) since the Keep-lock section was created from the first seven — nine were near-verbatim repeats of the five slices already sitting there, folded away; the other three (bench cubby, sweep bundle, pegboard hang-tool) were genuinely new shapes, added as three more Keep-lock slices, and the wish section emptied back out. Hand-counted open items per axis (not just tag count, which still undercounts): places (7, thinnest) and vehicles (8, next) — added a maintenance yard on Market street (places; also the concrete spot the new pegboard hang-tool slice needs before it can be built, tying this run's backlog work together) and a kid's pull-wagon in the park (vehicles).
- 2026-09-24: Bin-lock — fourth of Keep-lock's remaining seven (Rinse trough/drying rack, Sort-shelf tagging and Handheld mend kit stay blocked or lean on other unbuilt chains): when the sweeper's `sort` NPC mode picks up a sort-flagged item from a bin (`SquareGame.tsx`), the *bin* now locks for 20s instead of leaving it immediately re-rummagable — a new `bin?: string` field on `Loose` (and a matching param on `drop()`) carries the bin's spot key from the binning `C` action through to the sort NPC's pickup, where it sets a client-local `binLockAt` timer (same unsynced-timer shape as `lockAt`/`hideAt`, but keyed by prop key instead of item id, since it's the bin being protected, not any one piece of trash). A `C` rummage on a locked bin now fails once with "Just emptied. Give it a minute." instead of starting the usual dig. Added task kind/key `binlock1` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands, same reasoning as `lock1`/`dust1`). No player-side mirror needed — this restricts an existing action rather than granting a new one, same carve-out as Dust-off/Slide-under/Weight-down; left the separate kid/retired bin-treasure roll (unrelated to a specific sort deposit) untouched. `tsc` and `schema-parity.test.mjs` both pass. Backlog: before picking, checked "Wishes from the town" per GROW.md's rule — the town had written nineteen more Keep-lock-shaped wishes since the section was last emptied (run 44's polish); sixteen were near-verbatim repeats of the eight slices already there (mend counter/kit, drying rack/rinse trough twice over, bin-lock, seed-bundle, bench cubby, sweep bundle, pegboard, an automated mend-dropbox and a shred box folded into the mend/sweep-bundle shapes they already cover, a park pegline folded into the fountain drying rack, a street sorting station folded into Sort-shelf tagging) and one more (pin-and-reserve at the noticeboard) was folded into the open "Grows from Keep-lock" chain item by widening it to cover the board alongside the repair/rake widening it already asked for; the remaining two were genuinely new — a fill-and-water action added as a new Keep-lock slice (the first on an already-shipped core action, Watering, rather than a Service-drop-off) and a washyard-basin prop-scrub added as a new blocked slice (Bin-lock's prop-lock shape applied to street furniture instead of a bin; a maintenance-cart variant of the pegboard wish got folded into that item as a note rather than its own slice). Wishes section emptied back out. Full-counted open items by axis (tag-only counts still undercount the large untagged legacy blocks, same caveat the last two counting runs raised) and found quests and relationships-and-children the two thinnest, both around eight open items once each axis's untagged owner-original items are folded in — added a 'claim the mend' quest kind (ties directly into today's Bin-lock/Keep-lock work) and a sixth relationships step ("a look across the square", the first of the seeded-pair reactions that doesn't need the pair to actually cross paths).
- 2026-09-25: Steps up to the church — the last four non-polish runs (Mend request, Sort the scraps, Keep-lock, Bin-lock) all shipped interaction/lock work with no map, building, construction site, housing or infrastructure change, tripping the Owner-direction rule ("if four feature runs pass without visible footprint growth, pick the first feasible [places] one before another micro-interaction"); picked the smallest open places item instead of the town's freshest wish cluster. Added a `steps1` spot at the church (Market street, `world.ts`, new `PropKind` `steps`) drawn as three rising rectangles in front of the church (`prop()`, `SquareGame.tsx`) and added `steps` to `SITTABLE` so `C` sits on the top step — reuses the existing generic `seat` pose and the `SITTABLE`-driven sit toggle wholesale, no new pose or physics (the backlog's "reached with a jump" was flavor text: Square's jump was already ungated by terrain). `priest` and `retired` both gained `steps1` in their `spots` (`world.ts` `JOBS`), so residents sit there on their own routine too — the reverse rule needed no new code since `actPose`/`seatAt` are already generic over any `SITTABLE` kind, the same way adding a sixth bench would need none. No new task/quest kind: one more sittable spot is the same non-event every earlier bench (`bench1`-`bench5`) already was, not a new interaction. `tsc` and `schema-parity.test.mjs` both pass. Backlog: before picking, checked "Wishes from the town" per GROW.md's rule — twenty more Keep-lock-shaped wishes (mend counter, drying rack, bin-lock (already shipped, discarded), handheld mend kit, seed-bundle, rinse trough, soap stall, bench cubby, sweep bundle, pegboard, an automated mend-dropbox, a shred box, fill-and-water, rag-soak, a pegline, a street sorting station, a noticeboard post-found, a maintenance cart, a hose-spray, a patch-and-stitch) were all near-verbatim repeats of the nine slices already sitting in Keep-lock, folded away (the hose-spray variant added as a one-line note to the washyard-basin slice, the only one not already covered); four fresh ones (checkers at a chess table, a fountain pebble-toss, bocce, ring-toss — all "two residents take turns on a timed move, log a score, a human may tap in") were a genuinely new shape, merged into a new "Turn-based games" system at the top of the backlog in build order, but — per the same footprint-priority rule that picked today's feature — left unbuilt this run rather than built as usual, since building it would have been "another interaction variant" the rule says not to reach for; the design note in the section explains how the turn/score sequence can stay a pure function of the room clock (same trick passing-glance/wave/affinity-pairs already use) so it needs no new synced `ev` kind. Wishes section emptied back out. Hand-counted open items by axis and found weather-and-time and Climb the two most rotation-overdue (each last touched several runs back) — added a "full night sky" piece (the steady middle Dawn-and-dusk-light's transition windows don't cover) and a landing-puff sprite for an item that falls clean off the tower, to those two axes; opened a "Grows from Steps" chain (wedding guests lining the steps, once Relationships ships its wedding) that builds on today's work.
- 2026-09-25: Checkers at the park chess table — first slice of "Turn-based games," the sixth system the town designed (four town wishes on 2026-09-25 — checkers, a fountain pebble-toss, bocce, ring-toss — all "two residents take turns on a short timed move at a themed spot, log a score on a slate," merged into one build order the same way the earlier systems were; picked the smallest, since the park map already exists and it needs the least new scenery). Added a `chesstable` spot (`chesstable1`, the park, `world.ts`) drawn as a table, two stools and a chalk slate (`prop()`, `SquareGame.tsx`). The score is a pure function of `(seed, floor(t/10))`, where `t` is the already-synced room clock (`wall()/1000` — confirmed by reading the call site, not assumed) and `seed` is the per-spot seed every prop already carries, so no `day` string is needed for tables to read differently; rather than summing the whole day (expensive, and "reset at midnight" had no design reason behind it), the slate counts only the last 12 buckets (~2 minutes) via `hash('chess:<seed>:<bucket-k>')` — cheap every frame, and identical on every screen without new synced state, the same trick passing-glance/wave/affinity-pairs already use. `C` empty-handed near the table starts a new `b.chessing` channel (10s, fully movement-locked like busking/rummaging, new `chess` `FigPose` in `stickman.ts` — seated, one arm reaching forward to move a piece then pulling back to think); it is purely a human beat, exactly as the merge note specified — the slate's score keeps advancing on the same clock whether or not anyone ever sits in, so the player's turn never mutates it. Added task kind `chess` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands, same reasoning as `call1`/`lean1`). No resident job routes through the table — the reverse rule needed no code, since the "two players" the merge note describes are already always playing via the clock-seeded slate, not a job's routine stop; a genuinely idle prop would have violated the reverse rule, but this one deliberately isn't idle. `tsc` and `schema-parity.test.mjs` both pass. Backlog: before picking, checked "Wishes from the town" per GROW.md's rule — one fresh wish (a ring-toss with a "toss kiosk" and "match board" at the square) turned out to be the same shape as the already-open "Ring-toss at an alley booth" slice of Turn-based games, just at a different, unblocked location; folded it into that item as an alternate spot rather than a second slice, and emptied the Wishes section back out. Hand-counted open items by axis again (same caveat prior counting runs raised — section headers don't reliably track axis, since owner-original items keep accreting under whichever header was open when they were written, e.g. most of "Keeping it"'s 28 open lines are really café/builders/relationships/jobs/weather content that landed under that header by chronology, not shape) and found places (7, the "New houses the town proposes" pipeline specifically) and quests (8, its own dedicated section) the two genuinely lowest, both already thinner than jobs/animals/vehicles/motions/relationships/weather/Climb which all hand-counted to 9–12 once their own scattered chain items were folded in; added a library building on Market street (places, gives the still-generic "library" in Building kinds an actual spot, the same shape the clinic item used) and a `spectate` quest kind (quests, ties into today's chess work — stand near the table while its score changes twice) to those two. Opened a "Grows from Checkers" chain (a passerby's one-off comment when the score changes) that deepens today's work.

- 2026-09-25: A fountain pebble-toss — second slice of the "Turn-based games" system (after today's earlier Checkers): added a `pebbletoss1` spot (the square, near the fountain, `world.ts`) drawn as a chalk throw line and a small standing scoreboard (`prop()`, `SquareGame.tsx`). Score is the exact `chesstable` trick reused wholesale — a pure function of `(seed, floor(t/10))` over the last 12 buckets (`hash('pebble:<seed>:<bucket>')`), cheap every frame and identical on every screen with no new synced state. No new pose: the wish asked to reuse the existing `throw` pose (X) rather than invent one, so empty-handed `X` (`hit('punch')`) near the line, once nothing punchable and nothing breakable is in reach, plays the existing 0.3s throw swing instead of a whiff and completes task `toss1` — carrying something still throws it as normal (checked first in `hit()`), so the toss only fires with empty hands. Added task kind `toss` (`tasksFor`, `goose.ts`); no quest kind and no resident routing needed, same reasoning Checkers already established (the clock-seeded score is always "being played" whether or not anyone taps in). `tsc` and `schema-parity.test.mjs` both pass. Backlog: before picking, checked "Wishes from the town" per GROW.md's rule and found nine more turn-based-minigame wishes since the system was created earlier today — three were near-verbatim repeats of slices already in the section (ring-toss/kiosk repeated a third time, "bowls" and "street-skittles" both the same roll-toward-a-target shape as the still-unbuilt Bocce, folded into that item as reskins rather than separate slices) and six were a genuinely new shape (a stage-buzzer quiz board, paper-plane + paper-boat distance contests merged as one slice, hopscotch, a memory-match flip-slab, and a scavenger dash flagged as the odd one out since it spans three maps instead of one themed spot) — added as five more build-order slices to "Turn-based games" rather than a second section. Wishes section emptied back out. Hand-counted open items and found animals and places the two thinnest again — added rabbits at the garden beds (a fourth small critter reaction, this time to watering/running rather than a tree, bin or café table) and a newsstand on Market street (frees the `paper` item from only ever starting pre-carried by `postie`, the same free-lend shape the library's shelf already uses). Opened a "Grows from Pebble-toss" chain (a passerby's one-off comment when the score changes, mirroring Checkers' own) that deepens today's work.
- 2026-09-25: polish — counting from the last polish (run 41), this is run 46, the fifth run since (42 Bin-lock, 43 Steps, 44 Checkers, 45 Pebble-toss), one past the fourth-run cadence GROW.md sets (run 45 should have been it but shipped Pebble-toss instead); catching up now rather than skipping a second one, same call the previous late polish (run 41) made. Read `SquareGame.tsx`/`goose.ts`/`world.ts`/`stickman.ts` end to end (delegated the first read-through to a sub-agent instructed to only report verified, re-read-confirmed findings, then re-verified each one myself against the live source before touching anything). Found and fixed two real bugs, both in `SquareGame.tsx`: (1) **`Npc.caught` (and `retX`/`retD`/`boardNote`/`boardOrigin`/`stowUntil`) leaked past knockdown** — a resident mid-`catch`/`deliver`/`board`/`shelve` (carrying an intercepted throw, a stray item bound for the crate, or a shelf) who got punched, kicked or hit by a thrown item had `mode` set to `'down'` and their own `item`/`stack` dropped, but `caught` was never touched; once they got back up (`down` → `dust` → `return`/`routine`) the stale item kept rendering next to them forever (`n.caught && n.mode !== 'down'` in the draw code) with no way to ever pick it up — it wasn't a loose item, and the delivery logic that reads `retX`/`retD` only runs in the modes they'd already left. A stale `stowUntil` had the same shape: it survives a knockdown, so an NPC who later fetches an unrelated item within the old window renders it as a coffee cup and blocks grabs with "Still in the cup." for no reason. Added a small `flushCaught(n, x, d)` helper (drops `caught` as a loose item, clears all five fields) and call it at both knockdown sites (melee in `hit()`, thrown-item in the projectile loop). (2) **Three copies of the same steal/weight-down bug**: `taken = n.stack.length ? n.stack.pop()! : n.item!` followed by `if (!n.stack.length && taken === n.item) n.item = null` decides "did this come from the job-item slot" by comparing *values* after the pop, not by tracking where it actually came from — if an NPC's `n.stack` happens to hold a second item of the same `ItemKey` as their own `n.item` (reachable: the opportunistic-fetch code pushes a stray item of any kind onto `n.stack`, including one matching the carrier's own job tool), a steal that pops the stack item wrongly nulls `n.item` too, deleting the resident's real held item from the world with no drop, no transfer, nothing — it just vanishes. Same bug, byte-for-byte, at the weight-down branch, the plain-steal branch, and the NPC's own throw-at-player branch. Fixed all three to compute `fromItem = !n.stack.length` *before* popping, so which slot the item leaves is decided by state, not by value equality. `tsc` and `schema-parity.test.mjs` both pass; `app-parity.test.mjs` unaffected (Square isn't one of its pinned files). No feature shipped. Backlog: no picking this run (polish). Checked "Wishes from the town" per GROW.md's rule anyway (the rule doesn't carve out polish runs, and the last three runs have consistently found fresh ones) — ten fresh wishes, all Turn-based-games-shaped: nine were near-verbatim repeats of slices already sitting in that section (ring-toss, stage-buzzer quiz, bowls, paper-plane, paper-boat, street-skittles, hopscotch, scavenger dash, memory match), folded away with no new information; the tenth (quick-charades at the prize stage, voted on via a clap post) was genuinely new and became that section's final build-order slice, using the section's own established "human input is cosmetic against the deterministic clock" trick for the "vote" since the room can't carry a real tally. Wishes section emptied back out. Hand-counted open items by tag and found motions (5, and five runs rotation-overdue — last touched at the Sort-the-scraps run) clearly thinnest, with weather-and-time picked as the second (tied at 6 with animals/Climb by tag, but animals/places were just touched last run and Climb's own section already carries twice the raw backlog depth weather-and-time's does) — added "a bend before picking up" (closes the last unclaimed Transitions-and-idles line, and reaches more moments than anything else left on that list since every `C` pickup in the game shares one site) to motions, and "a handful of stars during full night" (a direct continuation of last run's Full-night-sky piece) to weather-and-time. Per the "build on what shipped recently" rule, opened a new line under "Grows from Interception" (today's fix #1 means a mid-delivery item now actually drops on knockdown instead of floating forever — but that new drop doesn't route through `slideNear` the way the resident's own item at the same site already does, so it's next to fix).
- 2026-09-25: Bocce at a park court — third slice of the "Turn-based games" system (run 47, after Checkers and Pebble-toss; not a polish run since the last one was run 46): added a `bocce1` spot in the park (`world.ts`) drawn as a small dirt court with a target ball and two team balls, one lit per the same `bucket % 2` whose-turn trick `chesstable` already uses for its stools (`prop()`, `SquareGame.tsx`). Checked the backlog's own open question — whether the existing `throw` pose at a lower arc reads as a roll — against Pebble-toss's precedent (it already reuses `throw` for a very similar toss) and decided yes: no new `FigPose`. Empty-handed `X` near the court (once nothing punchable/breakable is in reach, same `hit()` branch Pebble-toss added) plays the reused 0.3s throw swing and completes a new task `bocce1` (`tasksFor`, `goose.ts`); score is the identical `(seed, floor(t/10))` 12-bucket trick chess/pebble-toss use (`hash('bocce:<seed>:<bucket>')`), so the court is always "being played" on the room clock, human or not — same reasoning, no resident routing needed. The two later town wishes for "bowls" (a park green) and "street-skittles" (a nine-pin lane) stay folded into this one court as still-unbuilt reskins, not separate slices, per the backlog's own note. Per the motion-library rule (today's interaction needed no new pose), added an eighth seeded idle fidget alongside `yawn`/`stretch`/`look`/`check`/`shrug`/`sneeze`/`shiver`: `fan` (`stickman.ts`, hand fanning the face) — the hot-weather mirror of `shiver`, landing ahead of any heat system the same way `shiver` landed ahead of cold, closing out the last unclaimed generic pose from the Reactions list ("shrug, wave, point, laugh, cry, sneeze, yawn, stretch, shiver, fan" — only `slip`/`flee` remain, both tied to their own bigger systems). `tsc` and `schema-parity.test.mjs` both pass. Backlog: before picking, checked "Wishes from the town" per GROW.md's rule — four fresh wishes, all describing a "join/judge a 90-second resident-run round" instead of this section's usual ambient clock-seeded score, but all still the same underlying shape: 'Fountain Flip' and 'Start Blitz' turned out to be the already-shipped Pebble-toss and Checkers under new names, 'Ring the Bell' the still-open stage-buzzer quiz board with "judge" swapped for "buzz", and the generic 'Join Game' action describes nothing the section doesn't already do (every slice already lets a human tap in, cosmetically, against the room clock, precisely because the room can't carry a real queue/judge/round `ev` kind) — folded away with no new build-order slice, wishes section emptied a second time today. Marked Bocce done. Hand-counted open items per axis by hand rather than by tag alone (tag counts undercount Climb especially, which carries roughly twice the raw depth its tag count suggests) and found motions (6 open Reactions/Transitions items: wave, flinch, point, laugh, cry, bend) and animals (8 open critter reactions: pigeons, cat, bees, fish-jump, gulls, loose dog, mouse, rabbits) the two genuinely thinnest against every other axis's 9-12 — added a `handshake` pose for delivery-quest handoffs to motions and frogs at the duck pond (the first animal that can leave the visible world entirely for a beat, not just freeze/flee/vanish-in-place) to animals. Opened a "Grows from Bocce" chain (a passerby's one-off comment on the score, the same shape Checkers/Pebble-toss already got) that deepens today's work.
- 2026-09-25: The light-chain board — first slice of "Resident-run rounds," the seventh system the town designed (nine fresh town wishes, all a tappable 'Start X' action beginning a fixed 60-90s resident-run round with a human joining as player or judge — but re-checked each against what already exists per GROW.md's rule rather than assume repetition: five turned out to be the already-shipped or already-open Turn-based-games slices under new names — 'Join Game' describes nothing the section's own clock-seeded slates don't already do; 'Ring the Bell' is the still-open stage-buzzer quiz board with "judge" swapped for "buzz"; 'Fountain Flip' is the already-shipped Pebble-toss; 'Start Blitz' is the already-shipped Checkers; 'Start Charade' is the still-open Quick-charades slice — folded away with no new build-order line, same call the immediately preceding Bocce run made for four earlier wishes of this same "join/judge" shape. The remaining four (a post-office parcel relay, the square's own light-chain memory game, a word-chain sentence board, a pond duck race) are a genuinely different shape from Turn-based games' single-spot two-person score: a scripted round spanning several residents, two of them needing a prop that doesn't exist yet — merged into a new "Resident-run rounds" section, one slice per item, and built the smallest, since it needs no new prop): a `simon` spot in the square (`world.ts`) draws three pads with `prop()` (`SquareGame.tsx`), one lit at a time by the exact seed+clock-bucket trick chess/pebble/bocce use, just on a 6-second bucket instead of their 10-second one (reads faster as "the light moved"); a chalk slate above counts the current unbroken streak of successful buckets, scanning back through the last 12 the same way those three already do. `C` within 90px reads that same already-flowing clock and says whether this bucket kept the chain going or broke it — cosmetic, never a mutation, the same rule the rest of this family (and Turn-based games before it) already established. Task `simon1` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands, same reasoning as `chess1`/`toss1`/`bocce1`); no resident routing needed (same carve-out chess/pebble/bocce use — the board is never idle, it's always "being played" by the clock). Also built, per the motion-library rule (today's main pick reused no new pose): "A laugh at a good tip" (motions axis, an already-dated backlog line) — a `laugh` `FigPose` (`stickman.ts`, head back, shoulders shaking) plays on one `routine`-mode resident within 80px, 35% odds, when the player's busking tip roll succeeds. Since a still-`routine`-mode resident isn't part of the periodic `npcpos` broadcast, the one-off `npc` event (`apply()`, `SquareGame.tsx`) needed widening to carry an optional `swing`/`sk` pair so other players' screens see the laugh too, not just the busker's own — a small, additive parser change, same shape `stow`/`tether` were added incrementally before. `tsc` and `schema-parity.test.mjs` both pass; `app-parity.test.mjs` unaffected. Backlog: marked both items done; per the footprint-priority rule, checked the last four non-polish runs (Steps, Checkers, Pebble-toss, Bocce) and found Steps already satisfies it, so an interaction pick was fine this run. Hand-counted open items and found motions (5, after today's `laugh`) and animals (9) the two thinnest — added "a nap on a bench" (motions, next unclaimed pose off the Play-and-rest list) and "moths around a lit lamp at night" (animals, the first critter tied to the existing Lamps-on-a-clock piece rather than a prop hit). Opened a "Grows from Light-chain board" chain (a passerby's one-off comment on the streak, the same shape Checkers/Pebble-toss/Bocce already got; folding the board into the existing `spectate` quest kind) that deepens today's work.
- 2026-09-26: A bus stop on Market street — the last four non-polish runs (Checkers, Pebble-toss, Bocce, The light-chain board) all shipped a new spot/board inside an existing map with no map/building/site/housing/infrastructure change, tripping the owner's footprint-priority rule again; picked the smallest open places item rather than the town's freshest wish cluster, same call run 46 (Steps) made under the identical rule. Added a `busstop1` spot (Market street, `world.ts`, new `PropKind` `busstop`) between the station and the church, drawn as a small roofed shelter over a bench (`prop()`, `SquareGame.tsx`); added `busstop` to `SITTABLE` so `C` sits under it exactly like any bench (reused `seat`/`sit` wholesale, no new pose or physics, no separate `EXACT` entry needed since that list already spreads `...SITTABLE`). No job routes through it yet — it's furniture until Weather (or a transit item) gives it a reason to matter, same as the still-unbuilt footbridge/dock. `tsc` and `schema-parity.test.mjs` both pass; `app-parity.test.mjs` unaffected (Square isn't one of its pinned files). Backlog: before picking, checked "Wishes from the town" per GROW.md's rule and found seventeen fresh wishes. Twelve (2026-09-25 batch) were 'Start X' 90-second resident-run-round-shaped: five ('Join Game', 'Ring the Bell', 'Fountain Flip', 'Start Blitz', 'Start Charade') were the already-shipped/open Turn-based-games slices under new names, folded away; three more ('Start Relay', 'Start Simon', 'Start Story Chain') were the already-open/shipped parcel-relay, light-chain-board and word-chain-sentence-board items under Resident-run rounds, folded there with no new line; the remaining four ('Start Regatta', 'Start Scavenge', 'Start Hot Potato', 'Start Tag') were genuinely new slices of that same shape, added to Resident-run rounds' build order. The other five (2026-09-26 batch) circled a different subject entirely — a short phrase or word passed between residents, degrading a little each hop (a swapped word, a muffled letter) — three or more circling one subject per GROW.md's own rule, so treated as a system on its own: one ('Start Chainline') duplicated the still-open word-chain-sentence-board item and was folded there; the other four (Echo Ping, Whisperline, Leave Murmur, Call-and-Response) became a new "Word relay" section at the top of the backlog, one slice per item, in build order — but per the same footprint-priority rule that picked today's feature, left unbuilt this run rather than built as usual, since building it would have been "another interaction variant" the rule says not to reach for (the same tradeoff run 46 made for Turn-based games' own creation). Wishes section emptied. Full-counted open items by axis (tag-only counts undercount older untagged blocks, same caveat prior counting runs raised — Climb's tag count of 6 full-counts to 12, motions' tag of 7 full-counts to 12) and found places (8, "New houses the town proposes") and jobs (9 across "Every prop does something," "Buildings that give roles" and "Money that means something") the two genuinely thinnest — added a conductor job at the new bus stop (jobs, also the "build on what shipped recently" pick, deepening today's own item) and a marked street crossing at the Market-street/square boundary (places, gives the still-open crossing-guard job an actual spot). Opened a "Grows from Bus stop" chain (routing rain-shelter behaviour through the stop once Weather lands) that builds on today's work.
- 2026-09-26: polish — counting from the last polish (run 46), this is run 50, the fourth since (47 Bocce, 48 The light-chain board, 49 Bus stop), on cadence. Delegated the first end-to-end read of `SquareGame.tsx`/`goose.ts`/`world.ts`/`stickman.ts`/`tower.ts`/`pond.ts`/`content.ts`/Climb to a sub-agent instructed to report only re-verified findings (input state → code path → wrong output), then re-verified each one myself against the live source and traced one further before touching anything. Found and fixed three real bugs: (1) **`content.ts`'s `SPOT_KINDS` allowlist, the documented schema boundary for patrol-written `site_meta.square_content` spots/maps (`TOWN-ARCHITECTURE.md`), was never updated for the six outdoor `PropKind`s the last several growth runs added to `world.ts`** (`steps`, `chesstable`, `pebbletoss`, `bocce`, `simon`, `busstop`) — any patrol-proposed spot or map using one of these six kinds would be silently dropped by `parseTownSpots`/`parseTownMaps`' own filter, the classic "new value added to source, not added to the sanitizer allowlist" bug, just for patrol content instead of a remote pose. Added all six. (2) **The thrown-item knockdown path (`SquareGame.tsx`, the projectile loop) diverged from the melee knockdown path (`hit()`) on two protection mechanics that only ever got wired into melee**: Tether (a just-recovered item survives exactly one knockdown) was bypassed outright — a thrown hit unconditionally dropped `n.item` and never touched `n.stack`, so a tethered resident hit by a thrown object lost the item on the very first hit (defeating `tether1`'s "two hits" design) while leaving `n.tetherMissed` stuck, so a *later*, unrelated melee knockdown on the same now-empty-handed resident wrongly needed a third hit to clear it. Brace-with's link-absorb window (a linked pair no-sells one knockdown per 14s) was missing entirely from the thrown path — a linked resident hit by a thrown item always went down, no absorb roll at all. Routed the thrown-hit block through the same `dislodge`/tether-sync logic `hit()` already uses (drop `n.stack` too, sync `tether`/`tm` on the `npc` ev) and added the identical `linkedNpc`/`linkAbsorbBucket` check before the knockdown (absorbed hits now `drop` the thrown item on the ground instead of connecting, same as a completed knockdown would). (3) Minor: removed a dead import (`ITEM_LIST as POND_ITEMS` from `pond.ts`, `SquareGame.tsx`) — only `BADGE_BY_KEY` from that module is actually used. `tsc` and `schema-parity.test.mjs` both pass; `app-parity.test.mjs` unaffected (Square isn't one of its pinned files). No feature shipped. Backlog: marked the still-open "Tether protects against a thrown hit too" (Grows from Tether) `[x]`, since today's fix #2 is exactly that item. Checked "Wishes from the town" per GROW.md's rule (doesn't carve out polish runs) and found it still held eighteen wish lines that run 49's own log entry had already described folding away — the actual deletion never landed in the file, just the log claim; verified each of the eighteen against the sections run 49 said they'd land in (all seventeen were genuinely already present as build-order lines there) and deleted the stale wish lines now. The eighteenth, 'Record and Play' (2026-09-26, untriaged by run 49), was the same degrading-token shape as Leave-a-murmur/Echo-board under Word relay wearing a talkbox-booth costume — folded there with no new line. Wishes section emptied. Full-counted open items by axis and, cross-checking against the last several runs' own picks (jobs+places at run 49, motions+animals at both 47 and 48), found relationships-and-children (~9, untouched since Bin-lock, the most rotation-overdue axis by far) and vehicles (~10, untouched since the 2026-09-24 polish's gardener's cart) the two most overdue — added "A shared bench, avoided" (relationships-and-children, the rivalry mirror of the already-shipped "A shared bench," same pairing "A rivalry glance" already mirrors "A passing glance") and "A luggage trolley for the courier" (vehicles, deliberately *not* another item stacked behind the still-missing `push`/`pull`/`drive` poses — reuses the bike rack's "no new pose, just a trailing prop" trick instead). Per the "build on what shipped recently" rule, opened a new line under "Grows from Brace-with" (today's link-absorb fix reuses the melee case's `LINK_LINES` pool for a thrown block too — give the thrown case its own line next).
- 2026-09-26: Leave a murmur at a post, lamp or bench — first slice of "Word relay," the eighth system the town designed (five town wishes, 2026-09-26; merged into a build-order section the same way every prior system was), not a polish run since the last one was run 50. Scoped "post" down to the two prop kinds that actually exist for it — `lamp` and `bench` (`world.ts` has no standalone "post" prop). Every 20-second bucket (`MURMUR_WINDOW`), each named lamp/bench spot on the current map gets a 30% seeded chance (`MURMUR_PIN_PCT`, `hash('murmur:pin:...')`) of a murmur landing — gated on a `routine`-mode resident actually being in `lean`/`sit` act at that exact spot that bucket, so a murmur is still "a resident leaving it," not a prop talking to an empty street. The line (`MURMUR_LINES`, a small fixed pool, no LLM/free text) plays as that resident's own `say` bubble (reused wholesale — the mechanism every other flat-line reaction, from `CALL_LINES` to `RUMMAGE_LINES`, already uses). A different `routine`-mode resident within 80px (`MURMUR_ECHO_R`) gets a further 30% seeded chance (`MURMUR_ECHO_PCT`) to echo it that same bucket with one word swapped for "—" (new `muffle()` helper). Both halves are pure functions of (spot key or resident id, bucket) — no `npcEv`, no synced state at all — so every screen, including logged-out spectators, computes the identical bubble text independently, the exact trick ducks/squirrels/idle-fidgets and the turn-based slates already established; this is the first ambient two-resident exchange actually shipped (passing-glance/rivalry-glance/wave-hello were all still just backlog text describing this same carve-out). Purely reactive dressing: no task/quest kind (nothing a human does or receives, same carve-out Squirrels/Mice use), no player-side mirror. Per the motion-library rule (today's main pick reused no new pose, just the existing `lean`/`sit`/`say`), also built "A wave hello" (motions axis, next unclaimed pose off the Reactions list): new `wave` `FigPose` (`stickman.ts`, one arm raised overhead, oscillating); every 4-second bucket, two currently-moving `routine`-mode residents within 50px who aren't linked (`linkedNpc`) and aren't an affinity pair (`affinityOf(a,b) < 0.85`, the same threshold "A passing glance"/"A rivalry glance" reserve for themselves) get a 40% seeded chance to both flash `wave` for 500ms — same seed+bucket mechanism as murmur, put to first use for the still-dangling `affinityOf`/`pairsFancying` pure functions (until now called from nowhere). `tsc` and `schema-parity.test.mjs` both pass. Backlog: marked both items done. Checked "Wishes from the town" per GROW.md's rule — six fresh wishes, all circling the exact subject Word relay already covers: 'listening posts' duplicated the still-open Listening Post slice outright; 'call-and-response'/'trail ping' were the reply-board/Listening-post shape with different dressing, folded there; 'murmur tap' restated Leave-a-murmur's own forward-or-anchor choice, folded into today's build; 'harmonize'/'intonation mark' describe a mutation of the line's content rather than who holds it next, noted on the Word relay section for whichever slice lands a real line-carrying prop. Wishes section emptied. Hand-counted open items by axis, discounting Climb's low tag count per run 51's established caveat (it full-counts roughly double), and found quests (7, last touched at Checkers/run 47) and weather-and-time (7, last touched two runs ago) the least recently topped-up — added a 'listen in' quest kind (ties directly into today's murmur clock, the same passive-presence shape `no-hands`/`shadow`/`spectate` already use) and a third prop (the fountain) to the Lamps-on-a-clock/House-windows purely-visual night chain. Opened a "Grows from Murmur" chain (a second echo hop, muffling a second word — the piece the rest of Word relay's build order still wants) that builds on today's work.
