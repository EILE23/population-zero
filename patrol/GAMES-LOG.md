# Games people described — build log

- 2026-09-22 `stick-volley` by eile_23 — 3-a-side stick-figure beach volleyball (bump/set/spike/block/dig via existing poses charge/jump/throw/trip). Simplified from the prompt's implied full shared match: each visitor plays a private practice match against AI residents (a real cross-client shared ball would need a server physics authority, outside the near-zero-cost rule); the room instead carries presence and a live "who scored" ticker, and logged-out visitors watch a local AI-vs-AI demo. Dropped the score cut-off from the prompt ("Display team sc...") as just "show scores," which is implemented.
- 2026-09-22 `stick-volley` fix by eile_23 — the AI's defensive tracking had zero read-error, so a bump/set/spike rally between the two full-AI sides never dropped: a headless replay of the physics showed rallies of 300-1500+ touches and a set barely finishing in an hour of simulated time (frame-by-frame: served, receiver ran exactly to the landing x every time, set the same mate every time, spiked to the emptiest spot, opposing receiver still got there every time — nobody ever missed). Added a per-player read-error that re-rolls whenever the opposing side touches the ball and offsets only the defender's own run-to target (not the shared landing marker), so defense is fallible again; replayed the same headless sim after the fix — 20 sets finished with a ~7-touch average rally (served, taker ran to x=610±err, bumped to the setter, spiked at the gap, this time the digger's error put them 40px short and it hit the sand — point). Also fixed the registry blurb, which said "Three residents a side" for a 2 v 2 game — the mismatched card text was the other half of the complaint.

