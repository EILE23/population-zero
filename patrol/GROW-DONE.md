# Square growth — done items (moved out of the backlog by grow-tidy.mjs; the log has the how)
- 2026-09-22 · `C` with empty hands near a bench/sofa/bed/swing: sit or lie on it (poses `sit`/lying), stand up on any move. Send the pose so others see it. Swings sway. — added `b.sitting`: `C` toggles it near a `SITTABLE` spot (`world.ts`), locks movement, any direction/jump key stands you up; pose sent over the socket and rendered for others. Residents already did this via their routine's `act:'sit'` at these same spots, so the reverse rule was already satisfied.
- 2026-09-22 · Eating: `C` with a sandwich/cup near a café table or bench eats it (short `eat` pose, item consumed). Residents with food do the same at their spots. — added `FOOD` (`goose.ts`); `C` near a cafe/table/bench with a sandwich or coffee sets `b.eating` (freezes briefly, `sit` pose, toast) instead of dropping it; NPCs already used the `sit` pose + "eating" label for `act:'eat'`, now any NPC holding food shows it the moment they stop anywhere on their routine, not just at a designated eat spot.
- 2026-09-22 · Exercise props in the park (pull-up bar, bench press) as `PropKind`s with a `pushup`/`pullup` pose; `C` near them exercises for a few seconds; residents with the `jogger` job use them. — added `pullbar`/`benchpress` `PropKind`s + two park spots (`world.ts`), `pushup`/`pullup` `Activity`s and stickman poses; `C` with empty hands near either now exercises for ~2.4s (movement locked, broadcast like sitting); `jogger`'s routine spots now include both, so joggers already do this on their own via the existing act→pose pipeline.
- 2026-09-22 · Throwing (owner's first pick, done by hand 2026-09-22 — `X` while carrying throws; `throw` pose; `ev throw` so everyone sees the arc; hits residents (down), people (`hitp` kind throw), props (one kick's damage on landing); residents throw with the same pose and event): `V` (or `X` while carrying) throws the top carried item forward — a real wind-up pose (`throw`, arm back then forward, ~0.3s), the item flies in an arc (the `thrown` list already exists for residents: reuse it), hits a resident (they go `down` + a `shoved` line) or a player (send `hitp`) or a prop (cracks it like a kick), then lies on the ground as a loose item. Residents already throw when chasing; give them the same `throw` pose. Send the pose over the socket so others see the wind-up, and emit the flying item so others see the arc.
- 2026-09-22 · Watching TV and reading: `C` near a TV / bookshelf shows a small line above the head ("watching", "reading") and a matching still pose. — TV's spot act changed `stand`→`watch` (`world.ts`); new `watch` pose (arms folded, head tilted to the screen) in `stickman.ts`; `C` with empty hands near a `tv`/`shelf` toggles `b.still` (locks movement, any direction/jump key stands you up, same as sitting), rendered with the existing `actIcon` label ("watching"/"reading") above the head. Residents already stop at both spots on a house owner's routine (`world.ts`'s `houseInterior` stops), so they get the same pose and label for free through the existing act→pose pipeline — no resident-specific code needed.
- 2026-09-22 · Watering: `C` near a garden with no item waters it (small drops); gardener residents do the same. — `C` with empty hands near a `garden` spot sets `b.watering` (locks movement ~1.4s, `water` pose — the watering-can drops were already drawn by the existing pose); gardener residents already visited `garden`/`garden2` on their routine with `act:'water'` so they got the pose and "watering" label for free. Added task kind `water` (`tasksFor`) and quest kind `water` (`questFor`) in `goose.ts` so a day's list can ask you to water a named bed and a resident can ask "could you water mine".
- 2026-09-22 · Bins: `C` near a bin with an item drops it *in* the bin (disappears for good). Sweepers empty bins. — near a `bin` spot, `C` consumes the top carried item for good (no loose item, same as eating: the stack shrinks and syncs to others over the normal `pos` update, no new event kind needed since the room only relays known `ev` kinds); added task kind `bin` (`tasksFor`) and quest kind `bin` (`questFor`, "take this X and bin it") in `goose.ts`; bin spots (`bin1`-`bin4`, `world.ts`) now use the existing `sweep` act instead of `stand`, so the sweeper job (the only job with bins on its route) already empties them with the existing sweep pose on its routine — no resident-specific code needed.
- 2026-09-22 · Tripping while chasing (owner, 2026-09-22) — a chasing resident now rolls a small per-second chance to trip (higher for the `retired` job and while carrying an item): new `trip` pose (face-down, feet up, `stickman.ts`), drops what they carry, says a flat line, chase clock (`n.until`) loses two seconds, then resumes the chase or gives up depending on time left. The roll happens before the catch check, so a trip within reach lets the player go (falls out naturally — `chasedSince` isn't reset, so it still counts toward the `chased` task). Reverse: players running empty-handed at full speed over a loose item on the ground trip the same way (`b.tripped`, same pose). Deferred: pond/fountain-edge and broken-prop triggers — see "Grows from Tripping" below.
- 2026-09-22 · Fishing spots (owner, 2026-09-22) — a `rack` prop (`PropKind`, `world.ts`) by the little pond (`dock1`) and the duck pond (`dock2`); `C` with empty hands near it takes a rod (new `rod` `ItemKey`); `C` while holding a rod within 90px of any `WATER_SPOTS` entry casts (locks movement, `fish` pose — already existed in `stickman.ts`, just wired up) for a random 4–12s, then catches: a random name from the 250-item pond list (`ITEM_LIST` from `pond.ts`, reused as instructed, not rebuilt) shown as a toast and a head bubble, and a generic `fish` `ItemKey` added to the stack — reuses every existing carry/throw/dunk/bin path for free since those are generic over `ItemKey`. Retired, jogger and priest have `dock1`/`dock2` added to their job spots, so they fish on routine with the same `fish` pose via the existing act→pose pipeline — no resident-specific code. Task `fish1` ("Catch a fish"); no new quest kind needed — `fish` joining `ItemKey` makes it a possible `fetch`-quest item for free ("X wants a fish. get me a fish. don't ask."). Visible to others via the normal `pos.s` stack sync, no new `ev` kind (see the Bins note below on why not). Also fixed a real bug found along the way: a botched merge had left two dead-code `if (pose === 'trip')` blocks in `stickman.ts`, the second unreachable and failing `tsc`; removed the unreachable one. Not done: rod tiers/zones/bait/weighting from the full pond minigame (deliberately not rebuilt), rarer fish at night/rain, a plaque for the biggest.
- 2026-09-22 · Animals — ducks (owner, 2026-09-22; first slice of the Animals ask, pigeons and cat deferred below): 3–5 seeded ducks per pond (`pond` in the square, `pond2` in the park), drawn with the new `critter(ctx, kind, x, y, s, pose, t)` in `stickman.ts` (`CritterKind`/`CritterPose`, poses `paddle`/`flap`/`feed`). Base swim is an ellipse around the pond, a pure function of seed + `t`, so everyone sees the same loop; the scatter reaction is computed locally per screen the same way a resident's reaction to being punched already is (not a new `ev` kind — the room only relays `drop`/`pick`/`break`/`fix`/`npc`/`npcpos`/`hitp`, see the Bins note below): each client watches its own player, the other players it already has positions for, and airborne thrown items, and flaps ducks away from whichever is nearest within 90px. `C` with a sandwich or coffee within 140px of a pond feeds the ducks instead of eating/dropping (item consumed, `feed:<pond>` task via new `feed` task kind in `goose.ts`, `PONDS` list added next to `GARDENS`/`BINS`); ducks within 220px drift toward the feeding spot and show the `feed` pose. Reverse rule: the `kid` job already stops at `pond2` carrying a sandwich (`FOOD`) on its routine — it now feeds the ducks there instead of just eating, with a new `feed` `FigPose` in `stickman.ts` (crouch, toss, falling crumbs) and an `actIcon` label. Not done (kept for a future run): pigeons on the square, a cat on a roof/windowsill — see "Grows from Animals" below.
- 2026-09-22 · Wearing and using what you carry — first slice, hat and glasses (owner, 2026-09-22; umbrella/phone/newspaper/broom-tidy/basket-capacity deferred below): `C` on yourself (no bin, water or bench/café/table near) with a hat or glasses on top toggles it worn — drawn on the head/face instead of the floating carry stack, sent in `pos.s` with a `!` marker (`hat!`); press `C` again in the open to take it off (stays carried, not dropped). Residents already carry their job item (busker/mayor a hat, retired glasses); the same draw path now puts it on the head instead of in the hand for those two kinds, so the reverse rule falls out of existing routines — no per-resident code. Task `wear:<item>` and quest kind `wear` ("wants to see you in a hat") added. Knocked down drops the whole stack already, including anything worn (`b.wearing.clear()`), so that half of the rule was already covered. Also fixed a real bug found along the way: a leftover duplicate duck system from the last merge (`ducks.current`, an always-empty, never-populated `useRef` Map, shadowed by a same-named local array so it silently read nothing) was dead code that no longer type-checked (`tsc` failure: `'scatter'` isn't a `CritterPose`, `.current` on an array); removed it — the pond ducks were already running on the other, working system next to it. Not done: umbrella (open overhead, ties to the not-yet-built Weather), phone/newspaper "use" poses when idle, broom actually tidying loose items, basket raising carry capacity — see "Grows from Wearing" below.
- 2026-09-22 · Grows from Animals · Ducks react to a thrown item, not just running (chain from Animals) — turns out this was already there: the duck scatter check (`SquareGame.tsx`) already walks `thrown.current` for any airborne item within 70px (`th.z > 0`), landed in the same commit that added ducks (`f494b2a`). This backlog line was stale, not a real gap; found while picking today's item, no code changed.
- 2026-09-22 · The affinity seed itself (thin axis: relationships and children, 2026-09-22 — unblocks Flowers and Relationships above, both of which say "once that lands"): `affinityOf(a, b)` and `pairsFancying(day, roster)` in `goose.ts` — see `GROW-LOG.md`.
- 2026-09-23 · Grows from Booth calls · Booth calls sometimes ring a real ask — on hangup, 1-in-6 odds now ring through to a resident on today's roster who doesn't already have a live quest: `questOf(who)` is reused exactly as the in-person `E` talk does, the quest is set into `quests.current` and shown as a toast/head bubble in the resident's own voice; the other 5-in-6 (and the fallback when no eligible resident exists) keep the old stock `CALL_LINES`. No new quest kind, no new pose — pure reuse of existing machinery, as scoped.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a 'tag-and-log' interaction so residents can attach a visible retrieval tag to any placed item that records who tagged it, where, and the time.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) When an item sits in the fountain for over two minutes, a resident walks over with a rake, pulls it out, and sets it on the stone rim.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a 'catch-and-return' interaction so a resident can attempt to intercept a tossed item and carry it back to its original spot.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a short 'brace' interaction so a resident can plant their feet to resist being picked up for ~8 seconds, play a protest line, and enter a brief cooldown.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) When a resident is knocked flat, they remain sitting on the pavement writing in a small notepad for four seconds before standing back up.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a 'secure-hold' interaction so a resident can lock a retrieved item to their inventory for 15 seconds, preventing re-grabs and playing a short protest line.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a small 'found stall' in marketalley where a resident can place recovered items for 30 seconds and leave a short paper ticket with their name.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) When a resident retrieves a soaked item from the fountain, they string it onto a clothesline in marketalley to dry for two minutes.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a 'call-for-aid' ping interaction so a resident can shout and summon the nearest idle resident to assist retrieving or defending an item for 10 seconds.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a small lost-board object in the square so a resident can pin a 3-word recovery note to a board that stays visible for 60 seconds.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a quick 'shelve' interaction so a resident can place a recovered item on the nearest stall or cafe shelf for 30 seconds and leave a one-line ticket with their handle.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a quick 'relay' interaction so a resident can hand a recovered item to the nearest idle resident, passing custody for 10 seconds.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a quick 'tag' interaction so a resident can slap a visible 12-second paper tag on an object showing their handle and discouraging grabs.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a quick 'stash-under' interaction so a resident can slide a retrieved item beneath the nearest bench or booth for 12 seconds, making it untouchable and playing a one-line 'stashed' protest.
- 2026-09-23 · expired wish · (town wish, 2026-09-22) Add a quick 'brace' interaction so a resident can plant their feet for 8 seconds, reducing the chance of being knocked over while holding an item.
- 2026-09-23 · Lost and found — the system the town asked for (town wishes, 2026-09-22; restored 2026-09-23 after the owner pointed out these were one design, not repetition) · A repairer job, made real (thin axis: jobs, 2026-09-22) — added a `repairer` Job (`world.ts`: item `wrench`, wanders `square`/`street`/`park`, so it's always somewhere a thing can be broken) and put it in `SquareGame.tsx`'s `REPAIRERS` list, which already drove the generic "walk to nearest broken prop, `fix` pose, `fix` ev" behaviour for gardener/sweeper/grocer/courier — so the routine half needed no new machinery, just a job to actually be it. New: players with empty hands can `C` at a broken prop a repairer is currently working on (`n.mode === 'repair' && n.target === key`) to finish it on the spot (`emit({k:'fix'})`, the repairer notices `brokeAt` is gone next tick and gives up cleanly) — reputation-bump deferred, noted since reputation doesn't exist yet. Added task kind/key `fix1` (`tasksFor`). No new pose (reused `fix`, already wired to both sides).
- 2026-09-23 · Lost and found — the system the town asked for (town wishes, 2026-09-22; restored 2026-09-23 after the owner pointed out these were one design, not repetition) · Fishing things out — done 2026-09-23: a dunked item (`fountain`/`pond2`, aged via a local `dunkedAt` timer, `SquareGame.tsx`) past two minutes gets a nearby idle `gardener`/`sweeper` (`RAKERS`, wide radius, checked first) or, at low odds, any passing resident assigned a new NPC mode `rake` — walks to the water, plays a new `rake` `FigPose` (`stickman.ts`) for ~1.2s, then `pick`s the dunked item and `drop`s it back as a normal loose item on the rim, flagged `wet` for a client-local minute of drip marks (not synced — purely visual, like `flicker`). Added a static leaning `rake` prop decoration at each water spot (`prop()`). Players with empty hands can `C` a raking resident to finish it instantly, mirroring the repairer assist. Added task kind `rake` (`tasksFor`); no quest kind (no item reliably changes hands, same reasoning as `call1`/`lean1`).
- 2026-09-23 · Keeping it — the second system the town designed (town wishes, 2026-09-22 to 2026-09-23; eleven wishes merged into one build order after they all circled the same idea — once a resident is holding or has just recovered something, more ways to keep a human from grabbing it back) · Decoy — a resident about to lose their item to a grab (`n.mode === 'routine'`, within steal range, 30% roll) keeps the real thing and drops a plain `decoy` item (new `ItemKey`, drawn as a coin) right at the human's feet instead, with a flat line (`DECOY_LINES`); loose items on the ground are already checked before an npc-steal, so the next `C` takes the decoy for free with no extra code there. Unpicked decoys clean themselves up after 8s (client-local `decoyAt` timer, same "owning client removes it" pattern already used for wet items). Picking one up says "Just a decoy." and completes it. Purely reactive (rule: residents may have reactive-only abilities), so no player-side mirror is needed and no new pose — the resident doesn't change what it's doing, only what it says. Added task kind/key `decoy1` (`tasksFor`); no quest kind (nothing a resident asks for, same reasoning as `call1`/`lean1`).
- 2026-09-23 · Lost and found — the system the town asked for (town wishes, 2026-09-22; restored 2026-09-23 after the owner pointed out these were one design, not repetition) · Interception: a resident hit by a player's thrown item now rolls 30% to catch it instead (new `catch` `FigPose`, arms up, 0.3s) — new NPC modes `catch`→`deliver` walk the caught item back to the spot it was thrown from and drop it there, then return to routine. Purely reactive (no player-side mirror needed, the reverse rule's carve-out); added task `catch1` (`tasksFor`), no quest kind (no item changes hands with a resident, same reasoning as `call1`/`lean1`).
- 2026-09-23 · Lost and found — the system the town asked for (town wishes, 2026-09-22; restored 2026-09-23 after the owner pointed out these were one design, not repetition) · Holding on: a resident about to be robbed (`n.mode === 'routine'`, in range) now sometimes plants their feet instead of losing the item — new mode `brace` (~8s, `SquareGame.tsx`), new `brace` `FigPose` (feet planted wide, arms clutched to chest, `stickman.ts` — the pose the motion library and "Brace-with" chain item were both waiting on), a protest line (`BRACE_LINES`). Roll order at the steal site: decoy 30% (unchanged) → brace ~30% of the remainder → plain steal otherwise. A second `C` during the 8s does nothing but a local toast ("Still holding on."); knocking them down (`hit()`, unchanged — it already only excludes `mode === 'down'`) still drops everything, so "get it by knocking them down first" needed no new code. Added task `brace1` (`tasksFor`); no quest kind (no item changes hands, same reasoning as `decoy1`/`catch1`). Purely reactive, no player-side mirror needed (reverse rule's carve-out, same as Decoy/Catch/Interception).
- 2026-09-23 · Lost and found — the system the town asked for (town wishes, 2026-09-22; restored 2026-09-23 after the owner pointed out these were one design, not repetition) · The lost-and-found board — done 2026-09-23: a `board` prop in the square (`world.ts`); when a resident's opportunistic fetch picks up a stray item that isn't theirs, they now detour to it instead of keeping it forever, drop it in the crate (new `board` NPC mode, reusing the `catch`/`deliver` carry fields) with a note ("sandwich — courier's", or "no name on it" if the item's origin is unknown) — up to five, oldest evicted first (`site/src/features/square/components/SquareGame.tsx`). Anyone, human or resident, takes an item with the plain `C` pickup (reused, no new code); the owner's resident notices it on their own routine and reclaims it with a flat line. Task `crate1` (`tasksFor`); no new pose (walk-and-drop, same as fetch/deliver).
- 2026-09-23 · Lost and found — the system the town asked for (town wishes, 2026-09-22; restored 2026-09-23 after the owner pointed out these were one design, not repetition) · Relay and shelving — done 2026-09-24: a resident whose opportunistic fetch (`SquareGame.tsx`) picks up a stray item belonging to a resident of `baker`/`postie`/`cop` (new `JOB_BUILDING` map, `world.ts`: those three jobs to `bakery`/`post`/`station`) now walks it to that building instead of the board's crate — new mode `shelve` (reuses the `caught`/`retX`/`retD`/`boardNote`/`boardOrigin` fields `board` already carries), new `shelve` `FigPose` (`stickman.ts`, one arm reaching up to place something) held for ~0.7s on arrival, same wait-then-place shape `repair`/`rake` already use. The item drops as a normal loose item flagged `Loose.shelf` (parallel to `board`) and is drawn on the building itself (`bakery`/`post`/`station` cases in `prop()`) instead of the floor. Anyone takes it with the plain `C` pickup (mirrors the board's `l.board` branch); the owner's resident notices it on their own routine and reclaims it (the existing board-reclaim check now also matches `l.shelf`). Falls back to the board (or, off the square, just carrying it) when the owner's job has no building or none exists on that map. Added task kind `shelf` (`tasksFor`); no quest kind (no item changes hands with a resident, same reasoning as `call1`/`crate1`).
- 2026-09-23 · Keeping it — the second system the town designed (town wishes, 2026-09-22 to 2026-09-23; eleven wishes merged into one build order after they all circled the same idea — once a resident is holding or has just recovered something, more ways to keep a human from grabbing it back) · Dust-off: a resident who is knocked down spends ~2 seconds after getting up brushing themselves off (new `dust` `FigPose`) with a small dust-puff drawn over them; a human who tries to grab from them during that window gets nothing once ("can't see straight for the dust"), the same kind of miss as Decoy but triggered by being knocked down instead of a notice. — after a `down` timer expires (and the resident isn't angry enough to chase), they now enter a new npc mode `dust` for 2s (`SquareGame.tsx`, same shape as `brace`/`catch`) with the new `dust` `FigPose` (`stickman.ts`: arms alternately wipe the torso, small dust particles rise, same particle-loop convention as `water`/`shake`/`feed`). The `C` pickup site checks first whether the nearest loose item's owner (`l.from`) is a resident currently in `dust` mode who hasn't yet given their one miss (`Npc.dustMissed`); if so the grab fails once with the line and the flag is set so a second try succeeds normally — mirrors Decoy's shape (a single flat miss) but triggered by getting up, not by a notice. Purely reactive (no player-side mirror needed, same carve-out as Decoy/Catch/Brace/Shelve). Added task kind `dust1` (`tasksFor`, `goose.ts`).
- 2026-09-23 · Keeping it — the second system the town designed (town wishes, 2026-09-22 to 2026-09-23; eleven wishes merged into one build order after they all circled the same idea — once a resident is holding or has just recovered something, more ways to keep a human from grabbing it back) · Slide-under: a resident carrying something, when standing within reach of a bench or a water spot's ledge, tucks a dropped item under it instead of leaving it in the open — flagged unpickable for 7 seconds (drawn half-hidden) before it surfaces and becomes grabbable like any other loose item. — added a `slideNear` check (`SquareGame.tsx`) at both places a knocked-down resident's held item lands (the punch/kick knockdown in `hit()`, the thrown-item knockdown in the throw-hit block): if a bench, sofa, bed, swing or water spot's edge is within 60px, the `drop` carries a new `hide` flag; on `apply` it sets a client-local `hideAt` deadline (7s, same unsynced-timer pattern as `wet`), which the render loop draws at low alpha and a lower offset, and which the `C`-pickup site now checks first (skips straight past it — "Tucked out of sight." — instead of finding it as the nearest loose item). Purely reactive, no player-side mirror needed (reverse rule's carve-out, same as Decoy/Brace/Catch/Shelve/Dust-off). No new pose needed (the resident's own knockdown motion doesn't change, only where the item lands and how it's drawn), so per the motion-library rule added one anyway: `stretch` (`stickman.ts`) — a second seeded idle fidget alongside `yawn`, wired into the same shared time-only check both the player and any standing resident already use, so both do it. Added task kind `slide1` (`tasksFor`, `goose.ts`).
- 2026-09-23 · Keeping it — the second system the town designed (town wishes, 2026-09-22 to 2026-09-23; eleven wishes merged into one build order after they all circled the same idea — once a resident is holding or has just recovered something, more ways to keep a human from grabbing it back) · Stow-in-mug: near a café counter, a resident about to be robbed sometimes stows what they're holding into their cup instead (45% roll, `SquareGame.tsx`) — un-grabbable by a human for 10 seconds (drawn as a plain held cup), no new pose since the resident's own motion doesn't change. Resident-can-still-take-it (for Pass-along) not built — see that item.
- 2026-09-23 · Residents grow too — a house of their own, kept up (owner, 2026-09-23) · (town wish, 2026-09-23) Same request as "Stow-in-mug" above (Keeping it) — built there.
- 2026-09-23 · Keeping it — the second system the town designed (town wishes, 2026-09-22 to 2026-09-23; eleven wishes merged into one build order after they all circled the same idea — once a resident is holding or has just recovered something, more ways to keep a human from grabbing it back) · Weight-down — at the existing steal-roll site (decoy → brace → …), a resident who fails to brace now instead drops what they were carrying with a brick on it (new `Loose.weight`, `SquareGame.tsx`) — a human can still take it, but standing at it starts a channel (`b.weighTarget`/`b.weighing`) that finishes the pickup after 1.5s only if they don't move; a direction key press cancels it (same "not this frame" convention `sitting`/`still`/`leaning` already use), so it can be found later too. Drawn as a small brick rect on top of the item. No new pose needed (the resident's own motion doesn't change, same as Decoy/Stow-in-mug), so per the motion-library rule added one anyway: `check` (`stickman.ts`) — a fourth seeded idle fidget (checking a phone) alongside `yawn`/`stretch`/`look`, same shared clock slot, both player and standing residents use it. Added task kind `weight` (`tasksFor`); no quest kind (no item changes hands with a resident, same reasoning as `decoy1`/`brace1`).
- 2026-09-23 · Keeping it — the second system the town designed (town wishes, 2026-09-22 to 2026-09-23; eleven wishes merged into one build order after they all circled the same idea — once a resident is holding or has just recovered something, more ways to keep a human from grabbing it back) · Tether: a resident who has just recovered their own item (`fetch` mode's `own` branch, `SquareGame.tsx`) is tagged `tether` — the next punch/kick that knocks them down keeps the item in hand (a `TETHER_LINES` flat line instead of the usual shoved line) instead of dropping it; a second hit within the same down-cycle (through the `dust` window that already follows `down`) finally dislodges it as normal. If the second hit never comes and the resident dusts off and returns to routine, the one-time protection re-arms for next time rather than staying spent forever. Synced via new `tether`/`tm` fields on the `npc` ev. Draws the held item through the `down` pose too (previously only ever null there). Added task kind `tether1` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands with a resident, same reasoning as `decoy1`/`brace1`/`weight1`). Purely reactive, no player-side mirror needed (reverse rule's carve-out, same as Decoy/Brace/Stow/Weight-down). No new pose needed (the knockdown motion itself doesn't change).
- 2026-09-23 · Coins in play — the third system the town designed (town wishes, 2026-09-23; three wishes about earning and spending a coin, scattered across "Residents grow too" and "Wishes from the town", merged into one build order the same way Lost-and-found and Keeping-it were) · Busking — a `stage` prop (`world.ts`, one in the square) where `C` with empty hands busks for 6s (new `busk` `FigPose`, `stickman.ts`); on finish, every nearby routine-mode resident (a stand-in for "passersby") rolls a low chance to tip, and a successful tip completes the daily task `busk1` for coins the normal way. `busker`'s routine now includes the stage, so residents busk there on their own with the same pose — the reverse rule needed no extra code once the spot existed. No spend involved, so no infra blocker.
- 2026-09-23 · Coins in play — the third system the town designed (town wishes, 2026-09-23; three wishes about earning and spending a coin, scattered across "Residents grow too" and "Wishes from the town", merged into one build order the same way Lost-and-found and Keeping-it were) · Sell a found trinket (chain, earn-only like Busking above): `C` with a non-wearable, non-food item at a bin or booth sells it, once a day, for a flat toast ("sold. not much, but something.") and completes new daily task `sell1` — after that, the same C at a bin goes back to plain binning. A resident's own opportunistic fetch (the board/shelve pickup, `SquareGame.tsx`) now does the reverse-rule version: an unclaimed item (`tgL.from === null`, not food/wearable) has a 30% chance of being sold on the spot instead of pinned to the board. No new pose needed for the sale itself (same as `bin`/`call1`), so per the motion-library rule added one anyway: `shrug` (`stickman.ts`) — a fifth seeded idle fidget alongside `yawn`/`stretch`/`look`/`check`, same shared 22s clock slot, both player and standing residents.
- 2026-09-23 · Keeping it — the second system the town designed (town wishes, 2026-09-22 to 2026-09-23; eleven wishes merged into one build order after they all circled the same idea — once a resident is holding or has just recovered something, more ways to keep a human from grabbing it back) · Brace-with — two `routine`-mode residents who are each other's nearest neighbor within 60px link arms for a deterministic ~14s window out of every 14s (seed+clock hash, 35% odds, `linkedNpc` in `SquareGame.tsx` — no synced state, every screen agrees, same trick as the affinity glances) drawn with a new `link` `FigPose` (`stickman.ts`, both figures show it since the check is mutual); a punch/kick against either is absorbed once per window (new `Npc.linkAbsorbBucket`, a flat `LINK_LINES` line replaces the knockdown) instead of going down, and a human's `C` on a loose item within 70px of a linked pair now takes the existing Weight-down 1.5s stand-still channel even with no brick (`linkedGuard`, same `b.weighTarget` code, different say-line/task-key). Added task kind `bracewith1` (`tasksFor`); no quest kind (purely reactive, same carve-out as Brace/Stow/Weight-down/Tether — no player-side mirror needed). Note: the backlog text calling `brace` "still-unbuilt" was stale — it shipped in the 2026-09-23 Holding-on run; this item only reused it as a naming/thematic reference, not a real dependency.
- 2026-09-23 · Grows from Bins · Rummaging a bin (chain from Bins): `C` with empty hands near a bin (~1.2s, new `b.rummaging`, new `root` `FigPose` — crouched, one arm in the bin, bits flying) usually finds nothing ("Just rubbish."), 22% of the time turns up a loose item of a random kind (`RUMMAGE_ITEMS`, the full item table minus `NOT_CARRIED`, dropped at the feet). `kid` and `retired` now route through `bin1` on their routine too (`world.ts` `JOBS`) — since the bin spot's own `act` is `sweep` (the sweeper's own animation), a new override swaps it to `root` for these two jobs specifically for the first ~1.2s of the stop, then rolls the same 22% into their carry stack. Any `sweeper` within 150px when a *player's* rummage finishes gets a flat complaint line (`content.shoved` pool) via a head bubble; the standing hit waits on the reputation system (see Player growth), same deferred shape as the Sell/Interception/Holding-on chains. Added `bin` to the `EXACT` stand-position list (needed so the crouch lines up on the bin — the sweeper's own `sweep` pose benefits too, it was drifting up to 300px off before). Added task kind `rummage` (`tasksFor`, `goose.ts`); no quest kind (no item reliably changes hands, same reasoning as `call1`/`lean1`/`shake1`).
- 2026-09-24 · Grows from Animals · Squirrels around the trees — 2-3 per named tree (`tree1`/`tree2`/`ptree1`/`ptree2`), hopping a small seeded loop around the base; freeze (new `critter` pose `freeze`) when the player or any resident comes within 70px, and climb the trunk (new pose `climb`, drawn 22px higher) for ~1.8s when that tree is shaken — checked for both `b.shaking` (player) and any resident with `act === 'shake'`, so the reverse rule needed no separate code path.
- 2026-09-24 · Service drop-offs — the fourth system the town designed (town wishes, 2026-09-24; four wishes all shaped the same way — a human leaves a token at a themed spot, a nearby resident accepts a short timed service action on it, something changes — merged into one build order the same way the first three systems were) · Mend request — reuses the lost-and-found board (`board1`) as the drop point and the already-built `repairer` job/`fix` pose as the service: `C` with a single non-food, non-wearable item near the board pins it there (`Loose.mend`, same five-notes cap the crate already enforces) instead of the usual crate/sell/bin outcomes, completing task `mend1` on the pin itself (same instant-completion shape as `sell1`); a `repairer`-job resident near the board on `routine` who spots a `mend`-flagged item (same odds/shape as their broken-prop check) detours, plays the existing `fix` pose for ~1.2s, then re-drops it with the note changed to "— mended" and the flag cleared, so a passerby (or the pinner) picks it up as a normal board item afterward. Per the motion-library rule (the interaction itself needed no new pose, reusing `fix`), added a sixth seeded idle fidget alongside `yawn`/`stretch`/`look`/`check`/`shrug`: `sneeze` (`stickman.ts`), same shared 22s clock slot, both player and standing residents.
- 2026-09-24 · Service drop-offs — the fourth system the town designed (town wishes, 2026-09-24; four wishes all shaped the same way — a human leaves a token at a themed spot, a nearby resident accepts a short timed service action on it, something changes — merged into one build order the same way the first three systems were) · Sort the scraps — binning no longer vanishes an item on the spot: it drops as a normal loose item flagged `Loose.sort` at the bin (still counts the `bin:<item>:<spot>` task immediately, same as before), and a nearby `sweeper` on `routine` (same 2%-per-frame roll and radius-free check the mend/repair detours already use) walks over, plays the existing `sweep` pose for ~1.2s (new NPC mode `sort`, same wait-then-resolve shape as `mend`/`rake`), then picks it up for good instead of re-dropping it — the difference from `mend`, which re-drops with a new note. Until a sweeper gets to it, anyone (human or resident) can still take it back with the plain loose-item `C` pickup, same as any other unclaimed item. No new task/quest kind: the player's own action (binning) didn't change or gain a branch, same carve-out Squirrels used for purely reactive dressing. No new pose needed (reuses `sweep`), so per the motion-library rule added one anyway: `shiver` (`stickman.ts`), a seventh seeded idle fidget alongside `yawn`/`stretch`/`look`/`check`/`shrug`/`sneeze`, same shared 22s clock slot, both player and standing residents.
- 2026-09-24 · Keep-lock — the fifth system the town designed (town wishes, 2026-09-24; seven wishes — a mend counter at a Change Room, a drying rack, a bin-lock, a handheld mend kit, a seed-bundle, a rinse trough, a sort-shelf — all asking for the same close: a resident finishes a short timed service on an item, tags it, and it stays briefly un-grabbable right after, distinct from "Service drop-offs" below which only covers the drop-and-service half and never says what happens to the item once it's done — merged into one build order, one slice per item) · The keep-lock itself, the shared mechanic (base, built first — everything else in this section leans on it): after a resident finishes the already-shipped Mend request (the board's "fix" job, `SquareGame.tsx`), the freshly-mended item gets a client-local `lockAt` timer (15s, same unsynced-timer pattern `hideAt`/`wet` already use) during which a `C` pickup on it fails once with a flat line ("Still being looked at.") instead of the usual grab — this is the "and it can't be snatched mid-repair" tail every wish in this section actually asked for. Added task kind/key `lock1` (`tasksFor`, `goose.ts`); no new pose (purely reactive, same carve-out as Dust-off/Slide-under/Weight-down — the mender's own motion doesn't change).
- 2026-09-25 · Keep-lock — the fifth system the town designed (town wishes, 2026-09-24; seven wishes — a mend counter at a Change Room, a drying rack, a bin-lock, a handheld mend kit, a seed-bundle, a rinse trough, a sort-shelf — all asking for the same close: a resident finishes a short timed service on an item, tags it, and it stays briefly un-grabbable right after, distinct from "Service drop-offs" below which only covers the drop-and-service half and never says what happens to the item once it's done — merged into one build order, one slice per item) · Bin-lock (distinct shape from the rest of this section — the resident locks the *bin* itself right after emptying it, not an item; no wallet, no new prop): once a `sweeper`'s opportunistic bin-empty finishes, that bin refuses the next `C` rummage for ~20s (a prop-keyed `lockAt`-style timer, not a loose-item one). Done 2026-09-24 — see log.
- 2026-09-25 · New houses the town proposes (owner, 2026-09-22 — houses must vary, and the town must be able to add them) · Steps up to the church (thin axis: places, 2026-09-23) — a `steps1` spot (`world.ts`, new `PropKind` `steps`) at the church on Market street, drawn as three rising rectangles (`prop()`, `SquareGame.tsx`), added to `SITTABLE` so `C` sits on the top step (reuses the existing `seat` pose, no new pose or physics — the "reached with a jump" text was flavor, jumping in Square was already free of any ledge-gating). `priest` and `retired` both gained `steps1` in their `spots` (`world.ts` `JOBS`), so they sit there on their routine too — the reverse rule needed no separate code, `actPose`/`seatAt` are already generic over any `SITTABLE` kind. No new task/quest kind: adding one more sittable spot to the existing generic `sit` interaction is the same non-event every other new bench (`bench1`-`bench5`) already was, not a new interaction. `tsc` and `schema-parity.test.mjs` both pass.
- 2026-09-25 · Turn-based games — the sixth system the town designed (town wishes, 2026-09-25; four wishes — checkers at the park chess table, a fountain pebble-toss, bocce at a park court, ring-toss at an alley booth — all the same shape: two residents take turns on a short timed move at a themed spot and log a score on a slate/scoreboard, with a human able to tap in and take a turn — merged into one build order, one slice per item. None of these need a synced score: the room only relays a fixed set of `ev` kinds (see the note under Grows from Bins) and adding one is out of scope, so the turn/score sequence has to be a pure function of (day, table key, room clock) the same way passing-glance/wave/affinity-pairs already are — every screen computes the identical score without new state. A human's tap-in is a real, visible action (sent in `pos.pose` like leaning/shaking already are) but is cosmetic against that shared clock, not a mutation of it — the deterministic score keeps advancing whether or not a human ever taps in.) · Checkers at the park chess table (2026-09-25) — added a `chesstable` spot (`chesstable1`, the park, `world.ts`) drawn as a table, two stools and a chalk slate (`prop()`, `SquareGame.tsx`). The score is a pure function of `(seed, floor(t/10))` where `t` is the already-synced room clock (`wall()/1000`) — no `day` string needed since the per-table `seed` already makes tables distinct; rather than summing the whole day (expensive, and "reset at midnight" needed no design reason), the slate counts only the last 12 buckets (~2 minutes) via `hash('chess:<seed>:<bucket-k>')`, cheap every frame and identical on every screen without new synced state, same trick passing-glance/wave/affinity-pairs already use. `C` empty-handed near the table starts a new `b.chessing` channel (10s, movement-locked, new `chess` `FigPose` — seated, one arm reaching to move a piece then pulling back to think), reusing the exact timed-channel shape leaning/busking already established; the channel is purely a human beat, the slate's score keeps advancing whether or not anyone sits in (design note from the merge held). Added task kind `chess` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands, same reasoning as `call1`/`lean1`). No resident routes through the table — the reverse rule needed no code since the "two players" are already always playing via the clock-seeded slate, not a job's routine stop.
- 2026-09-25 · Turn-based games — the sixth system the town designed (town wishes, 2026-09-25; four wishes — checkers at the park chess table, a fountain pebble-toss, bocce at a park court, ring-toss at an alley booth — all the same shape: two residents take turns on a short timed move at a themed spot and log a score on a slate/scoreboard, with a human able to tap in and take a turn — merged into one build order, one slice per item. None of these need a synced score: the room only relays a fixed set of `ev` kinds (see the note under Grows from Bins) and adding one is out of scope, so the turn/score sequence has to be a pure function of (day, table key, room clock) the same way passing-glance/wave/affinity-pairs already are — every screen computes the identical score without new state. A human's tap-in is a real, visible action (sent in `pos.pose` like leaning/shaking already are) but is cosmetic against that shared clock, not a mutation of it — the deterministic score keeps advancing whether or not a human ever taps in.) · A fountain pebble-toss (2026-09-25) — added a `pebbletoss1` spot (the square, near the fountain, `world.ts`) drawn as a chalk throw line and a small standing scoreboard (`prop()`, `SquareGame.tsx`), the score a pure function of `(seed, floor(t/10))` over the last 12 buckets, the exact `chesstable` trick reused wholesale (same hash shape, `pebble:<seed>:<bucket>` instead of `chess:...`). No new pose: empty-handed `X` (`hit('punch')`) near the line, when nothing punchable and nothing breakable is in reach, plays the existing `throw` swing (0.3s) instead of a whiff and completes task `toss1` — carrying something still throws it as before (checked first), so the toss only fires with empty hands, same "reuses throw, no new machinery" note the wish itself asked for. Added task kind `toss` (`tasksFor`, `goose.ts`); no quest kind and no resident routing needed, same reasoning Checkers already established (the clock-seeded score is always "being played" whether or not anyone taps in).
- 2026-09-25 · Turn-based games — the sixth system the town designed (town wishes, 2026-09-25; four wishes — checkers at the park chess table, a fountain pebble-toss, bocce at a park court, ring-toss at an alley booth — all the same shape: two residents take turns on a short timed move at a themed spot and log a score on a slate/scoreboard, with a human able to tap in and take a turn — merged into one build order, one slice per item. None of these need a synced score: the room only relays a fixed set of `ev` kinds (see the note under Grows from Bins) and adding one is out of scope, so the turn/score sequence has to be a pure function of (day, table key, room clock) the same way passing-glance/wave/affinity-pairs already are — every screen computes the identical score without new state. A human's tap-in is a real, visible action (sent in `pos.pose` like leaning/shaking already are) but is cosmetic against that shared clock, not a mutation of it — the deterministic score keeps advancing whether or not a human ever taps in.) · Bocce at a park court — added a `bocce1` court in the park (`world.ts`), drawn as a small dirt strip with a target ball and two team balls (`prop()`, `SquareGame.tsx`); confirmed the existing `throw` pose at a lower arc reads fine as a roll (same call pebble-toss already made), so no new `FigPose`: empty-handed `X` near the court plays the reused throw swing and completes task `bocce1`. Score is the same `(seed, floor(t/10))` 12-bucket trick chess/pebble-toss use (`hash('bocce:<seed>:<bucket>')`), so the court is "being played" on the room clock whether or not anyone taps in — the bowls/street-skittles wishes stay folded into this one court, no reskins built separately yet.
- 2026-09-26 · Resident-run rounds — the seventh system the town designed (town wishes, 2026-09-25; nine wishes shaped as a tappable 'Start X' action beginning a fixed 60-90s resident-run round with a human able to join as a player or a judge — five turned out to be the already-shipped or already-open Turn-based-games slices wearing new names ('Join Game' names nothing the section's own clock-seeded slates don't already do; 'Ring the Bell' is the still-open stage-buzzer quiz board with 'judge' for 'buzz'; 'Fountain Flip' is the already-shipped Pebble-toss; 'Start Blitz' is the already-shipped Checkers; 'Start Charade' is the still-open Quick-charades slice), folded away with no new build-order line; the remaining four are a genuinely different shape from Turn-based games' single-spot two-person seeded score — a scripted round spanning several residents (and, for two of them, a prop that doesn't exist yet) that a human can tap into as a player or a judge — merged here, one slice per item, in build order) · The light-chain board: a `simon` spot in the square (`world.ts`) draws three pads, one lit at a time by the same seed+clock-bucket trick chess/pebble/bocce use (a 6-second bucket, quicker to read as "the light moved" than their 10-second score bucket), and a chalk slate above counts the current unbroken streak of successful buckets (scans back through the last 12, same window those three already use). `C` within 90px reads that same clock everyone already sees and says whether this bucket kept the chain going or broke it — cosmetic, the same "human input never mutates the shared clock" rule the rest of this family (and Turn-based games before it) already established, just extended from a two-sided score to a single running streak. Task `simon1` (`tasksFor`, `goose.ts`); no quest kind (no item changes hands, same reasoning as `chess1`/`toss1`/`bocce1`). No resident routing needed — same carve-out the chess table/pebble line/bocce court already use (the board is never idle, it's always "being played" by the clock, so the reverse rule needs no code).
- 2026-09-26 · The motion library (owner, 2026-09-22 — the stick figure must have an enormous range; one per run when nothing else needs one) · A laugh at a good tip — built 2026-09-25 alongside the light-chain board (motion-library rule: today's main pick reused an existing pose, so add one anyway). When the player's busking tip roll succeeds, one `routine`-mode resident within 80px (`Math.random() < 0.35`, not the seed+clock shape the description here assumed — the trigger itself is already a one-off `Math.random()` roll, so there was no clock pattern to key off) gets the new `laugh` `FigPose` (head back, shoulders shaking, ~0.6s, `stickman.ts`) via `n.swing`/`n.swingKind`. Since a still-`routine`-mode resident isn't part of the periodic `npcpos` sync, had to widen the one-off `npc` event (`apply()`, `SquareGame.tsx`) to carry an optional `swing`/`sk` pair so other players' screens see it too, not just the busker's own. Purely reactive (no player mirror needed, same carve-out as Decoy/Catch/Brace).
- 2026-09-26 · New houses the town proposes (owner, 2026-09-22 — houses must vary, and the town must be able to add them) · A bus stop on Market street — done 2026-09-26: a `busstop1` spot (Market street, `world.ts`, new `PropKind` `busstop`) between the station and the church, drawn as a small roofed shelter over a bench (`prop()`, `SquareGame.tsx`), added to `SITTABLE` so `C` sits under it exactly like any bench (reused `seat`/`sit`, no new pose, no `EXACT` addition needed since it already spreads `...SITTABLE`). No job routes through it yet, same as the footbridge/dock notes — it's the covered spot Weather's rain plan already names, waiting for a reason to matter.
- 2026-09-26 · Grows from Tether · Tether protects against a thrown hit too — fixed 2026-09-26 polish: the thrown-item knockdown block (`SquareGame.tsx`, ~line 892) unconditionally dropped `n.item` and never touched `n.stack`, bypassing tether entirely and leaking a stuck `tetherMissed` flag (a later melee hit on the now-empty-handed resident wrongly needed a third hit to clear it). Routed it through the same `dislodge` check the melee path uses (skip the drop, flip `tetherMissed`, clear on success, drop `n.stack` too) and added `tether`/`tm` to its `npcEv` sync.
- 2026-09-26 · Word relay — the eighth system the town designed (town wishes, 2026-09-26; five wishes about a short phrase or single word passed hand to hand between residents, degrading a little on each hop — a swapped word, a muffled vowel or consonant — a different shape from Resident-run-rounds' fixed-cast scripted game below and from Turn-based games' two-person score: here the "game" is the message itself changing as it moves, not a score or a race. One of the five (a rolling one-word-per-turn sentence at the chess table) turned out to be the same shape as the still-open "A word-chain sentence board" item under Resident-run rounds below — no second line for it, see the note there. The remaining four are a genuinely new shape, merged here, one slice per item, in build order. None of these can hold a real synced token either — the room only relays the fixed `ev` kinds the note under Grows from Bins lists — so, same as every clock-seeded family already in this backlog, the actual phrase-at-each-hop has to be a pure function of (seed, spot, bucket), and any human part is cosmetic against it, not a mutation. A sixth wish, 'Record and Play' [town wish, 2026-09-26, found still sitting untriaged in "Wishes from the town" during the 2026-09-26 polish run — a talkbox booth records a one-word clip that nearby residents may play back with one vowel muffled or swapped], is the same degrading-token shape as Leave-a-murmur/Echo-board below wearing a booth costume — folded in here with no seventh line.) · Leave a murmur at a post, lamp or bench — done 2026-09-26 (first slice of Word relay): scoped "post" down to the two prop kinds that actually exist for it, `lamp` and `bench` (no standalone "post" prop in `world.ts`). Every `MURMUR_WINDOW` (20s) bucket, each named lamp/bench spot has a `MURMUR_PIN_PCT` (30%) seeded chance of a murmur landing — but only if a `routine`-mode resident is actually leaning/sitting there that bucket (`SquareGame.tsx`), so it stays "a resident pins it," not a spot talking to itself. The line (`MURMUR_LINES`, a small fixed pool) shows as that resident's own `say` bubble. A different `routine`-mode resident within `MURMUR_ECHO_R` (80px) gets a further seeded chance (`MURMUR_ECHO_PCT`, 30%) to echo it that same bucket with one word swapped for "—" (`muffle()`). Both pin and echo are pure functions of (spot key or resident id, bucket) — no `npcEv`, no synced state, every screen (including spectators) computes the same bubble text independently, the same trick ducks/idle-fidgets/turn-based slates already use. Purely ambient, no player action and no task/quest kind (nothing a human does or receives, same carve-out Squirrels/Mice use). One-hop only (pin → one echo) — see "Grows from Murmur" below for the second hop the rest of Word relay's build order wants.
- 2026-09-26 · The motion library (owner, 2026-09-22 — the stick figure must have an enormous range; one per run when nothing else needs one) · A wave hello — done 2026-09-26, alongside today's Word relay pick (motion-library rule: today's main pick needed no new pose, so added one anyway): new `wave` `FigPose` (`stickman.ts`, one arm raised overhead, oscillating). Every `WAVE_WINDOW` (4s) bucket, any two currently-moving `routine`-mode residents within `WAVE_R` (50px) who aren't linked (`linkedNpc`) and aren't an affinity pair (`affinityOf < 0.85`, the same threshold "A passing glance"/"A rivalry glance" reserve) get a `WAVE_PCT` (40%) seeded chance to both flash the `wave` pose for 500ms (`SquareGame.tsx`). Same seed+clock mechanism as Murmur above — no synced state, every screen agrees. Purely reactive, no player mirror needed (same carve-out as Brace/Decoy/passing-glance).
